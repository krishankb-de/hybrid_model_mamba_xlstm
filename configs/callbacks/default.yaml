# Default callbacks configuration

# Model checkpointing
checkpoint:
  monitor: "val/loss"
  mode: "min"
  save_top_k: 3
  save_last: true
  filename: "checkpoint-{epoch:02d}-{val_loss:.4f}"
  auto_insert_metric_name: false
  every_n_epochs: null
  # 500 steps ≈ 17 min at 2s/step on A100-40GB bf16 — bounds worst-case loss
  # on a 36-48h Willi walltime to ≤1 cadence window.
  every_n_train_steps: 500

# Early stopping
early_stopping:
  enabled: false
  monitor: "val/loss"
  patience: 10
  mode: "min"
  min_delta: 0.001

# Learning rate monitoring
lr_monitor:
  enabled: true
  logging_interval: "step"

# Model summary
model_summary:
  enabled: true
  max_depth: 2
