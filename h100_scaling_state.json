{
  "current_phase": "phase11_report_gen_eval_full_data",
  "last_updated": "2026-08-30T11:30:00+0200",
  "OBJECTIVE_PIVOT_2026_08_16": {
    "new_primary_objective": "MEDICAL REPORT GENERATION scored by ROUGE-L + CheXbert F1 against ground-truth radiology reports. NOT retrieval.",
    "retrieval_status": "SUPPORTING CHAPTER -- complete, closed, numbers FINAL. Do not re-run retrieval arms to chase them. Clean protocol MIMIC i2t R@10 10.81% -> 14.59% (+3.78pp, 6.2 SE); protocol-matched (val==test) 11.07% -> 17.14%; Indiana flat within noise throughout (0.0390-0.0485, SE 0.76pp).",
    "why_defensible": "Retrieval is finished as a research question here: 10 clean nulls, 1 dominant lever (ViT adaptation depth) and that lever is EXHAUSTED (depth saturated at 12/12 blocks, vit_lr is an inverted-U with 1e-6 already optimal, scope null). The two live constraints are both DATA not method (Indiana data-bound; image tower memorises the 27570-pair set as soon as vit_lr rises, 6G-1 train/clip_loss 1.17->0.04 while val 2.585->3.49). Report generation also exercises the causal Mamba/mLSTM decoder AS A GENERATOR, which retrieval never did -- it only ever used it as an encoder.",
    "phase_renumbering": "OLD Phase 7 (multi-source CXR) -> demoted to 9G, optional. NEW Phase 7 = PhysioNet credentialing (BLOCKING). NEW Phase 8 = local MIMIC-CXR-JPG build. NEW Phase 9 = retrieval on full data (Arm 0/1/2 + augmentation + selection-protocol rerun). NEW Phase 10 = image-conditioned report generator. NEW Phase 11 = report-gen eval. OLD Phase 8 (writeup) -> Phase 12."
  },
  "DIAGNOSIS_is_it_overfitting": {
    "short_answer": "NO -- the model is not currently overfitting at its operating point. Overfitting is the WALL THAT PINS THE ONE WORKING LEVER AT ITS MINIMUM SETTING. Read this before proposing any data or regularisation lever.",
    "three_statements_that_look_contradictory_but_are_about_different_things": {
      "overfitting_is_NOT_binding": "Scope = TEXT TOWER + OPTIMIZER. Epochs 23->14 moved R@10 +0.06pp (0.1084->0.1090). 6B-3: lower head_lr REMOVED the late-epoch rollover entirely (arm ends at its max, no decline) and the PLATEAU HEIGHT DID NOT MOVE (0.119 vs 0.122). THIS IS THE DECISIVE EXPERIMENT: it eliminated the overfitting SYMPTOM and the ceiling did not move, so whatever sets the ceiling is not overfitting.",
      "the_image_tower_memorises": "Scope = ViT AT ELEVATED vit_lr. 6G-1: train/clip_loss 1.17 -> 0.702 -> 0.113 -> 0.037 as vit_lr 1e-6->3e-5, while val/clip_loss RISES 2.585 -> 3.487. Overfit onset moves earlier monotonically: best step 4750 -> 3250 -> 2500 -> 1750.",
      "binding_constraint_is_the_image_representation": "Scope = THE ACTUAL DIAGNOSIS. 6C: every text-side and objective-side lever null; both positives ever recorded are image-side."
    },
    "real_characterisation": "'Amount of image adaptation' = DEPTH x LR x SCOPE and all three are now measured out: depth PHYSICALLY EXHAUSTED (12/12 ViT-B/16 blocks), scope NULL (0.1704 vs 0.1714), LR an INVERTED-U with 1e-6 already optimal. No dose left to give. The optimum is pinned at 1e-6 -- three orders below head_lr -- because it is 85.1M trainable image params against 27,570 images. THE MODEL SITS AT THE OVERFITTING KNEE: not broken by overfitting, DOSE-LIMITED by it. Mild real overfitting does exist at the canonical point (best step 4750/6000, mild late rise; the 6G-7 protocol cost 2.55pp at vit=12 vs 0.26pp at vit=2 is precisely a measurement of how much) -- but no points are being LOST to it; the loss is the points turning the lever up WOULD have bought.",
    "consequence_for_phase9": "CHANGES WHAT COUNTS AS SUCCESS. WRONG framing 'more data fixes overfitting' predicts the current number rises on its own. RIGHT framing 'more data moves the knee right' predicts the vit_lr optimum SHIFTS RIGHT of 1e-6 and the peak is higher -- that is 9C's pre-registered prediction and the correct test. THEREFORE: if 9B (data only, recipe unchanged) barely moves, THAT IS NOT A FAILURE, it is the expected result under the correct mechanism. 9C is where the effect must appear.",
    "indiana_caveat": "Indiana is flat within noise on EVERY variant tested (0.0390-0.0485, SE 0.76pp). That is a DOMAIN-DIVERSITY problem, not overfitting -- and full MIMIC is more of the SAME domain. Expect partial help at best; do NOT bank the Indiana gate on it. That is what 9G and VinDr-CXR are for.",
    "after_the_pivot": "For REPORT GENERATION the blocker is neither overfitting nor data -- there is NO IMAGE-CONDITIONED DECODER AT ALL (hybrid_lm.py:147 forward() takes only input_ids). That is a capability gap, and it is Phase 10. The overfitting question belongs to the retrieval chapter and is now answered and closed."
  },
  "report_generation_success_bar": {
    "split": "OFFICIAL mimic-cxr-2.0.0-split.csv.gz test split (subject-disjoint by construction), ~3.3k findings-bearing studies",
    "floor": {
      "rouge_l": 0.15,
      "chexbert_f1_micro": 0.25,
      "rule": "beats a retrieval-NN baseline; proves the decoder conditions on the image at all"
    },
    "target": {
      "rouge_l": 0.22,
      "chexbert_f1_micro": 0.4,
      "rule": "competitive with R2Gen-class published baselines"
    },
    "stretch": {
      "rouge_l": 0.26,
      "chexbert_f1_micro": 0.5,
      "rule": "competitive with strong modern RRG systems"
    },
    "secondary": "BLEU-4, METEOR",
    "PRE_REGISTERED_CONTROL": "The retrieval-NN baseline (retrieve nearest training report with the Phase-9 model, emit verbatim) is run FIRST and its scores become the REAL floor. MIMIC reports are heavily templated (6C-3: 2% of the gallery is exact-duplicate) so n-gram metrics reward copying. A generator that does not beat its own retrieval baseline has contributed nothing."
  },
  "mimic_full_build_decisions": {
    "source_project": "MIMIC-CXR-JPG v2.1.0 (377,110 JPGs, ~570 GB) -- NOT MIMIC-CXR DICOM (4.7 TB). Reports from MIMIC-CXR v2.1.0 mimic-cxr-reports.zip (~135 MB). DUA signed SEPARATELY per project under one credentialing.",
    "storage_strategy": "chunked download -> downscale in flight -> DELETE originals. ~310-400 GB transferred, ~6 GB kept. Peak disk ~4 GB regardless of corpus size. Fits the 200 GB HPC quota.",
    "stored_resolution": "320 px square (not 224) -- +3 GB, preserves RandomResizedCrop(224) headroom. Square resize is deliberate parity with the existing T.Resize((size,size)).",
    "selection": "frontal only (PA/AP), ONE image per study -> ~190-210k pairs before the text filter.",
    "primary_eval_split": "official mimic-cxr-2.0.0-split.csv.gz. The legacy train[90%:] N=3063 gallery is a CONTINUITY number only, and only if the 8D leakage-join recall check passes."
  },
  "builds_on": "HYBRID_ARCH_REFACTOR_PLAN.md + hybrid_arch_refactor_state.json (COMPLETE — MIMIC i2t R@10 8.23%->10.45% Target tier; Indiana 3.90% intrinsic; Stage-0 PPL 15.62). That plan is finished; historical reference only.",
  "primary_thesis": "Migrate the completed A100-40GB pipeline to H100 (94/141GB) and use the headroom on the three A100-era ceilings: (1) contrastive negatives capped at ~31 (in-batch only, no all_gather, moco=0) -> scale true per-step batch to 128-256; (2) Stage-0 undertrained (bs=8/40GB, 40K of ~117K steps) -> finish it; (3) 70M cap -> port v2 arch to 150M. Plus multi-source CXR data (IU-Xray excluded) for the intrinsic Indiana gap. Clean per-lever attribution held throughout.",
  "approved_plan": "/Users/krish/.claude/plans/i-want-to-implement-twinkling-ullman.md",
  "branch": "h100_scaling",
  "hardware": {
    "primary": "single H100 80GB (aisc gx07-13; measured 81559MiB — NOT the 94/141GB originally assumed; see 2026-07-08 notes)",
    "secondary": "2-4 H100 node (Phase 3 all_gather)",
    "slurm_partitions": {
      "training": "aisc-batch (7-day cap)",
      "eval_smoke": "aisc-shortrun (1-day cap)"
    },
    "gres": "--gres=gpu:h100:X (X=1..8)"
  },
  "success_bar": {
    "floor": {
      "MIMIC_i2t_R10": 0.1045,
      "Indiana_i2t_R10": 0.0404,
      "stage0_ppl": 15.62,
      "rule": "no regression vs current best; recover Indiana floor"
    },
    "target": {
      "MIMIC_i2t_R10": 0.12,
      "Indiana_i2t_R10": 0.055,
      "stage0_ppl": 13.76,
      "rule": "MIMIC old-stretch parity + Indiana uplift + backbone PPL gate"
    },
    "stretch": {
      "MIMIC_i2t_R10": 0.14,
      "Indiana_i2t_R10": 0.07,
      "stage0_ppl": 13.1,
      "rule": "dissertation headline; matches baseline PPL"
    }
  },
  "resolved_decisions": {
    "slurm": "training -> aisc-batch (7-day); eval/smoke -> aisc-shortrun (1-day); --gres=gpu:h100:X. 7-day cap => full Stage-0 in ONE block.",
    "model_scale": "150M v2 — port v2 arch (HybridNorm hybrid topology, middle-mLSTM 25% ratio, attention pooling, mLSTM stabilization) into hybrid_150m_v2; fresh Stage-0.",
    "priority": "backbone + MIMIC — finish Stage-0, then scale contrastive batch to push MIMIC to stretch.",
    "indiana_data": "add diverse CXR (CheXpert/PadChest/VinDr); IU-Xray EXCLUDED from training (it IS the 743-pair Indiana eval set — zero-leakage).",
    "per_source_text": "free-text passthrough (MIMIC findings+impression; PadChest translated-EN) + label->prompt template (CheXpert/VinDr structured labels -> synthetic report sentences, CheXzero-style).",
    "max_position_embeddings_150m": "1024 (v2 parity; PubMed-512 / CXR-256 corpora fit).",
    "kd_teacher": "BioMedLM 2.7B primary; larger frozen biomedical teacher (BioGPT-Large/Meditron-7B/OpenBioLLM-8B) fallback if PPL gate missed (141GB fits 7-8B).",
    "python": "target py>=3.10 on H100 (verify stack; fall back to 3.9 if a dep breaks). Keep 3.9-syntax hygiene (forward-compatible; preserves willi bridge).",
    "multi_gpu_phase3": "secondary — deferred until after single-H100 MIMIC win (Phase 6)."
  },
  "phases": {
    "phase1_plan_of_record": {
      "status": "complete",
      "goal": "Create plan-of-record + resumable state + gitignore/CLAUDE.md wiring; validate; commit on h100_scaling branch.",
      "tasks": {
        "1A_create_plan_md": "done",
        "1B_create_state_json": "done",
        "1C_gitignore_allowlist": "done",
        "1D_claude_md_repoint": "done",
        "1E_validate_for_willi": "done — 69 passed, 5 skipped, 9/9 gates green",
        "1F_commit": "done"
      }
    },
    "phase2_h100_infra": {
      "status": "local_complete_server_pending",
      "goal": "H100 trainer configs (single-GPU primary + 2-4 node DDP file), SLURM templates (aisc-batch/shortrun), py>=3.10 env verify, parity tests, H100 smoke.",
      "tasks": {
        "2A_h100_single_gpu_yaml": "done",
        "2B_h100_multi_ddp_yaml": "done",
        "2C_slurm_templates": "done — train_stage0_h100.sh / train_biomedclip_kd_h100.sh / eval_h100.sh (ENV-parametrized)",
        "2D_py310_env_verify": "pending_h100_box",
        "2E_flash_attn": "done — documented opt-in (not forced into base reqs)",
        "2F_parity_tests": "done — test_h100_trainer_configs_resolve (2 cases green)",
        "2G_h100_smoke": "pending_h100_box",
        "2H_validate_commit": "done — validate green 9/9 gates"
      }
    },
    "phase3_distributed_negatives": {
      "status": "pending_secondary",
      "goal": "Grad-preserving all_gather in CLIP loss (gated world_size>1). Deferred until after Phase 6 single-H100 MIMIC win."
    },
    "phase4_150m_v2_config": {
      "status": "complete",
      "goal": "hybrid_150m_v2.yaml (dim=768, 12 layers, [m,m,m,m,L,L,L,m,m,m,m,m], hybrid norm, attention pool). Param-count verified.",
      "actual_param_count": "183.72M (nominal '150M'; untied 50k-vocab embeddings ~77M dominate — consistent with 70M config -> 83M naming convention)",
      "learning_rate": "4.0e-4 (sqrt-width scaled from 6e-4; tunable to 5e-4)",
      "tasks": {
        "4A_config": "done",
        "4B_layer_pattern": "done — [m,m,m,m,L,L,L,m,m,m,m,m] 3 mLSTM centered (25% ratio)",
        "4C_parity_test_param_count": "done — test_hybrid_150m_v2_config_and_param_count, tight band [181,186]M",
        "4D_validate_commit": "done — 72 passed, 9/9 gates"
      }
    },
    "phase5_stage0_150m": {
      "status": "COMPLETE_final_PPL_13.18_stretch_tier",
      "goal": "Stage-0 pretrain 150M v2 on H100 (~3B tokens Chinchilla, bs=48, WSD). Gate PPL <= 13.76. BioMedLM teacher primary + fallback.",
      "launch_script": "scripts/train_stage0_150m_h100.sh (aisc-batch, gpu:h100:1, 4-day, MAX_STEPS=120000 BATCH_SIZE=48 overridable)"
    },
    "phase6_contrastive_batch_scale": {
      "status": "COMPLETE_null_result_best_i2t_R10_0.1113_floor_cleared_target_missed",
      "goal": "Canonical recipe held; batch sweep {64,128,256}, accum=1, LR sqrt-scaled. Gate MIMIC i2t R@10 >= 12% (stretch).",
      "launch_script": "scripts/train_biomedclip_kd_150m_h100.sh (aisc-batch, gpu:h100:1, BATCH_SIZE={64,128,256} overridable; needs Phase-5 stage0_model_only.pt)"
    },
    "phase7_physionet_credentialing": {
      "status": "7A_7B_7C_7D_7E_DONE_2026-08-16__only_7F_quota_check_remains",
      "user_confirmed": "CITI training, PhysioNet credentialing, and DUA (both mimic-cxr/2.1.0 and mimic-cxr-jpg/2.1.0) are DONE as of 2026-08-16. PhysioNet username: bhushkri.",
      "goal": "CITI 'Data or Specimens Only Research' course -> PhysioNet credentialed account application (CITI completion REPORT pdf + research-use description + supervisor reference) -> sign DUA on BOTH mimic-cxr/2.1.0 AND mimic-cxr-jpg/2.1.0 -> verify a real download.",
      "CRITICAL": "This project has NEVER had PhysioNet credentialing (see note 2026-07-25: 'no PhysioNet credentialing yet, so full-MIMIC scale-up stays out of scope'). The reviewed proposal's claim 'same DUA and CITI training you already have; it's a click' is FALSE. Realistic lead time 1-4 weeks, dominated by human review -- the supervisor reference must actively respond to an email, which is the usual source of delay.",
      "tasks": {
        "7A_citi_training": "DONE (user, 2026-08-16)",
        "7B_credentialing_application": "DONE (user, 2026-08-16)",
        "7C_dua_both_projects": "DONE (user, 2026-08-16) -- both mimic-cxr and mimic-cxr-jpg",
        "7D_netrc_setup": "SUPERSEDED 2026-08-16 -- .netrc/HTTP Basic Auth does NOT work against PhysioNet for this project. See 7D_auth_mechanism_correction below. A leftover ~/.netrc for physionet.org is harmless (simply unused by the current code) but can be removed.",
        "7D_auth_mechanism_correction": "CORRECTED 2026-08-16 after a long live diagnostic chain that ruled out netrc parsing, password correctness (tested with a fresh plain-alphanumeric password), file formatting (cat -A clean), and User-Agent/WAF filtering (browser UA also 403'd) one at a time. DEFINITIVE TEST: curl -u user https://physionet.org/settings/profile/ -> 302 to /login/ REGARDLESS of credential correctness; curl -H 'Authorization: Basic ...' against a /files/ URL -> 403; IDENTICAL URL with a session cookie -> 200. PhysioNet's Django deployment does not honour HTTP Basic Auth for this project at all -- the wget --user --ask-password recipe on PhysioNet's own project pages (which the original reviewed proposal cited) is stale for this deployment. FIX: build_mimic_cxr_local.py rewritten to use session-cookie auth. _get_session() reads ~/.physionet_session (chmod 600, raw sessionid value only) and raises loudly if absent -- no silent fallback to a Basic-Auth path known not to work. Get the cookie from a logged-in browser: dev tools -> Application/Storage -> Cookies -> physionet.org -> sessionid. TREAT LIKE A PASSWORD: a live session cookie lets anyone holding it act as the user until it expires or they log out; one was briefly pasted in chat during this debugging session and the user was advised to log out (invalidates it) and use a fresh one. SEPARATE BUG CAUGHT AND FIXED IN THE SAME PATCH: when the cookie expires MID-fetch, PhysioNet 302s to /login/ and requests follows redirects by default, so this arrives as an ordinary 200 with an HTML login page as the body -- without a guard this gets written straight into a .jpg/.csv.gz and the resume check (Path.exists()) then skips the corrupt file FOREVER on every subsequent run (discovered only weeks later as garbage in the training set). _download() now checks Content-Type/resp.url BEFORE streaming any bytes and returns a SESSION_EXPIRED (498) sentinel instead of 200; stage_fetch aborts the WHOLE run immediately and unconditionally on seeing it (not folded into the ok==0 check, since a mid-chunk expiry can leave ok>0 in the same chunk that also has SESSION_EXPIRED entries). 4 new unit tests added (mocked, no network): missing-cookie-file raises; login-page-shaped-200 detected + writes nothing; genuine-200 still writes correctly; partial-success-then-expired chunk still hard-aborts. validate_for_willi.sh GREEN 9/9, 104 passed (was 100) 5 skipped.",
        "7D_verify": "DONE 2026-08-16, confirmed live end-to-end. Job 2457693: all 4 small files + IMAGE_FILENAMES fetched, session cookie auth confirmed, reports.zip unzipped cleanly, '[meta] done -> /sc/home/krishankumar.bhushan/dataset/mimic_smoke_test'. ~30 min wall (13:19-13:49), confirming the script's 1-day --time default (not the earlier borrowed 10-minute override) was the fix. ALSO CAUGHT: configs/dataset/cxr_mimic_full.yaml's local_parquet_dir default had used the PhysioNet username (bhushkri) instead of the cluster/OS username (krishankumar.bhushan) in a filesystem path -- fixed, along with the same mistake in dump_legacy_gallery_hashes.py's --cache-dir default. History of what it took to get here, preserved below. Job 2457565 (manual override --account=aisc --partition=aisc-batch --qos=aisc) actually RAN: auth succeeded, 3 of 4 small files fetched before a manual --time=00:10:00 override killed it mid-download of mimic-cxr-reports.zip. Script defaults fixed to bake in that exact working account/partition/qos combo. RETRY (job 2457605) hit a SECOND, more serious bug: stage_meta printed '[meta] have mimic-cxr-reports.zip' (treating the killed job's truncated-but-nonzero-size zip as already-downloaded) and crashed with zipfile.BadZipFile at unzip time. ROOT CAUSE: _download() wrote directly to dest with no atomicity, so a killed job left a partial file that every resume check (dest.exists() and size>0) trusted as complete. FIXED PROPERLY: _download() now streams to a dest.name+'.part' sibling and Path.replace()s into dest only after the full body is consumed -- dest is therefore either absent or complete, never partial. Fixing THIS surfaced a THIRD bug in the same function: the HTTP status line (200) arrives before the body streams, so a connection dying mid-body was returning a bare 200 even though dest was correctly never written -- silently reporting success for a failed download. Restructured so body-stream failures are caught in their own scope, last_status is not left stale at a misleading 200, and the attempt retries as a fresh request. 2 new unit tests (was 4, now 5 for this file's auth/download logic): '.part' file is not left behind after a clean success (regression guard on the existing happy-path test); a simulated mid-stream ConnectionError never returns 200 and never creates dest. validate_for_willi.sh GREEN 9/9, 105 passed (was 104) 5 skipped. RETRY of the retry (job 2457659) hit the SAME BadZipFile again, in only 3 seconds -- turned out to be a SIXTH, purely operational issue, not a further code bug: /tmp IS NODE-LOCAL, not shared, and every job so far had landed on the same compute node (gx17v1). rm -rf /tmp/mimic_smoke_test run on the login node (lx01) had ZERO effect on gx17v1's own /tmp -- the corrupt zip from the FIRST killed job (2457565) had been persisting there the whole time, immune to every login-node rm. Confirmed the atomic-write fix WAS correctly deployed (grep -n 'part.replace|SESSION_EXPIRED' on the cluster copy showed the patched lines) -- it simply never got a chance to matter against pre-existing contamination a login-node rm couldn't reach. FIX: stop using /tmp for cluster smoke tests; use the shared filesystem instead (/sc/home/$USER/dataset/..., the same one the real build already targets). Re-verify: STAGE=meta OUT=/sc/home/$USER/dataset/mimic_smoke_test sbatch scripts/build_mimic_cxr_local.sh (brand-new path, no rm needed) -- watch for '[meta] done' and confirm '[meta] unzipping reports' does not crash. Six prior client-side/mechanics issues (NONE about the actual PhysioNet credentials) now resolved: login-node block, missing --account, wrong-QOS account (aisc on cpu-batch), submit-limit account (default), .netrc/HTTP-Basic-Auth-not-supported -> session-cookie auth, and node-local /tmp. TRADEOFF ACCEPTED (still open, worth a helpdesk email before the long fetch): aisc-batch is GPU-capable and preempted at any time for a job using zero GPU.",
        "7E_verify_network_path": "ANSWERED 2026-08-16, confirmed live + against docs.sc.hpi.de (fetched via WebFetch: Login-Nodes, Data-Transfer, Partitions, Run-Nodes pages). Login node rejects ALL script execution outright (confirmed live, not just 'heavy' commands -- stronger than the earlier known `bash <script>` block). Official recommendation: external downloads on COMPUTE nodes via Slurm -- cpu-interactive (8h cap) for meta/manifest/pack, cpu-batch (7-day cap) for fetch. NOT a Run Node (rx01/rx02, 8GB/4-core, explicitly NOT for data acquisition). NOT an AISC GPU partition (zero GPU need here). scripts/build_mimic_cxr_local.sh (NEW) implements this: STAGE env var selects stage, submits to cpu-batch, no --gpus, no --account=aisc (general partitions, not the AISC allocation). COURTESY STEP NOT YET DONE: docs state verbatim 'Always contact helpdesk before transferring large datasets' -- email sc-helpdesk@hpi.de before the full fetch. CORRECTION to earlier 'compute nodes are offline' characterization: that was about a SPECIFIC 401 gated-dataset auth failure (HF_DATASETS_OFFLINE=1), not a proven lack of general egress -- the official docs recommending downloads FROM cpu-interactive/cpu-batch would be nonsensical if those nodes had no route out. This build never touches a GPU node anyway.",
        "7F_quota_check": "pending -- bytes AND INODES. ~227k report .txt + ~200k jpg = ~430k inodes, which can trip an inode cap long before the 200 GB byte cap."
      },
      "do_while_waiting": "Phase 10A/10B (decoder architecture + tests) and 11A (metric harness) need NO data and can be built and unit-tested against the existing 27.5k mirror."
    },
    "phase8_mimic_full_local_build": {
      "status": "CODE_COMPLETE_2026-08-16_network_stages_pending_7D_7E",
      "what_shipped": {
        "scripts/build_mimic_cxr_local.py": "four stages (meta/manifest/fetch/pack), requests-based (not wget/curl) -- sidesteps the 5 corrections below entirely rather than patching around them",
        "scripts/mimic_cxr_vendor/section_parser.py": "VERBATIM copy of MIT-LCP/mimic-cxr@e8d26fff txt/section_parser.py (fetched 2026-08-16 via curl, commit SHA recorded in file header)",
        "scripts/mimic_cxr_vendor/extract.py": "per-study findings/impression extraction, ported from MIT-LCP/mimic-cxr@18cdc41c txt/create_section_files.py's per-study logic (custom_indices/custom_section_names overrides -> section_text() -> last-matching-index for findings AND impression separately, per Phase 8F)",
        "scripts/dump_legacy_gallery_hashes.py": "NEW -- produces the pack --exclude-hashes input by hashing the legacy train[90%:] gallery with the IDENTICAL text construction/normalisation MIMICValDataset uses",
        "configs/dataset/cxr_mimic_full.yaml": "NEW -- local_parquet_dir set, schema-compatible with mimic_cxr.yaml",
        "scripts/train_contrastive.py load_mimic_cxr": "local_parquet_dir branch added (:437-490); MIMICJointDataset needed ZERO changes",
        "scripts/evaluate_cxr_retrieval.py": "local_parquet_dir/mimic_split params on build_dataloader + CLI; BOTH IndianaEvalDataset and MIMICValDataset fixed for str image paths (the crash the original review missed)",
        "scripts/eval_h100.sh": "LOCAL_PARQUET_DIR/MIMIC_SPLIT env levers added, empty default",
        "scripts/train_biomedclip_kd_h100.sh": "DATASET_CONFIG env lever added, default mimic_cxr UNCHANGED",
        ".gitignore": "guards added for dataset/mimic_full/, *.parquet, PhysioNet metadata files, legacy_gallery_hashes.txt",
        "requirements.txt": "added pandas, requests, pyarrow (all already transitive deps, now pinned direct since the build script imports them)"
      },
      "verification_done": {
        "validate_for_willi": "GREEN 9/9 gates, 99 passed (was 92) 5 skipped -- 7 new parity tests, none silently skipped (both import-failure skip branches confirmed NOT triggered)",
        "new_tests": [
          "test_build_script_hash_matches_repo_leakage_join_convention",
          "test_extract_findings_impression_basic_and_custom_override",
          "test_cxr_mimic_full_config_present_and_consistent",
          "test_load_mimic_cxr_dispatches_to_local_parquet_when_configured",
          "test_evaluate_cxr_retrieval_handles_str_image_paths",
          "test_h100_scripts_expose_dataset_config_and_local_parquet_levers",
          "test_gitignore_guards_credentialed_mimic_build"
        ],
        "manual_integration_smoke": "manifest and pack stages run END-TO-END against synthetic PhysioNet-shaped CSV/report fixtures (not in the pytest suite -- network-shaped checks). manifest correctly: filtered to frontal view, deduped one-per-study, resolved the files/pXX/pSUBJECT/sSTUDY.txt report path, extracted findings/impression separately, computed report_hash. pack correctly: joined a synthetic leaked-subject hash and dropped exactly that subject's row; HARD-FAILED with a clear error when match rate (50%) was below the 95% default gate; succeeded when --min-match-frac was lowered to 0.4.",
        "not_testable_from_here": "stage_meta and stage_fetch need a real PhysioNet connection -- run `meta` first (small, ~150MB) as the real auth/connectivity smoke test once Phase 7E is answered."
      },
      "goal": "scripts/build_mimic_cxr_local.py, four stages: meta -> manifest -> fetch (chunked, resize, delete originals, resumable) -> pack (train/validate/test parquet). ~190-210k frontal pairs at 320px ~= 6 GB on disk.",
      "REVIEWED_PROPOSAL_VERDICT": "Strategy is CORRECT and adopted. Five concrete bugs/risks from the original wget-based draft were identified 2026-08-16. IMPLEMENTATION NOTE: the shipped build_mimic_cxr_local.py uses Python `requests` instead of wget/curl, which SIDESTEPS corrections 1/2/3 by construction (no --cut-dirs, no --base list, no -N) rather than patching wget invocations. Correction 4 (silent 401 swallowing) IS explicitly implemented as a status-code tally + hard raise in stage_fetch. CORRECTION 5 UPDATE (2026-08-16, later): '.netrc auto-read' turned out to be MOOT, not fixed -- live testing proved PhysioNet's Django app does not honour HTTP Basic Auth for this project AT ALL, regardless of how the credentials are supplied (curl -u with a fresh password also 403'd/302'd). The wget --user --ask-password recipe this whole correction was built around is stale for PhysioNet's current deployment. Auth is now session-cookie based (~/.physionet_session) -- see phase7_physionet_credentialing.7D_auth_mechanism_correction for the full diagnosis and the SESSION_EXPIRED mid-run guard this also required. Kept below for historical record of what was originally reviewed.",
      "corrections_required_HISTORICAL_addressed_via_requests_not_wget": {
        "1_cut_dirs_off_by_one": "--cut-dirs=4 is WRONG, should be 3. URL path after -nH is files/mimic-cxr-jpg/2.1.0/files/pXX/...; cutting 4 strips the SECOND files/ too, so images land at pXX/... while the resize step looks for files/pXX/.... Every chunk converts 0 images and the run REPORTS SUCCESS. Safer: skip --cut-dirs entirely, use absolute URLs + explicit -O per file.",
        "2_wget_base_unreliable": "GNU wget documents -B/--base as resolving relative links for HTML input (--force-html); behaviour with a bare URL list is version-dependent. Write ABSOLUTE URLs into the listing.",
        "3_drop_N_flag": "-N with -c makes wget warn and disable timestamping. Noise that masks real errors.",
        "4_check_false_swallows_401": "A 401 on every file in a chunk is indistinguishable from a few tolerated 404s; the run would spin through ~100 chunks downloading nothing. ASSERT: if a chunk converts 0 of N, abort with the wget exit code.",
        "5_netrc_https_version_dependent": "wget .netrc over HTTPS is version-dependent. Prefer curl --netrc, or wget --user/--password from ENV (never argv -- leaks into ps and SLURM logs). Settled by 7D."
      },
      "8C_official_section_parser": "DONE. Vendored VERBATIM (not reimplemented) -- see what_shipped.section_parser.py above. Provenance is now a cited commit SHA, not a homegrown regex.",
      "8D_leakage_guard": "DONE. Subject-level exclusion implemented in `pack` as a HARD gate (raises below --min-match-frac, default 0.95) rather than a warning. dump_legacy_gallery_hashes.py (NEW) produces the input. Hash construction VERIFIED byte-identical to normalize_report_text (evaluate_cxr_retrieval.py:414) and text_hash (train_contrastive.py:419-424) via test_build_script_hash_matches_repo_leakage_join_convention. The underlying under-match RISK is unchanged by any of this -- the gate protects against silently trusting a bad join, it doesn't improve the join itself.",
      "8E_code_wiring": "DONE. load_mimic_cxr (train_contrastive.py:437-490) has a local_parquet_dir branch, tested via a mocked load_dataset call (test_load_mimic_cxr_dispatches_to_local_parquet_when_configured). MIMICJointDataset.__getitem__ needed ZERO changes as predicted. evaluate_cxr_retrieval.py's str-image crash (the thing the ORIGINAL proposal missed) is FIXED in both IndianaEvalDataset and MIMICValDataset, verified with a real temp JPEG round-tripped through both classes (test_evaluate_cxr_retrieval_handles_str_image_paths). build_dataloader + CLI + eval_h100.sh all threaded through.",
      "8F_emit_for_report_gen": "DONE. manifest emits findings/impression as separate untruncated columns plus has_findings/has_text flags; build_report.json reports both with_findings and with_findings_or_impression counts plus a per-split breakdown.",
      "8G_dua_hygiene": "DONE. .gitignore guards added (dataset/mimic_full/, *.parquet, PhysioNet metadata files, legacy_gallery_hashes.txt). Default build path (cxr_mimic_full.yaml) is /sc/home/krishankumar.bhushan/dataset/mimic_full (FIXED 2026-08-16 -- an earlier version wrongly used the PhysioNet username bhushkri instead of the cluster/OS username in this filesystem path), outside the repo regardless."
    },
    "phase9_retrieval_on_full_data": {
      "status": "pending_blocked_on_phase8",
      "goal": "Supporting chapter extended to full MIMIC. Single-lever attribution preserved.",
      "arms": {
        "9A_prep_early_validation_tooling": "DONE 2026-08-17, then CORRECTED same day after a live discovery. GAP FOUND: fetch processes all 218131 images in plain sequential order, scattering the ~27570 legacy-training studies Arm-0 needs across ~110 chunks -- meaning they would not all be present until the ~10-day full fetch was nearly done, defeating the point of a cheap early check. FIX: fetch stage gained --study-hashes <file> (env STUDY_HASHES) -- filters manifest to report_hash matches before computing todo, reusing all existing chunk/resume/atomic-write/SESSION_EXPIRED logic unchanged. INITIAL PLAN WAS WRONG: 'run in a separate OUT dir' does NOT isolate anything -- manifest.parquet's local_jpg column is an ABSOLUTE PATH BAKED IN AT MANIFEST-GENERATION TIME (str(out/'files'/...)), not recomputed from whatever --out a later fetch/pack call uses. Copying manifest.parquet into a fresh dir does not relocate those paths. DISCOVERED LIVE: user's arm0-subset fetch (job 2462107, pointed at a fresh mimic_arm0_subset dir with a COPIED manifest.parquet) showed rows already marked done at startup (3252 of 20482) -- real overlap with the concurrently-running main fetch (job 2461245), because BOTH were actually reading/writing the SAME original mimic_full/files/... tree the whole time. HARMLESS, not data loss -- both jobs converge on the one correct shared tree -- but the isolation plan simply doesn't work as described. CORRECTED APPROACH: use the SAME --out for both fetch and pack. Added --study-hashes to pack TOO (was fetch-only), plus --out-prefix (e.g. arm0_) so pack's output (arm0_train.parquet etc.) does not clobber the eventual production train/validate/test.parquet in the same directory. pack already only includes rows with local_jpg present, so this works correctly on a PARTIALLY-downloaded subset -- no need to wait for 100% completion. 2 new tests: test_fetch_study_hashes_filter_restricts_to_matching_subset, test_pack_study_hashes_filter_and_out_prefix (verifies exact row match, prefix naming, and that it correctly excludes a hash-matched-but-not-yet-downloaded row). validate_for_willi.sh GREEN 9/9, 109 passed (was 107) 5 skipped. OBSERVED LIVE MATCH RATE: 20482 of 26968 unique requested hashes matched (~76%) -- the 26968 unique count itself is below the raw 27570-line file because ~602 are duplicate report hashes (templated reports; consistent with 6C-3's ~2% duplication finding). SEPARATE FINDING FROM THE SAME LIVE RUN: running the arm0 fetch CONCURRENTLY with the main fetch on the SAME compute node (16 combined connections to physionet.org from one IP) produced a wall of ConnectTimeoutErrors -- not damaging (failed URLs just get retried on next resubmit) but directly informs the still-open WORKERS/throughput question: evidence AGAINST raising concurrency further, contradicting the earlier 'try workers=16' plan.",
        "9A_arm0_reproduction_control": "NON-NEGOTIABLE, RUNS FIRST -- now runnable EARLY via 9A_prep rather than gated on the full 10-day fetch. Rebuild ONLY the same ~27.5k studies through the new pipeline, rerun D1c. GATE: reproduce 0.1459 +/- 1.1pp. 3.5 GPU-h to find a pipeline change (section parser / view selection / extra resample) instead of discovering it inside a confounded multi-day result.",
        "9B_arm1_data_only": "Full build, D1c recipe unchanged, same 6000 steps. Epoch budget moves 13.93 -> ~1.8 at identical GPU-hours.",
        "9C_arm2_vit_lr_sweep": "vit_lr {1e-6,3e-6,1e-5,3e-5} on the full set. PRE-REGISTERED PREDICTION: the inverted-U optimum shifts RIGHT of 1e-6 and the peak is higher. STATE THE HYPOTHESIS NARROWLY: this plan already established that OPTIMIZATION-side overfitting is NOT binding (epochs 23->14 = +0.06pp; lower LR removed the rollover without changing plateau height). What 6G-1 measured is that the IMAGE TOWER SPECIFICALLY memorises. So the claim under test is 'more data relaxes the image-adaptation dose constraint', NOT 'more data fixes overfitting'. Null is reportable either way.",
        "9D_augmentation": "THE ONE UNTESTED FREE LEVER. VERIFIED: there is NO image augmentation anywhere in this repo -- all four transform sites (train_contrastive.py:186,300,368 and evaluate_cxr_retrieval.py:250) are Resize -> [Grayscale(3)] -> ToTensor -> Normalize. An 85M-param ViT trains on 27,570 images with none, and 6G-1 measured that it memorises them. RandomResizedCrop(224, scale=(0.8,1.0)) + mild rotation as ONE parallel arm. This is why Phase 8 stores at 320px. Not on the rejected-by-prior-evidence list.",
        "9E_selection_protocol_rerun": "FREE METHODOLOGICAL RESULT. Rerun the 6G-7 2x2 ({vit=2,vit=12} x {val==test, clean}) on full data. Measured finding was that test-informed selection is worth 2.2pp at 85M trainable image params BECAUSE the model overfits; with ~8x data it should SHRINK. How selection-protocol advantage scales with data volume is rarely published and costs nothing extra.",
        "9F_gates": "Indiana i2t R@10 >= 4.04% floor (target 5.5%) with MIMIC held >= Phase-6G. Plus official-split evals.",
        "9G_multisource_optional": "WAS Phase 7. DEPRIORITISED: full MIMIC is ~8x the data for a fraction of the engineering, and PhysioNet credentialing also unlocks VinDr-CXR directly. Revisit only if Indiana is still flat after 9F. IU-Xray stays EXCLUDED from training."
      }
    },
    "phase10_image_conditioned_generator": {
      "status": "10A_and_10B_architecture_DONE_2026_08_20 -- 10B_training_onward_pending",
      "VERIFIED_GAP": "HybridLanguageModel.forward() (hybrid_lm.py:147) accepts ONLY input_ids -- no inputs_embeds, no encoder_hidden_states, no cross-attention. generate() (:230) likewise. THERE IS CURRENTLY NO WAY TO CONDITION THE DECODER ON AN IMAGE. This is the single largest piece of new work in the pivot and the reviewed proposal does not mention it at all.",
      "tasks": {
        "10A": "DONE (2026-08-20). forward(input_ids=None, inputs_embeds=None, ...) + generate(..., prefix_embeds=None) added to HybridLanguageModel (hybrid_lm.py). Additive, default-None -- all 21 audited call sites untouched. forward() raises ValueError unless exactly one of input_ids/inputs_embeds given. generate()'s prefix_embeds=None branch is byte-identical to the pre-10A code (separate branch); the new branch builds hidden_states=cat([prefix_embeds, embeddings(input_ids)]) and loops via forward(inputs_embeds=...), re-embedding sampled tokens each step (no KV/state cache exists). 5 new parity tests in tests/test_willi_parity.py incl. bit-identical-equivalence pins for both forward() and generate(). validate_for_willi.sh: 115 passed (was 110), 9/9 gates green.",
        "10B_architecture": "DONE (2026-08-20). hybrid_xmamba/models/prefix_mapper.py: ImagePrefixMapper(patch_dim, decoder_dim, k, dropout=0.1) -- Linear -> adaptive_avg_pool1d(N->k) -> GELU -> Dropout -> Linear, deliberately attention-free. Unit-tested (shape + gradient flow) with synthetic (B,197,768) tensors for k in {8,32,64}. NOT wired to real BiomedCLIP patch grid or trained yet.",
        "10B": "PREFIX CONDITIONING FIRST (not cross-attention): project the BiomedCLIP ViT patch grid (197x768) through ImagePrefixMapper (architecture done, see 10B_architecture) to k prefix tokens in dim=768, prepend, cross-entropy on the report. Mamba/mLSTM are RECURRENT so a prefix is absorbed into the state and needs ZERO kernel changes; cross-attention would mean new per-layer modules and new Triton work. Sweep k in {8,32,64} -- the depth-analogue lever. STILL PENDING: real training run, blocked on Phase 8 report corpus + GPU.",
        "10C": "Initialize the image tower from the PHASE-9 BEST CONTRASTIVE CHECKPOINT, not stock BiomedCLIP. This is exactly where the retrieval chapter pays for itself and 6C measured the dividend: fine-tuned ViT 0.1172 vs stock 0.0232 with the same text tower. Run stock-ViT as an ablation -- the delta is a headline result linking the two chapters.",
        "10D": "Decoder init from the Phase-5 Stage-0 backbone (val PPL 13.18). Train on findings; findings-only vs findings+impression per 8F.",
        "10E": "INFRA DONE (2026-08-20), NOT YET RUNNABLE. configs/model/hybrid_150m_v2_rrg.yaml (identical arch to hybrid_150m_v2.yaml + image_patch_dim/prefix_k/decoder_lr/head_lr). New ReportGenerationLightningModule (hybrid_xmamba/training/lightning_module.py): decoder + ImagePrefixMapper + optional BiomedCLIP image_encoder.trunk.forward_features(...) for the pre-pooling patch grid -- this call path did not exist anywhere in the repo before (every existing CLIP site uses the pooled 512-d output); loaded lazily via load_image_encoder() so the module stays CPU-testable via a precomputed batch['patch_grid'] tensor with no open_clip/BiomedCLIP weights. Loss: CE over report-token positions only, prefix+pad positions -100-masked; confirmed this relies on nn.CrossEntropyLoss()'s undocumented-in-code default ignore_index=-100 (hybrid_lm.py passes no ignore_index arg) -- pinned by a manual-CE regression test so a future edit there can't silently break it. scripts/train_report_generation.py reuses train_contrastive.py's load_mimic_cxr/ImageTextDataset directly (bypasses prepare_dataloader's dataset_name=='mimic_cxr' dispatch string entirely rather than fighting it). scripts/train_report_generation_h100.sh mirrors train_biomedclip_kd_h100.sh's --exclude=ga03,gx17v1,gx13v1 / env-lever / offline-HF conventions, fails fast if DECODER_CKPT is missing (same pattern as STAGE0_CKPT). 4 new parity tests. validate_for_willi.sh: 125 passed (was 123), 9/9 gates green. Hydra compose of model=hybrid_150m_v2_rrg dataset=cxr_mimic_full verified directly (prefix_k=32, image_patch_dim=768 resolve correctly); train_report_generation.py's AST + imports verified directly. Cannot actually run training -- no Phase 8 data yet, no Phase 10D decoder checkpoint yet. This is scaffolding, not a completed 10E gate.",
        "10F": "Numerical gates. 150M is SPIKE-FRAGILE: the fp32 Mamba-scan / mLSTM-exp-gate fix (2026-07-16) is load-bearing; keep gradient_clip_val=0.5 and monitor grad_norm from step 0."
      }
    },
    "phase11_report_generation_eval": {
      "status": "11A_DONE_2026_08_20, 11B_DONE_2026_08_30_job_2494784_real_CheXbert_F1_n1433_14label_micro_0.3097_macro_0.1548_5label_micro_0.3059_macro_0.1983_clears_Floor_tier_below_Target, 11C_DONE_2026_08_28 (n=1433, generator beats retrieval floor on ROUGE-L/BLEU; reconfirmed again 2026-08-30 job 2493923 bit-for-bit) -- retrieval-baseline CheXbert F1 (true floor per pre-registered rule) / 11D / 11E pending",
      "tasks": {
        "11A": "DONE (2026-08-20). scripts/evaluate_report_generation.py: rouge_l_score/corpus_bleu are pure Python (no dep); meteor_score_corpus is nltk-backed, returns None (not 0.0) and prints 'skipped' when the wordnet corpus isn't staged locally (no internet on compute nodes, same as 11B's CheXbert weights). greedy_decode() reuses generate(prefix_embeds=..., top_k=1) from Phase 10A; beam_search_decode(beam_size=3) is new code on forward(inputs_embeds=...) (generate() has no beam mode). --smoke-test CLI proves both decoders run end-to-end on a tiny random model + ImagePrefixMapper output, no checkpoint/data needed; --hyp-file/--ref-file CLI computes metrics over any precomputed pairs. 6 new parity tests incl. beam_size=1==greedy_decode invariant. validate_for_willi.sh: 121 passed (was 115), 9/9 gates green. Manually verified both CLI modes run via conda run -n willi_parity (this repo has no local .venv on this machine).",
        "11B": "INFRA DONE (2026-08-28), API FIXED + LIVE-TESTED 2026-08-29 (job 2492037, correctly skipped with ModuleNotFoundError pre-install). f1chexbert THEN SUCCESSFULLY INSTALLED 2026-08-29 via `uv pip install f1chexbert` (job 2493754) after working through a multi-layer cluster access saga: bare `pip` is disabled cluster-wide (custom error, fires on login node AND inside sbatch jobs) with `conda install` nominally preferred but f1chexbert is not on conda-forge; `python -m pip` fails inside this venv (`No module named pip`, deliberate anti-pip hardening); `srun --pty` is rejected on aisc-batch (batch-only partition) so even an interactive pip-install attempt was a dead end; `uv` (already used by setup_env_h100.sh to build this exact .venv, specifically because bare pip is blocked) is the actual sanctioned installer and worked cleanly via `sbatch --wrap=\"source .venv/bin/activate && uv pip install f1chexbert\"`. FULL RERUN at n=1433 (job 2493764, CHEXBERT=true): ROUGE-L/BLEU reproduced EXACTLY (rouge_l 0.2075, bleu_1 0.2706, bleu_4 0.0707 -- matches job 2491600/2491687 bit-for-bit), confirming the fixed integration doesn't disturb the existing metrics path. CheXbert itself STILL skipped, but with a new and more specific error: `OSError: We couldn't connect to 'https://huggingface.co' ... and couldn't find them in the cached files` -- CONFIRMS the open unknown flagged in the previous note. inspect_report_generation_h100.sh / retrieval_baseline_h100.sh export HF_HUB_OFFLINE=\"${HF_HUB_OFFLINE:-1}\" by default (correct for the report-gen model itself, which needs no network) but this also blocks f1chexbert's own first-run download of its underlying HF-hosted checkpoint, and nothing is cached yet on any node. Compute nodes DO have real internet (proven by the uv install itself succeeding via sbatch), so this is a pure env-var problem, not a network-access problem. validate_for_willi.sh: 142 passed, 9/9 gates (unchanged, no code touched this round -- purely a cluster install + a run-config lever). HF_HUB_OFFLINE=0 fix CONFIRMED CORRECT (job 2493901): reran and the huggingface.co OSError was gone, replaced by `FileNotFoundError: ~/.cache/chexbert/chexbert.pth`. ROOT-CAUSED BY READING f1chexbert's ACTUAL SOURCE (pulled the sdist tarball directly from PyPI, no GitHub repo exists for it): `F1CheXbert.__init__` checks `os.path.exists(CACHE_DIR/'chexbert.pth')` and if missing calls `download_model(repo_id='StanfordAIMI/RRG_scorers', filename='chexbert.pth', cache_dir=CACHE_DIR)`, which calls `hf_hub_download(..., force_filename=f)` inside a bare `except Exception as e: print(e)` that SWALLOWS the failure instead of raising -- `force_filename` was removed from `hf_hub_download()` in modern `huggingface_hub`, so this call almost certainly raises and is silently eaten, leaving the checkpoint never written and the later `torch.load()` hitting FileNotFoundError. FIXED (not a repo code change, a one-off cluster action): staged the file manually via `sbatch --wrap='... python3 -c \"from huggingface_hub import hf_hub_download; import shutil,os; p=hf_hub_download(repo_id=\\'StanfordAIMI/RRG_scorers\\', filename=\\'chexbert.pth\\'); shutil.copy(p, os.path.expanduser(\\'~/.cache/chexbert/chexbert.pth\\'))\"'` (job 2493894 failed on ga03 -- ARM node, x86 venv incompatible, no --exclude on this one-off wrap job; job 2493899 with --exclude=ga03,gx17v1,gx13v1 succeeded). Rerunning CHEXBERT=true HF_HUB_OFFLINE=0 (job 2493901): got PAST the FileNotFoundError entirely, but hit a THIRD, independent bug: `AttributeError: BertTokenizer has no attribute encode_plus` inside f1chexbert's own `tokenize()` helper -- `encode_plus` is a legacy tokenizer method removed in `transformers>=5.0`; f1chexbert==0.0.2 (last released 2023, pinned only `transformers>=4.23.1`, no ceiling) is fundamentally incompatible with the modern transformers `uv pip install f1chexbert` pulled in. DECISION (user chose, via AskUserQuestion, over pinning transformers<5 in the shared main .venv): build a fully ISOLATED venv for CheXbert scoring only, decoupled from the main pipeline entirely, since the main venv's recent transformers is load-bearing for GPT-2 tokenizer/BiomedCLIP text tower/BioMedLM teacher and downgrading it cluster-wide to satisfy one old scoring package was the wrong tradeoff. BUILT: scripts/evaluate_report_generation.py gained `write_hyps_refs()` (pure I/O, sanitizes embedded newlines in report text to keep hyps.txt/refs.txt one-report-per-line-aligned) + a `--dump-dir` flag wired into both --checkpoint and --retrieval-baseline modes (writes hyps.txt/refs.txt without needing --chexbert at all, decoupling generation from scoring). NEW scripts/score_chexbert_standalone.py: a small script that imports NOTHING from this repo (no hybrid_xmamba/Triton) and duplicates compute_chexbert_metrics's ~15-line f1chexbert call + output-dict shape by hand (kept in sync manually, since both wrap the same tiny stable public API) -- f1chexbert import deferred inside main() so the file stays parseable/testable even without f1chexbert installed. NEW scripts/setup_chexbert_venv_h100.sh (mirrors setup_env_h100.sh's uv-first pattern; builds .venv_chexbert with CPU-only torch + `transformers<5` + f1chexbert) and scripts/score_chexbert_h100.sh (runs score_chexbert_standalone.py against a DUMP_DIR's hyps.txt/refs.txt using that isolated venv; `${DUMP_DIR:?...}` required-var pattern matching build_mimic_cxr_local.sh/eval_h100.sh's existing convention). Both existing wrappers (inspect_report_generation_h100.sh/retrieval_baseline_h100.sh) gained a `DUMP_DIR` env lever (default empty/off) alongside CHEXBERT. 8 new tests (write_hyps_refs newline-sanitization + line-count round-trip; --dump-dir flag presence; standalone script's build_parser() importable/parseable without f1chexbert; standalone script's mismatched-hyp/ref-line-count SystemExit; both existing wrappers' DUMP_DIR lever; the new venv-setup wrapper's transformers<5 pin; the new scoring wrapper's required-DUMP_DIR guard). validate_for_willi.sh: 150 passed (was 142), 9/9 gates green. NEXT ACTION FOR THE USER: (1) `sbatch scripts/setup_chexbert_venv_h100.sh` (one-time, builds .venv_chexbert); (2) rerun the n=1433 --checkpoint inspection WITHOUT --chexbert but WITH `DUMP_DIR=results/report_gen_full_n1433` to dump hyps.txt/refs.txt from the already-working generation run; (3) `DUMP_DIR=results/report_gen_full_n1433 sbatch scripts/score_chexbert_h100.sh` to score them in the isolated venv -- this is the actual integration test for the whole isolated-venv design and should finally produce real CheXbert F1 numbers.  2026-08-30: ALL THREE STEPS RUN. Step 1 (job 2493922) SUCCEEDED cleanly -- .venv_chexbert built with torch==2.13.0+cpu, transformers==4.57.6, f1chexbert import OK. Step 2 (job 2493923, n=1433 --checkpoint inspection with DUMP_DIR, no --chexbert) SUCCEEDED and reproduced the exact same aggregate ROUGE-L/BLEU as the 11C full-data run bit-for-bit (rouge_l 0.20753696229134028, bleu_1 0.27059390591927956, bleu_4 0.0707177415733251, meteor null -- nltk unavailable, degrades gracefully as designed, num_examples 1433), plus dumped hyps.txt/refs.txt to results/report_gen_full_n1433/ successfully -- CONFIRMS --dump-dir works correctly end-to-end and doesn't disturb the existing metrics path. Step 3 (job 2494015, score_chexbert_h100.sh against that dump) FAILED with a FOURTH, independent f1chexbert-package bug (not this repo's code, not the same bug as the prior three): `ValueError: too many values to unpack (expected 3)` at f1chexbert.py:229, `y_type, y_true, y_pred = _check_targets(refs_chexbert_5, hyps_chexbert_5)` -- a 3-value unpack of the PRIVATE `sklearn.metrics._classification._check_targets` API. ROOT-CAUSED (not guessed) by diffing scikit-learn's own source directly across git tags via raw.githubusercontent.com: `_check_targets(y_true, y_pred)` returned exactly `(y_type, y_true, y_pred)` through tag 1.7.2, then scikit-learn 1.8.0 added a `sample_weight` param and return value, making it a 4-tuple. f1chexbert has no scikit-learn upper pin (same pattern as its missing transformers<5 pin), so `uv pip install f1chexbert` in step 1 silently pulled an incompatible scikit-learn>=1.8.0 into the isolated venv. FIXED (one-line version pin, not a code patch, same remedy class as the transformers<5 pin): scripts/setup_chexbert_venv_h100.sh now installs `\"scikit-learn<1.8\"` explicitly in both its uv and pip-fallback branches, and its import smoke test now also prints the installed scikit-learn version for future debugging visibility. New parity test test_setup_chexbert_venv_h100_slurm_wrapper_pins_scikit_learn_below_1_8 added (mirrors the existing transformers<5 pin test). validate_for_willi.sh: 151 passed (was 150), 9/9 gates green. NEXT ACTION FOR THE USER: (1) `git pull`; (2) `sbatch scripts/setup_chexbert_venv_h100.sh` again to REBUILD .venv_chexbert with the scikit-learn<1.8 pin (the existing venv from job 2493922 has the broken sklearn baked in and must be rebuilt, not reused); (3) once that finishes, `DUMP_DIR=results/report_gen_full_n1433 sbatch scripts/score_chexbert_h100.sh` again -- hyps.txt/refs.txt from job 2493923 are already on disk, no need to rerun generation. This should finally produce real CheXbert F1 numbers, four bugs in.  2026-08-30 (later): setup_chexbert_venv_h100.sh rerun (job 2494759) hit a FIFTH bug -- `uv venv` errored `A virtual environment already exists` (no --clear support for a rerun); --clear was added but job 2494771 THEN failed differently, `uv venv --clear`'s own removal logic hitting `Directory not empty (os error 39)` on this cluster's NFS-backed home; fixed for real with an explicit `rm -rf \"${VENV_DIR}\"` before venv creation (script no longer uses --clear at all). See notes array for the blow-by-blow of both venv-tooling failures. Job 2494776 (rerun with the rm -rf fix): SUCCEEDED cleanly, scikit-learn==1.7.2 resolved as expected. `DUMP_DIR=results/report_gen_full_n1433 sbatch scripts/score_chexbert_h100.sh` (job 2494784): SUCCEEDED -- REAL CHEXBERT F1 NUMBERS, FIVE BUGS AND TWO TOOLING FIXES IN. n=1433 (validate.parquet): CheXbert F1 (14-label) micro/macro = 0.3097/0.1548; CheXbert F1 (5-label) micro/macro = 0.3059/0.1983; exact-match label-set accuracy = 0.3531. Full per-label dict in results/report_gen_full_n1433/chexbert_metrics.json. Clears the plan's pre-registered Floor tier (ROUGE-L>=0.15 and CheXbert-14-micro>=0.25, both cleared: 0.2075 and 0.3097) but well below Target (>=0.22/>=0.40). Per-label pattern: precision consistently >> recall (Cardiomegaly 0.45P/0.63R is the one real exception; Atelectasis 0.41P/0.04R, Edema 0.40P/0.08R, Pleural Effusion 0.73P/0.15R typical), and 4/14 labels never predicted at all (Lung Lesion/Pneumonia/Pneumothorax/Pleural Other, F1=0) -- consistent with 11C's 73.6%-boilerplate-template finding: the generator is conservative and rarely asserts rare/serious findings, capping recall exactly where a clinical system needs it most. TWO CAVEATS before treating 0.3097 as final: (1) this is validate.parquet, NOT the official subject-disjoint test split (11D, still open); (2) the retrieval-NN baseline (11C) has NO CheXbert F1 yet -- the plan's own pre-registered rule treats the retrieval baseline's CheXbert F1 as the real floor for this metric specifically, and that comparison has not been made (only ROUGE-L/BLEU have been compared so far, where the generator already won). NEXT ACTION FOR THE USER: score CheXbert on the retrieval-NN baseline too, for the true floor comparison -- `DUMP_DIR=results/retrieval_floor_n1433 sbatch scripts/retrieval_baseline_h100.sh` (uses the same 191,462-pair train gallery and validate.parquet as job 2491687) then `DUMP_DIR=results/retrieval_floor_n1433 sbatch scripts/score_chexbert_h100.sh` against that dump. Independently open: 11D (official test split) and 11E (qualitative appendix, N=20 side-by-side pairs).",
        "11C": "DONE (arm0 2026-08-24, full-data n=1433 2026-08-28). Retrieve the nearest training report by stock BiomedCLIP cosine similarity, emit verbatim -- the REAL floor. arm0: retrieval beats the generator on every metric (generator contributed nothing). Full data n=1433 (jobs 2491600/2491687): REVERSED -- generator beats the floor on all 3 metrics (rouge_l 0.2075 vs 0.1881; bleu_1 0.2706 vs 0.2605; bleu_4 0.0707 vs 0.0465), though 73.6% of generations are exact-duplicate template text.",
        "11D": "Report on the OFFICIAL subject-disjoint test split. Legacy train[90%:] only if 8D recall passed.",
        "11E": "Qualitative appendix, N=20 side-by-side incl. failure cases. Cheap, and it is what a viva actually asks about."
      }
    },
    "phase12_eval_writeup": {
      "status": "pending",
      "goal": "analysis/h100_scaling_results.md structured as (1) report generation [primary] (2) retrieval [supporting chapter] (3) efficiency curves (4) honest limitations. Plus final state verdict + best ckpt path.",
      "efficiency_curves": "MEASURED 2026-07-28, jobs 2382432/2382434. See the 2026-08-16 note for the full findings. Curves are on the SERVER at analysis/efficiency_{150m,70m}/ -- not yet pulled into the repo."
    },
    "phase6b_lr_matched_rerun": {
      "status": "scripts_ready_run_pending_h100",
      "goal": "LR-matched bs=64 rerun. Phase-6 batch sweep was never LR-matched (backbone_lr/head_lr hardcoded at bs=128 sqrt-scaled values), so the winning bs=64 arm trained at ~1.4x its proper LR. Two arms: head_lr=4.24e-4 (sqrt-matched, default) vs 3.0e-4 (conservative/canonical A100). Motivated by grad_norm ~12.3 vs gradient_clip_val=1.0.",
      "launch_script": "scripts/train_biomedclip_kd_150m_h100.sh (BATCH_SIZE=64 default; LR + MAX_STEPS now DERIVED from batch, 384000-sample budget)",
      "baseline_to_beat": 0.1113,
      "noise_floor_note": "SE ~0.57pp at p~0.11,n=3063 => require >~1.1pp movement before calling a win"
    },
    "phase6c_measurement_block": {
      "status": "COMPLETE_job_2372055_teacher_3.40pct_student_11.72pct_oracle_99.0pct",
      "results": {
        "stock_vit_x_biomedclip_text_i2t_R10": 0.034,
        "stock_vit_x_student_text_i2t_R10": 0.0232,
        "finetuned_vit_x_biomedclip_text_i2t_R10": 0.0431,
        "finetuned_vit_x_student_text_i2t_R10": 0.1172,
        "oracle_ceiling_i2t_R10": 0.9902,
        "gallery_duplicate_fraction": 0.02,
        "false_neg_pairs_per_batch_bs64": 0.58,
        "prob_batch_has_any_false_neg_bs64": 0.194
      },
      "verdicts": "1) NOT teacher-bound - student is 3.4x stock BiomedCLIP. 2) The KD anchor is a DRAG not a ceiling: it pulls toward a 4.31% representation while CLIP builds an 11.72% one -> 6D-2 PROMOTED to co-priority. 3) False negatives QUANTITATIVELY DEAD (0.58 pairs per 4096-entry bs=64 matrix) -> MULTIPOS dropped from 6D-3. 4) Metric artifact DEAD (oracle 99.0%, dedup-aware differs by 0.03pp) -> keep the strict metric as headline. 5) Text tower is not the weak half (swapping BCT in costs 7.41pp) -> 6E deprioritised.",
      "launch": "CKPT=<best 6B ckpt> sbatch scripts/run_phase6c_measurements.sh",
      "goal": "Zero-training instrumentation, ~1-2 GPU-h. 6C-1 stock BiomedCLIP (own text+image towers) on identical train[90%:] N=3063; 6C-2 tower-swap 2x2 {student text, BCT} x {fine-tuned ViT, stock ViT}; 6C-3 duplicate/false-negative audit (CPU) -> oracle R@10 ceiling + per-batch-size false-neg rate; 6C-4 surface R@1/R@5 + dedup-aware R@10.",
      "gating": "DOES NOT gate 6D (user decision 2026-07-25: run 6D regardless of the teacher number). 6C explains and calibrates the writeup; 6C-2 alone gates 6E.",
      "new_scripts": [
        "scripts/reference_biomedclip_zeroshot.py",
        "scripts/audit_mimic_duplicates.py"
      ]
    },
    "phase6d_factorial_lever_block": {
      "status": "COMPLETE_2026-07-26_ViT_DEPTH_IS_THE_LEVER_D1c_0.168_in_training",
      "results_in_training_i2t_R10_final_at_ep13": {
        "D0_vit2_control": 0.116,
        "D1a_vit4": 0.132,
        "D1b_vit6": 0.15,
        "D1c_vit12": 0.168,
        "D2_kd_decay_2000_to_0": 0.118,
        "D3_siglip": 0.119,
        "D5_gamma_simcse_0": 0.122
      },
      "verdict": "6D-1 IS A DECISIVE MONOTONE WIN: 0.116->0.132->0.150->0.168 across unfreeze depth 2/4/6/12 = +5.2pp over control, ~9x SE. val/clip_loss falls monotonically alongside (2.897->2.585) and i2t R@1 nearly doubles (0.017->0.030), so it is generalization not artifact (eval images are never trained on). No OOM at bs=64 with all 85.1M ViT params trainable. In-training 0.168 EXCEEDS the plan's STRETCH tier (0.14). 6D-2/6D-3/6D-5 all NULL.",
      "kd_prediction_falsified": "6C led me to predict that releasing the alpha_kd=0.3 anchor would help, because BiomedCLIP's text tower is only worth 4.31% while CLIP builds an 11.72% one. WRONG. The mechanism fired exactly as designed (cos_text_teacher collapsed 0.570 -> 0.194, so the anchor genuinely released) and retrieval did not move (+0.2pp). val/clip_loss was marginally WORSE (2.934 vs 2.897), so at alpha=0.3 the KD term is a mild REGULARIZER, not a drag. The KD-anchor hypothesis is dead in BOTH directions -- neither ceiling nor drag. Do not revisit.",
      "launch": "./scripts/submit_phase6d_arms.sh   (dry run; add --submit to launch). ARMS=\"D0 D2 D3\" to subset. CLUSTER CONSTRAINT 2026-07-26: the aisc login node REFUSES `bash <script>` ('This command is not allowed on the login node!'). Use ./scripts/... , an `srun --pty bash` session, or paste the sbatch lines. sbatch itself IS allowed on the login node (that is how 6C ran).",
      "control_invariant": "Every 6D/6E/6F lever is env-overridable in train_biomedclip_kd_h100.sh and DEFAULTS to the Phase-6B recipe (VIT_UNFREEZE=2 KD_DECAY_STEPS=0 CLIP_LOSS=infonce MULTIPOS=false GAMMA_SIMCSE=0.1 BIDIRECTIONAL=false SELECTION_SPLIT=false), so an unmodified invocation IS 6D-0. Enforced by test_phase6d_config_defaults_are_the_control + test_h100_launch_script_exposes_phase6d_levers.",
      "goal": "D0 control (bs=64 LR-matched) | D1 vit_unfreeze {4,6,12} | D2 KD-anchor decay alpha_kd_post->0 | D3 SigLIP + multi-positive mask (one arm) | D4 stack of best-of-D1+D2+D3 | D5 optional gamma_simcse=0. D1-D3 parallel for attribution, D4 for the number.",
      "gate": "> 1.1pp over 6D-0 control (SE ~0.57pp at p~0.11 n=3063) or it is noise.",
      "rationale": "Six one-at-a-time nulls have made single-lever probing expensive per bit of information; factorial buys attribution AND a stacked shot at the 12% target in one block (~3.5 GPU-h per arm)."
    },
    "phase6e_bidirectional_text_encode": {
      "status": "not_started_conditional_on_6C2",
      "goal": "6E-1 cheap forward+reversed-pass average behind a config flag (no new params -> checkpoint-compatible). 6E-2 in-layer bidirectional SSM scan if 6E-1 wins (the publishable version).",
      "motivation_warning": "Motivated by REPORT STRUCTURE (Impression at ~token 300 recontextualizes Findings at ~token 40; a causal encoder cannot propagate that backwards), NOT by cos_text_teacher. The cos-ceiling argument is FALSIFIED (see notes 2026-07-25) and must not appear in the writeup."
    },
    "phase6g_vit_adaptation_dose_response": {
      "status": "COMPLETE_2026-07-27_vit_lr_1e-6_optimal_scope_null_cleansplit_MIMIC_0.1459_Indiana_0.0390",
      "authoritative_results": {
        "D1c_vit12_mimic_i2t_R10": 0.1714,
        "D1c_vit12_mimic_i2t_R1": 0.03,
        "D1c_vit12_mimic_t2i_R10": 0.1538,
        "D1c_vit12_mimic_paired_cosine": 0.423,
        "D1c_vit12_indiana_i2t_R10": 0.0485,
        "D1c_vit12_indiana_t2i_R10": 0.07,
        "D1b_vit6_mimic_i2t_R10": 0.143,
        "D1a_vit4_mimic_i2t_R10": 0.1319,
        "D0_vit2_mimic_i2t_R10": 0.1107,
        "lr_matched_dose_response_note": "All four at bs=64 head_lr=4.24e-4 backbone_lr=1.41e-5, 6000 steps, best-by-val/total_loss. vit 2/4/6/12 -> i2t R@10 0.1107/0.1319/0.1430/0.1714, R@1 0.0153/0.0196/0.0206/0.0300, R@5 0.0637/0.0738/0.0937/0.1032, t2i R@10 0.1041/0.1166/0.1394/0.1538, paired cosine 0.3824/0.3962/0.4084/0.4230. MONOTONE IN EVERY COLUMN. +6.07pp from depth 2->12 = ~10.6x SE. D0 (0.1107) reproduces the standing 0.1113 baseline to within 0.06pp, validating it as a clean control. Per-block returns diminish but do not saturate: 1.06/0.56/0.47 pp per block. NOTE the earlier vit=2 numbers (0.1113 at head_lr=6e-4, 0.1172 at 3.0e-4) are NOT part of this curve -- different head LRs; cite the LR-matched table.",
        "best_ckpt": "outputs/h100_6d_D1c/checkpoints/contrastive-step=004750-val/total_loss=2.6680.ckpt"
      },
      "goal": "Depth is exhausted at 12 (ViT-B/16 has 12 blocks) but 'amount of image adaptation' = depth x LR x scope, and ONLY depth has been swept. vit_lr has sat at 1e-6 for the entire project -- 3 orders of magnitude below head_lr -- so the winning arm is one where the whole tower is unfrozen but barely moving.",
      "tasks": {
        "6G-1": "vit_lr sweep at VIT_UNFREEZE=12: {3e-6, 1e-5, 3e-5}. Highest-value remaining experiment.",
        "6G-2": "Scope: _get_vit_blocks returns transformer blocks ONLY, so patch_embed/cls_token/pos_embed/final norm/visual projection stay frozen even at depth 12. Add opt-in full-tower unfreeze.",
        "6G-3": "Authoritative evaluate_cxr_retrieval.py on D1b + D1c (MIMIC) -- the writeup number.",
        "6G-4": "Indiana eval on D1c. Prior evidence: ViT unfreeze 0->2 left Indiana at 3.90% unchanged, but 12 blocks on 27.5K pairs is a far larger intervention and may overfit in-domain at cross-domain cost. GATES PHASE 7.",
        "6G-5": "Re-run D1c with SELECTION_SPLIT=true. Arm-level comparison used val/total_loss on train[90%:] = the eval gallery, i.e. test-set selection at the ARM level. Effect is 9x SE so not noise-mining, but the thesis headline should be confirmed under a clean protocol. This is what Phase 6F was built for."
      },
      "authoritative_6g": {
        "D1c_vit12_lr1e-6_blocks": 0.1714,
        "scopeall_lr1e-6_all": 0.1704,
        "lr3e6": 0.1632,
        "lr1e5": 0.1606,
        "lr3e5": 0.1456,
        "cleansplit_MIMIC": 0.1459,
        "cleansplit_Indiana": 0.039,
        "best_step_by_vit_lr": "1e-6->4750, 3e-6->3250, 1e-5->2500, 3e-5->1750 (overfit onset moves earlier monotonically)",
        "6G7_vit2_cleansplit_MIMIC": 0.1081
      },
      "final_2x2": "vit2/val==test 0.1107 | vit12/val==test 0.1714 (+6.07pp, 9.7 SE) || vit2/clean 0.1081 | vit12/clean 0.1459 (+3.78pp, 6.2 SE). Protocol cost: 0.26pp at vit=2 (0.5 SE, nil) vs 2.55pp at vit=12 (3.8 SE). QUOTE THE CLEAN PROTOCOL AS PRIMARY: 10.81% -> 14.59%, +3.78pp."
    },
    "phase6f_eval_protocol_fix": {
      "status": "not_started",
      "goal": "Carve a disjoint selection split out of train[:90%] (train[:85%] / train[85%:90%] select / train[90%:] test). Today validation_split == test_split, so any checkpoint selection is selection-on-test. Do regardless; also unblocks checkpoint-on-retrieval, which is otherwise permanently banned."
    }
  },
  "open_questions": {
    "chexpert_vindr_template_wording": "Finalize label->prompt template in Phase 7 (CheXzero-style start; iterate if in-domain MIMIC regresses).",
    "lr_150m_exact": "RESOLVED 4.0e-4 (conservative sqrt-width scale); revisit only if Phase-5 loss slow.",
    "willi_retirement": "If willi/A100 retired post-migration, drop py3.9 guards + validate_for_willi.sh."
  },
  "notes": [
    "2026-08-17 (arm0 subset live, correction #2): THE 'SEPARATE OUT DIRECTORY' PLAN FOR ARM-0 DOES NOT WORK AS DESCRIBED -- FOUND AND FIXED SAME DAY. User ran steps 1-2 successfully (dump_legacy_hashes.sh: legacy_gallery_hashes.txt 3063 lines, legacy_training_hashes.txt 27570 lines -- both exactly matching expected sizes), then step 3 (STAGE=fetch OUT=mimic_arm0_subset STUDY_HASHES=legacy_training_hashes.txt, job 2462107) showed '20482 of 218131 manifest rows matched (26968 hashes requested)' then '20482 target / 17230 still to do' -- meaning 3252 rows were ALREADY marked done at the very start of a brand-new output directory's very first fetch. Root cause: manifest.parquet's local_jpg column is an ABSOLUTE PATH baked in AT MANIFEST-GENERATION TIME (str(out/'files'/...) inside stage_manifest), not recomputed from whatever --out a LATER fetch/pack call uses. Copying manifest.parquet into a new directory (as instructed) does NOT relocate those paths -- the copied file still says .../mimic_full/files/..., so the 'isolated' arm0 job was actually reading AND WRITING the exact same shared mimic_full/files/... tree as the concurrently-running main fetch (job 2461245) the whole time. VERDICT: harmless, not data loss -- both jobs converge on the one correct shared tree, and _stage scratch directories WERE correctly separate (mimic_full/_stage vs mimic_arm0_subset/_stage) so no in-flight-chunk corruption risk existed either. But the isolation plan simply doesn't achieve what it was designed to. FIX: dropped the separate-directory approach; use the SAME --out for both fetch and pack now. Added --study-hashes to `pack` too (previously fetch-only) plus a NEW --out-prefix arg (env OUT_PREFIX, e.g. 'arm0_') so pack's filtered output (arm0_train.parquet/arm0_validate.parquet/arm0_test.parquet) does not clobber the eventual production train/validate/test.parquet when packed from the same directory. pack already filters to local_jpg.exists() before anything else, so this works correctly even on a PARTIALLY-downloaded subset -- no need to wait for the arm0 fetch to fully finish before packing a usable (if smaller) set. 2 new tests (test_fetch_study_hashes_filter_restricts_to_matching_subset, test_pack_study_hashes_filter_and_out_prefix -- the latter specifically verifies a hash-matched-but-not-yet-downloaded row is correctly excluded, and that the prefix keeps the default filenames untouched). validate_for_willi.sh GREEN 9/9, 109 passed (was 107) 5 skipped. MATCH RATE OBSERVED LIVE: 20482/26968 unique requested hashes matched (~76%); 26968 unique is itself below the raw 27570-line file by 602 -- duplicate report hashes from templated reports, consistent with 6C-3's ~2% duplication finding, not a bug. SEPARATE FINDING, same log: running the arm0 fetch CONCURRENTLY with the main fetch on the SAME compute node (both landed on gx17v1, 16 combined connections to physionet.org from one IP) produced a wall of ConnectTimeoutErrors on many URLs. Not damaging -- failed URLs simply aren't added to staged_srcs, stay absent from local_jpg, and get retried on any future resubmit via the existing resumable design -- but this is real evidence AGAINST the earlier-planned 'try WORKERS=16' experiment (session note below), not for it: doubling effective concurrent connections from one node already produces outright connection failures, not just slower transfer, at only 2x the current worker count. NEXT ACTION: let both currently-running jobs continue (nothing to fix retroactively -- the arm0 job IS correctly and usefully front-loading the arm0-relevant subset into the real mimic_full tree); once the arm0-relevant images are mostly/fully downloaded, run `STAGE=pack OUT=.../mimic_full STUDY_HASHES=legacy_training_hashes.txt OUT_PREFIX=arm0_ sbatch scripts/build_mimic_cxr_local.sh`, then point the D1c recipe at the arm0_*.parquet files via DATASET_CONFIG=cxr_mimic_full (overriding the split filenames) to get the actual Arm-0 answer.",
    "2026-08-17 (later still): RESUME CONFIRMED CORRECT LIVE + ARM-0 EARLY-VALIDATION GAP FOUND AND FIXED. Job 2457894 ran 24h, completed 15 chunks (30000/218131, 13.8%), was cleanly killed by TIME_LIMIT (not a crash). User resubmitted (job 2461245): log showed '218131 target / 188131 still to do' -- 218131-188131=30000, an EXACT match to the killed job's last completed count, confirming the resumability design works correctly end to end in production, not just in theory. Also confirmed live: only ONE '[auth] session cookie loaded' line this time (was five) -- direct production confirmation the threading.Lock fix from earlier actually works, not just in its unit test. Progress-logging fix also confirmed working ('downloading N/2000 in this chunk' lines present). USER ASKED (correctly, and I should have caught this when writing Phase 9A originally): is there a way to validate this approach BEFORE spending ~10 days, so we don't waste time? ANSWER SURFACED A REAL GAP: Phase 9A's own Arm-0 reproduction control (rebuild the same ~27570 legacy studies, confirm retrieval reproduces 0.1459 +/-1.1pp) is the plan's designed answer to exactly this question -- but `fetch` processes all 218131 images in plain sequential chunk order, so the specific 27570 legacy-training studies are scattered across ~110 chunks and would not all be present until the ~10-day run was NEARLY DONE. This defeats the entire point of a cheap early sanity check -- discovered by the user's question, not caught when the phase was originally planned. FIX (user approved via AskUserQuestion, chose 'build it now'): added --study-hashes <file> (env STUDY_HASHES) to the fetch stage -- filters the manifest to report_hash matches BEFORE computing todo, reusing every existing chunk/resume/atomic-write/SESSION_EXPIRED mechanism unchanged, just one filter on top. scripts/dump_legacy_hashes.sh (NEW) dumps hashes for both the legacy test gallery (train[90%:], already needed for 8D) and now also the legacy TRAINING set (train[:90%], ~27570) -- dump_legacy_gallery_hashes.py already generalised via --split so zero code change was needed there, just running it a second time with a different split arg. Runs offline against the SAME already-populated HF cache Phase 6 used. MUST run in a SEPARATE OUT directory (e.g. mimic_arm0_subset) rather than inside the currently-running mimic_full build -- two concurrent fetch invocations against the same OUT would collide on the shared _stage scratch dir, since each wipes it at its own chunk boundary; isolated OUT dirs avoid this entirely at the cost of a small (~800MB) amount of redundant transfer for any overlapping images, negligible next to the 323GB already in flight. New test test_fetch_study_hashes_filter_restricts_to_matching_subset (mocked, no network) verifies the filter correctly restricts to matching rows and correctly ignores non-matching hashes without erroring -- had to route around a ProcessPoolExecutor+spawn pickling limitation specific to the test harness's dynamic module loading (not a real bug; confirmed by also running the filter manually against a synthetic manifest+fixture and observing correct row selection in the printed [fetch] log line). validate_for_willi.sh GREEN 9/9, 107 passed (was 106) 5 skipped. Committed. NEXT ACTION FOR THE USER: sbatch scripts/dump_legacy_hashes.sh (produces legacy_training_hashes.txt), then copy manifest.parquet from mimic_full into a fresh mimic_arm0_subset dir, then STAGE=fetch OUT=.../mimic_arm0_subset STUDY_HASHES=legacy_training_hashes.txt sbatch scripts/build_mimic_cxr_local.sh (small, ~1/8th the wall time of the full fetch) -- this can run ALONGSIDE the ongoing main fetch without interference. Once packed, run the existing D1c recipe against it via DATASET_CONFIG=cxr_mimic_full to get the Arm-0 answer in hours rather than after 10 days.",
    "2026-08-17 (fetch is live, real production run underway): meta+manifest RE-RUN AT THE PRODUCTION PATH (not the smoke-test one) -- user's first manifest attempt hit FileNotFoundError because meta had only ever run against mimic_smoke_test, not mimic_full; not a bug, just a workflow gap (both stages must share the same OUT). Fixed by re-running meta at OUT=/sc/home/krishankumar.bhushan/dataset/mimic_full (job 2457821, succeeded, ~30 min again) then manifest (job 2457888, succeeded in ~5 min, CPU-only). REAL PRODUCTION NUMBERS, matching plan predictions almost exactly: rows_in_split_csv 377110 -> after_frontal_filter 243334 -> after_one_per_study 218139; with_findings 149496 (predicted ~150-160k); with_findings_or_impression 218131 (predicted ~190k+); est_stored_gb 6.54 (predicted ~6); est_transfer_gb 322.8 (predicted ~310-400); split train 213357 / test 3041 / validate 1733; reports_missing_on_disk 0. This validates the ENTIRE pipeline (vendored parser, frontal/one-per-study filter, hashing) at full scale, not just synthetic fixtures. fetch LAUNCHED (job 2457894, 15:31) targeting 218131 images / ~323GB transfer. USER REPORTED after 1-2h: five duplicate '[auth] session cookie loaded' lines then silence, worried it was hung. DIAGNOSED AS TWO REAL (but non-blocking) BUGS, both fixed for future runs (does NOT affect the already-running job, which keeps its already-loaded code): (1) _get_session() race -- stage_fetch's ThreadPoolExecutor(workers=8) calls it concurrently on the first chunk, original check-then-set wasn't atomic, causing up to `workers` racing Session creations (harmless correctness-wise, same cookie every time, but wastes connections and confuses logs). Fixed with threading.Lock double-checked locking. NEW TEST deliberately slows Session construction to force the race window open -- VERIFIED it fails reliably against the unlocked pattern (16/16 raced in a standalone repro) and passes reliably against the fix, not passing by scheduling luck either way. (2) No progress output DURING a chunk's download phase (only after the full 2000-file chunk, download AND resize, completes) -- at real throughput a single chunk can take well over an hour with zero output, making 'slow' indistinguishable from 'hung' without a manual `du` check. Added a progress line every ~10% of a chunk. validate_for_willi.sh GREEN 9/9, 106 passed (was 105) 5 skipped. USER ALSO ASKED what the helpdesk-email/quota-check timing advice meant -- ANSWERED: manifest is pure local CPU (no network, produces only a handful of small files) so neither the network-transfer courtesy notice nor the inode quota matters for it; fetch is what actually transfers ~323GB and writes ~218k individual image files (~450k inodes total with the report .txt files), which is exactly what both the courtesy-contact policy and an inode cap would apply to. CONFIRMED VIA docs.sc.hpi.de/cluster/Storage/Quotas/: 200 GiB byte quota documented, but NO command for checking INODE usage is documented anywhere -- the only reliable path is asking sc-helpdesk@hpi.de directly, bundled with the transfer-courtesy email into one message. NOTED HONESTLY: fetch was already launched before either the helpdesk email or the quota check happened -- not catastrophic (hard-fail design means a quota hit surfaces as a clear error, not silent corruption) but both should still happen now, in parallel with the running job, per updated Phase 7F guidance. IMMEDIATE USER-FACING GUIDANCE GIVEN: `du -sh /sc/home/krishankumar.bhushan/dataset/mimic_full/_stage` run twice a few minutes apart, to confirm real progress on the currently-running job without touching it. NEXT ACTION: let job 2457894 keep running (it is very likely just slow, not stuck -- 5 real auth attempts is direct evidence of live network activity); send the combined helpdesk email; once fetch completes (or needs a resubmit for any reason), the next invocation will have both new fixes.",
    "2026-08-17 (the actual end of this session): PHASE 7D-VERIFY DONE -- meta STAGE FULLY WORKING END TO END. Job 2457693 (OUT=/sc/home/$USER/dataset/mimic_smoke_test, i.e. shared filesystem, not /tmp): all 5 small files freshly GET'd (not 'have'd -- fresh path, no contamination possible), session-cookie auth confirmed again, reports.zip unzipped without error, '[meta] done -> ...' printed. Took ~30 min wall (13:19-13:49 CEST), which is the honest download time on this connection and confirms the earlier --time=00:10:00 override (not the script's own 1-day default) was the actual cause of every prior 'failure' in this chain. Phase 7 is now COMPLETE except 7F (inode/quota check, still open, no urgency until fetch is imminent). ALSO FOUND AND FIXED (unrelated to the meta success, caught while reviewing before declaring done): configs/dataset/cxr_mimic_full.yaml's local_parquet_dir default read /sc/home/bhushkri/dataset/mimic_full -- bhushkri is the user's PhysioNet username, NOT their cluster/OS username (krishankumar.bhushan, visible in every real path throughout this whole debugging session). Fixed to /sc/home/krishankumar.bhushan/dataset/mimic_full; same fix applied to dump_legacy_gallery_hashes.py's --cache-dir default. Neither bug had been exercised yet (both are used only by load_mimic_cxr / the Phase 8D leakage-guard dump, neither run yet), so no data was ever misdirected -- caught before it could matter. validate_for_willi.sh GREEN 9/9 after the fix, no test count change (these are plain config/CLI defaults, not covered by a specific assertion). PLAN HYGIENE: rewrote the stale 7E guidance that still described cpu-batch/cpu-interactive as the working partitions -- corrected to note aisc-batch/aisc-interactive with explicit --account=aisc --qos=aisc is what actually works for this account, and added the node-local-/tmp warning inline there too so a future session sees it before choosing a build path. NEXT ACTION (fresh phase, no more debugging expected): `STAGE=manifest OUT=/sc/home/krishankumar.bhushan/dataset/mimic_full sbatch scripts/build_mimic_cxr_local.sh` -- this is CPU-only, no network, should be fast; READ build_report.json afterward (with_findings should land near ~150-160k on the full corpus) before touching `fetch`. Also still outstanding: the sc-helpdesk@hpi.de email (large-transfer courtesy notice + ask about a non-preemptible CPU-only queue alternative to aisc-batch) should go out before the real multi-hour `fetch` run, not before this smoke-scale `meta`/`manifest` work.",
    "2026-08-16 (genuinely final, this session): /tmp IS NODE-LOCAL, NOT SHARED -- the real explanation for the recurring BadZipFile. After the atomic-write fix (previous note) was deployed and its presence CONFIRMED on the cluster (grep -n 'part.replace|SESSION_EXPIRED' scripts/build_mimic_cxr_local.py showed the patched lines), the user did `rm -rf /tmp/mimic_smoke_test` then resubmitted -- and got the EXACT SAME zipfile.BadZipFile crash, in only 3 seconds wall time. Full log (cat, not tail -f, which had misleadingly shown only the last ~10 lines of the fast-finishing job) revealed ALL FIVE files printed '[meta] have ...' -- i.e. stage_meta believed everything was already downloaded, on a directory the user had JUST rm -rf'd. ROOT CAUSE: every job so far landed on the same compute node (gx17v1, visible in every log's hostname line), and /tmp on a compute node is typically a SEPARATE, NODE-LOCAL disk from /tmp on the login node (lx01) -- a very common HPC pattern. The user's rm -rf, run on lx01, had ZERO effect on gx17v1's own /tmp; the corrupt zip left by the FIRST killed job (2457565, the one killed by the borrowed --time=00:10:00) had been silently persisting on gx17v1's local disk through every subsequent 'clean' retry, immune to every rm run from the login node. The atomic-write fix itself was correct and deployed the whole time -- it simply never got a chance to matter, because the file it protects against was never actually re-downloaded (stage_meta's resume-skip logic worked exactly as designed against contaminated pre-existing state a login-node rm could not reach). FIX (documentation only, no code change needed): stop using /tmp for cluster smoke tests entirely. Use a path under the shared filesystem instead -- the same one the real Phase 8 build already targets (/sc/home/$USER/dataset/...). Updated H100_SCALING_PLAN.md's 7D-verify command accordingly (OUT=/sc/home/$USER/dataset/mimic_smoke_test, not /tmp/...). NOTE: this only ever affected the throwaway smoke-test path -- the real build's OUT was always specified as /sc/home/$USER/dataset/mimic_full (shared filesystem) from the start, so this gotcha never threatened the actual data build, only the verification step. NEXT ACTION: STAGE=meta OUT=/sc/home/$USER/dataset/mimic_smoke_test sbatch scripts/build_mimic_cxr_local.sh -- a brand-new path guarantees no leftover contamination regardless of which node the job lands on.",
    "2026-08-16 (actually final, this session): PARTIAL-DOWNLOAD CORRUPTION BUG FOUND AND FIXED LIVE, plus a worse latent one it exposed. Retry of the meta smoke test (job 2457605, using the corrected account/partition/qos defaults) crashed at zipfile.BadZipFile -- the PREVIOUS job's --time=00:10:00 kill (see the note above) had left a truncated mimic-cxr-reports.zip on disk, and stage_meta's resume check (dest.exists() and size>0) trusted it as already-downloaded ('[meta] have mimic-cxr-reports.zip'), skipping the re-fetch entirely. ROOT CAUSE: _download() wrote streamed bytes directly to the final dest path with no atomicity -- any interruption mid-write (SLURM kill, preemption, connection drop) left a partial-but-nonzero-size file that every resume check in the script would trust. FIX: _download() now streams to a dest.name+'.part' sibling and does an atomic Path.replace() into dest only after the full body is consumed; dest is therefore either absent or complete, NEVER partial, for both stage_meta and stage_fetch. WHILE FIXING THIS, found a SECOND, WORSE bug in the same function that had been latent all along: the HTTP status line (200) is received before the body streams, and the original code set last_status=resp.status_code immediately after getting the response -- so a connection that died mid-body-stream (same failure class as the SLURM-kill case, just interrupted one step earlier, e.g. a network blip) would fall through to `return last_status` = 200, reporting SUCCESS for a download that produced nothing at dest. RESTRUCTURED: the connection-attempt (sess.get) and the body-streaming (open+iter_content+replace) now have separately-scoped try/except blocks; a body-stream failure is caught, does NOT leave last_status stuck at a stale 200, and retries as a fresh request rather than falling through with a lie. 2 new/updated unit tests: the existing happy-path download test now also asserts no stray .part file is left behind after a clean success; a new test simulates a mid-stream requests.exceptions.ConnectionError and asserts the function does NOT return 200 and dest is NEVER created. validate_for_willi.sh GREEN 9/9, 105 passed (was 104) 5 skipped. USER ACTION REQUIRED before retrying: the corrupt zip from the killed job is still sitting in /tmp/mimic_smoke_test on the cluster and must be removed (rm -rf /tmp/mimic_smoke_test is simplest) before the next STAGE=meta run, since the fix prevents FUTURE partial files but does not retroactively repair this specific pre-existing one. NEXT ACTION: rm -rf /tmp/mimic_smoke_test; STAGE=meta OUT=/tmp/mimic_smoke_test sbatch scripts/build_mimic_cxr_local.sh -- if this completes cleanly through '[meta] done', Phase 7D-verify is FINALLY satisfied and the next step is `manifest` (still no network, read build_report.json before committing to the long `fetch`).",
    "2026-08-16 (truly final, this session): SESSION-COOKIE AUTH CONFIRMED WORKING LIVE. Job 2457565 (manual --account=aisc --partition=aisc-batch --qos=aisc, since the script's default at that point was still --account=default which hit AssocMaxSubmitJobLimit) actually RAN: log shows '[auth] session cookie loaded from /sc/home/krishankumar.bhushan/.physionet_session (32 chars)' then successfully fetched mimic-cxr-2.0.0-split.csv.gz, metadata.csv.gz, and chexpert.csv.gz in sequence -- the auth mechanism fix from earlier this session is VALIDATED against the real server, not just against mocks. Job then timed out mid-download of mimic-cxr-reports.zip (~135MB, the largest of the four small files) because the manual override also carried --time=00:10:00, borrowed from an earlier unrelated srun diagnostic and not intended as a real limit. FIX: build_mimic_cxr_local.sh #SBATCH defaults changed AGAIN -- partition=aisc-batch, account=aisc, qos=aisc together (exactly what job 2457565 proved works) -- so future submissions need zero manual flags, and the script's own --time default (1 day) is used instead of an accidentally-inherited 10-minute one. Updated test_build_mimic_cxr_local_slurm_wrapper_is_cpu_only_on_cpu_batch accordingly (now asserts partition=aisc-batch/account=aisc/qos=aisc, not account=default). validate_for_willi.sh GREEN 9/9, 104 passed 5 skipped (test count unchanged, assertions updated). NOTED TRADEOFF: aisc-batch is GPU-capable and preemptible-at-any-time for a job that never touches the GPU -- flagged to the user as worth raising with sc-helpdesk@hpi.de before the long fetch, alongside the still-outstanding 'contact helpdesk before large transfers' courtesy step. STILL PENDING: a clean end-to-end meta run with the corrected script defaults and no stray --time -- /tmp/mimic_smoke_test on the cluster currently holds a partial (3-of-4-files) state from job 2457565 and should be wiped (rm -rf) before retrying. NEXT ACTION: rm -rf /tmp/mimic_smoke_test; STAGE=meta OUT=/tmp/mimic_smoke_test sbatch scripts/build_mimic_cxr_local.sh -- watch for '[meta] done -> ...' in the log, which confirms all 5 files (4 small + IMAGE_FILENAMES) downloaded and the reports zip was extracted.",
    "2026-08-16 (final, this session): ROOT CAUSE FOUND -- .netrc/HTTP Basic Auth does not work against PhysioNet for this project AT ALL, independent of credential correctness. A user-supplied diagnostic (from an external review that had live curl access) proved it definitively: curl -u user https://physionet.org/settings/profile/ -> 302 to /login/ regardless of the password; the same account's Authorization header against a /files/ URL -> 403; the IDENTICAL /files/ URL with a session cookie -> 200. PhysioNet's Django app simply does not honour Basic Auth here -- the wget --user --ask-password recipe on their own project pages (which both the original external proposal and this project's first debugging attempts assumed was authoritative) is stale for the current deployment. This vindicates the earlier systematic elimination: netrc parsing (confirmed via curl -v showing a correctly-formed Authorization header), password correctness (retested with a fresh plain-alphanumeric password), file formatting (cat -A clean), and User-Agent/WAF filtering (browser UA also 403'd) were all correctly ruled out one at a time before concluding the cause was outside the client. REWROTE build_mimic_cxr_local.py's _get_session()/_download() for session-cookie auth: reads ~/.physionet_session (chmod 600, raw sessionid value, NOT the 3-line .netrc format), raises loudly if missing (no silent fallback to the now-known-broken Basic Auth path). ALSO CAUGHT AND FIXED a second, more dangerous latent bug the cookie-auth switch surfaced: when a session cookie expires MID-fetch, PhysioNet 302s to /login/ and requests follows redirects by default, arriving as an ordinary 200 with an HTML login page as the body -- unguarded, this writes login-page HTML into .jpg/.csv.gz files, and the resume check (Path.exists()) then skips those corrupt files FOREVER on every subsequent run, surfacing only weeks later as unexplained garbage in the training set. Fixed: _download() checks Content-Type/resp.url BEFORE streaming bytes, returns a SESSION_EXPIRED=498 local sentinel instead of 200; stage_fetch aborts the ENTIRE run immediately and UNCONDITIONALLY on seeing it -- deliberately NOT folded into the pre-existing ok==0 guard, because a cookie can expire mid-chunk, leaving ok>0 in the same chunk that also contains SESSION_EXPIRED entries (the ok==0 check alone would silently miss exactly this case). 4 new unit tests, all mocked/no-network: missing-cookie-file raises RuntimeError; a login-page-shaped 200 is detected and writes nothing to disk; a genuine 200 with real content-type still writes correctly (non-regression); a chunk with a first success then a session-expiry still hard-aborts. validate_for_willi.sh GREEN 9/9, 104 passed (was 100) 5 skipped. SECURITY NOTE: the user pasted a live session cookie value in chat during this diagnosis (necessary to prove the 200-with-cookie result) -- flagged immediately, advised to log out (invalidates it) and use a fresh cookie for the actual ~/.physionet_session file, same discipline as the earlier exposed-password incident. TOOLING NOTE: mid-session, Read/Bash access to this repo's already-edited files (build_mimic_cxr_local.py, eval_h100.sh) intermittently and then persistently returned EPERM for roughly 20 minutes of wall time, while creating brand-new files in the same directories worked fine throughout -- resolved itself after the user approved what was apparently a pending OS-level permission dialog ('resume the permission is given'); NOT a bug in the build script or the cluster, purely a local tooling hiccup, and the eventual Write of the patched file succeeded cleanly once access returned. NEXT ACTION (still not yet run): create ~/.physionet_session with a CURRENT session cookie value (browser dev tools -> Application/Storage -> Cookies -> physionet.org -> sessionid), chmod 600, then STAGE=meta OUT=/tmp/mimic_smoke_test sbatch scripts/build_mimic_cxr_local.sh.",
    "2026-07-07: Plan approved (/Users/krish/.claude/plans/i-want-to-implement-twinkling-ullman.md). Phase 1 started on branch h100_scaling. 3 Explore agents mapped infra/contrastive/model layers: key findings — CLIP loss in-batch only (no all_gather), Stage-0 undertrained (40K/117K), 150M/350M configs lack v2 wins. 4 user decisions locked (150M v2, backbone+MIMIC priority, diverse CXR w/ IU-Xray excluded, single-H100 first + 2-4 node file). 7 unresolved Qs answered (aisc-batch/shortrun partitions, gpu:h100:X, py>=3.10, BioMedLM teacher primary, max_pos=1024, Phase 3 secondary).",
    "2026-07-07: Phase 1 COMPLETE. H100_SCALING_PLAN.md + h100_scaling_state.json created; .gitignore allowlisted both; CLAUDE.md Session Bootstrap repointed to H100 plan (refactor plan -> completed-historical). validate_for_willi.sh green (69 passed, 5 skipped, 9/9 gates — no regression, doc-only). Committed on branch h100_scaling. current_phase -> phase2_h100_infra.",
    "2026-07-07: Phase 2 (H100 infra) LOCAL COMPLETE + committed. configs/trainer/h100_single_gpu.yaml (bf16, devices=1, compile=true, accum=1) + h100_multi_ddp.yaml (ddp, devices=-1, find_unused_parameters=true — the 2-4 node file). SLURM templates: train_stage0_h100.sh (aisc-batch 7-day, bs=64 accum=1, gc off), train_biomedclip_kd_h100.sh (aisc-shortrun, bs=128 = the H100 in-batch-negative lever, LR sqrt-scaled 32->128), eval_h100.sh (ppl/retrieval modes) — all ENV-parametrized (SCRATCH_ROOT/VENV_ACTIVATE) since aisc scratch/env paths TBD. flash-attn kept opt-in (not forced into base reqs — needs nvcc, breaks CPU/parity; ViT-only marginal). test_h100_trainer_configs_resolve added (2 cases green). validate_for_willi green 9/9 gates. 2D (py>=3.10 stack verify) + 2G (H100 smoke) deferred to physical H100 box. current_phase -> phase4_150m_v2_config (Phase 3 all_gather deferred post-Phase-6 per plan).",
    "2026-07-07: Phase 4 (150M v2 config) COMPLETE + committed. configs/model/hybrid_150m_v2.yaml: dim=768, 12 layers, layer_pattern [m,m,m,m,L,L,L,m,m,m,m,m] (3 mLSTM centered = 25% v2-parity ratio), norm_topology=hybrid, pooling=attention, max_pos=1024, lr=4.0e-4 (sqrt-width scaled, conservative), warmup=2000. Built + counted: 183.72M params (nominal '150M' — untied 50k embeddings ~77M dominate, same convention as 70M config -> 83M). test_hybrid_150m_v2_config_and_param_count added (asserts v2 invariants + tight [181,186]M band). validate_for_willi green 72 passed 9/9 gates. current_phase -> phase5_stage0_150m. NOTE: Phases 5-8 need the physical H100 (training runs); Phase 3 (all_gather) local but deferred post-Phase-6 per user.",
    "2026-07-07: 150M launch scripts created (Phase 5A/6A local prep): scripts/train_stage0_150m_h100.sh + train_biomedclip_kd_150m_h100.sh — thin wrappers over the generic H100 templates with 150M defaults + correct SBATCH walltime/mem (Stage-0 aisc-batch 4-day, contrastive aisc-batch 1-day). Runs are server-side. LOCAL SCAFFOLDING COMPLETE through Phase 4 + all launch scripts. Next action is on the H100 box: env setup (py>=3.10), Phase 2 2D/2G smoke, then sbatch train_stage0_150m_h100.sh.",
    "2026-07-08: FIRST H100 RUN on aisc (account=aisc; GPU alloc via --gpus=1 + --exclude=ga03,gx17v1; gpu:h100 gres unverified). Phase 2 2D DONE: setup_env_h100.sh builds .venv py3.11 (uv). CRITICAL: default PyPI torch wheel is cu130 but aisc H100 driver is CUDA 12.8 → installed torch 2.11.0+cu128 (setup pins TORCH_INDEX_URL=cu128). Stack is bleeding-edge (transformers 5.13, lightning 2.6.5) but Stage-0 ran: 183.7M student + 2.59B BioMedLM teacher loaded, PubMed packed 483M tokens, training started.",
    "2026-07-08: CORE PLAN ASSUMPTION WRONG — aisc H100 nodes are 80GB (81559MiB), NOT the 94/141GB the plan assumed. bs=48/accum=1 OOMs at first optimizer step in the Mamba selective_scan_parallel (289MB short, 96MB free) alongside the resident 2.6B teacher. FIX (Phase 5 launch script): gradient checkpointing ON (torch.utils.checkpoint per hybrid_block, wired at hybrid_lm.py:183-191) + microbatch 16 * accum 3 = eff batch 48 (token math preserved). Made ACCUM/GRAD_CKPT env-overridable in train_stage0_h100.sh. IMPLICATION for Phase 6: the 128-256 in-batch-negative thesis assumed 141GB — on 80GB the true batch ceiling is lower; train_biomedclip_kd_150m_h100.sh bs=128 will likely need grad-ckpt + reduced microbatch (accum does NOT add CLIP negatives, so batch ceiling directly caps the MIMIC lever). Re-scope Phase 6 target when reached.",
    "2026-07-11: PHASE 5 RUN FAILED GATE — job 2320933 completed all 120000 steps (~80h wall, 1.34 it/s) but final val/loss=7.044 -> val PPL 1165 (train PPL ~1106). Gate <=13.76 target / <=15.62 floor -> MISSED ~85x. Checkpoint UNUSABLE for Phase 6. Under systematic-debugging (root-cause first, no fix yet). Five variables changed at once vs the working A100 15.62 recipe: (1) hybrid_150m_v2 trained for the FIRST time [top suspect], (2) grad-ckpt ON, (3) eff batch 48 vs 64, (4) LR forced 6.0e-4 by train_stage0_h100.sh (overrode 150m config 4.0e-4), (5) torch 2.11/transformers 5.13. PROCESS GAP: Phase-2 2G arch smoke on 150m_v2 was NEVER run before the 3-day commit. EVIDENCE (scripts/diag_stage0_150m.sh, queue-and-forget): (A) loss+LR trajectory from version_1 tfevents [buffered stdout lost the live log; no metrics.csv but tfevents present]; (B) authoritative evaluate_lm.py PPL on stage0_model_only.pt to reconcile 1165 (lesson #5). Do NOT start Phase 6 or relaunch until A+B in.",
    "2026-07-11: CLUSTER REALITY — aisc-shortrun is INTERACTIVE-ONLY (srun/salloc); sbatch is REJECTED there. All sbatch scripts must use aisc-batch. Fixed partition in diag_stage0_150m.sh, eval_h100.sh, train_biomedclip_kd_h100.sh (were aisc-shortrun). srun --partition=aisc-shortrun for interactive sessions still fine. Corrects resolved_decisions.slurm (eval/smoke -> aisc-shortrun) for the sbatch case.",
    "2026-07-12: ROOT CAUSE FOUND (Phase-5 collapse). Evidence (diag_stage0_150m.sh tfevents + authoritative eval): val/loss 4.27 @ step666 (healthy, LR mid-warmup ~4e-4) -> 7.05 @ step3333 -> FLAT 7.05 for 100k steps (= unigram-entropy collapse / representation collapse). Collapse coincides EXACTLY with LR ramping to full 6.0e-4. Authoritative evaluate_lm.py PPL=1151.84 (loss 7.049) confirms it's real, not a metric artifact. CAUSE: generic train_stage0_h100.sh HARDCODED 70M values (LR=6.0e-4, warmup=1000) that overrode the 150M config's stability-tuned LR=4.0e-4/warmup=2000 -> too hot + too short warmup -> larger model collapsed. EXONERATED by evidence: arch (reached val 4.27 so fwd/bwd/grads work), gradient checkpointing (was learning), LR schedule shape (warmup/stable/decay correct). FIX: made LR/WARMUP env-overridable in train_stage0_h100.sh; train_stage0_150m_h100.sh now exports LR=4.0e-4 WARMUP=2000. NEXT (verify before 3-day commit — this is also the skipped 2G smoke): short smoke MAX_STEPS=6000 EXPERIMENT=..._lr4e4_smoke; confirm val/loss keeps DESCENDING past warmup (not collapse to 7.05). If stable -> full run; if still collapses -> drop LR=2.0e-4 / warmup=3000.",
    "2026-07-13: SMOKE PASSED — LR fix CONFIRMED. job 2329908 (MAX_STEPS=6000, LR=4.0e-4, warmup=2000, EXPERIMENT=h100_stage0_150m_v2_lr4e4_smoke): val/loss 3.141 -> val PPL 25.1 at step 6000, descending, NO collapse (vs failed run's 7.05/1165 frozen). Confirms root cause was the forced 6e-4 LR. throughput ~1.36 it/s => full 120k run ~73h (< 4-day wall). NEXT: move failed dir aside (outputs/h100_stage0_150m_v2 -> _FAILED_lr6e4) then launch full Stage-0 (sbatch train_stage0_150m_h100.sh, defaults 4e-4/2000/120k). Gate PPL <=15.62 floor / <=13.76 target at end. Then verify_stage0_handoff.sh on the good ckpt -> Phase 6.",
    "2026-07-14: FULL RUN COLLAPSED LATER (job 2332674, LR=4e-4). Trained HEALTHILY to step ~23660 (val PPL 18.75, descending, on track) then CATASTROPHIC COLLAPSE at ~step 24000 -> val PPL ~1170 (dead unigram entropy) held from step 25k-40k. Precursor recoverable wobbles at steps ~11k and ~14k (val PPL spiked to 30/86 then recovered). LR held constant 4e-4 (not the cause this time). grad_norm low (0.1-0.7) at sampled points incl post-collapse. => 150M is INTRINSICALLY UNSTABLE with this recipe: 70M stable@6e-4; 150M collapsed @3k(6e-4) and @24k(4e-4) — lower LR only DELAYS collapse. Smoke (6k steps) too short to catch it. ACTION: killed run. DEBUG (systematic, no blind relaunch): scripts/diag_collapse.sh dumps dense grad_norm/loss/beta2 over steps 20k-27k to tell spike-driven (->grad clip/beta2) vs gradual (->LR). Suspects: (a) LR still too hot for sustained 150M; (b) beta2 down-anneal 0.999->0.974 reduces optimizer smoothing late; (c) bf16 blowup in mLSTM exponential gates. Best pre-collapse ckpt ~step22000 = PPL ~19 (> 15.62 floor, unusable for gate). Phase 6 still blocked on a STABLE Stage-0.",
    "2026-07-14: ROOT CAUSE of the step-24k collapse = gradient SPIKE + too-loose clip. Dense tfevents (diag_collapse.sh, steps 20k-27k): grad_norm steady ~0.23 then step 24749 = 1.589 (~7x); at that exact step ce_loss 2.97->6.01->7.04 and stayed dead. adam_beta2 FLAT 0.999 (anneal not started) and LR FLAT 4e-4 => both RULED OUT. Clip IS active (lightning_module.py:232-244 on_before_optimizer_step, clip_grad_norm_ returns PRE-clip norm) but max_norm=1.0 is ~4x the 0.23 baseline — too loose; the clipped-to-1.0 spike still collapsed the fragile 150M mLSTM. FIX (minimal, one var): gradient_clip_val 1.0->0.5 (GRAD_CLIP env; 150m wrapper sets 0.5). LR/warmup/beta2 UNCHANGED (training was healthy: PPL 18.75 @23.6k). VERIFY: full run w/ clip=0.5, monitor PAST step ~30k (previous collapse 24.7k) via monitor_stage0.sh; kill early if it collapses. If 0.5 still collapses -> clip 0.3 + consider grad-norm step-skipping. Phase 6 still gated on a stable full Stage-0 (PPL <=15.62 floor; was on track pre-collapse).",
    "2026-07-16: CLIP 0.5 ALSO ONLY DELAYED (job 2339727, gradient_clip_val=0.5 confirmed). Healthy descent to val PPL 17.88 @ step 26994 then COLLAPSE to 1174 @ ~step 28000, dead through step 54k. Pattern now 3-for-3: LR6e-4->collapse@3k, LR4e-4->@24k, LR4e-4+clip0.5->@28k. Magnitude mitigations (LR, clip) only SHRINK the bad spike update -> delay, never prevent. Per systematic-debugging (3+ fixes fail => question approach, don't keep tweaking magnitudes). Model is intrinsically spike-fragile at 150M (70M was stable @6e-4/clip1.0). Loss stays FINITE 7.05 post-collapse (representation collapse to unigram, NOT NaN) => a bad-but-finite update tips weights into a dead attractor. RECOMMENDED real fix that PREVENTS (not delays): gradient-spike STEP-SKIPPING in on_before_optimizer_step (skip/zero the optimizer step when grad_norm > ~1.0, i.e. ~4x the 0.23 baseline) — standard large-model remedy (applies ZERO update on anomalous steps vs clip's bounded-but-still-bad update). Alternatives: fp32 mLSTM exponential gates (bf16 overflow?); shorter LR plateau/cosine decay. AWAITING user decision before code change to shared lightning_module.py. Best pre-collapse ckpt ~step27k = PPL ~17.9.",
    "2026-07-16: NUMERICAL ROOT FIX (user chose fp32 route). Audit found TWO bf16-unsafe hotspots causing the grad spikes: (1) PRIMARY — Mamba selective_scan_parallel runs entirely in bf16 and divides Bx by cumulative decay A_cum clamped to 1e-8 (scan_interface.py) -> underflow division = huge gradients (9/12 layers are mamba; ref Mamba keeps SSM scan in fp32). (2) mLSTM exp gate: gate_soft_cap=15 -> exp(i_logit) up to exp(15)=3.3M in bf16 (imprecise, large local grad). FIX: scan_interface.selective_scan now upcasts x/dt/A/B/C/D to fp32, runs scan, casts back; mlstm_block computes exp gate in fp32 then casts back. Local: forward+backward finite (fp32 AND bf16-autocast); 38 layer/parity tests pass (1 pre-existing unrelated failure test_hybrid_layer stale API). Kept clip=0.5 as backstop, LR 4e-4/warmup 2000 unchanged. VERIFY: full run + monitor PAST step 30k (clears both 24k & 28k collapse points). If it still collapses -> fall back to grad-spike step-skipping. Phase 6 still gated on a stable full Stage-0.",
    "2026-07-17: fp32 FIX CONFIRMED WORKING (job 2341991). CLEARED BOTH prior collapse points: val PPL 18.10@24k, 17.61@28k (both previously collapsed here), 16.45@54k and STILL DESCENDING. grad_norm rock-steady ~0.22-0.28 for 54k steps (no spikes) => bf16 exp/divide was the true root. Run healthy at ~54k/120k. On track to beat 15.62 floor and approach 13.76 target as WSD decay kicks in (decay starts ~85% ~step 102k). NEXT: let it finish 120k (~2 more days), gate check PPL<=15.62 floor/<=13.76 target, then verify_stage0_handoff.sh (RESULT PASS) -> Phase 6 contrastive.",
    "2026-07-19: STAGE-0 TARGET GATE CLEARED. WSD decay (LR 4e-4->~1e-4 by step 110k) drove val PPL 15.46@98k -> 14.42@106k -> 13.83@110k -> 13.44@114k, still dropping toward stretch (13.10). FLOOR 15.62 and TARGET 13.76 both BEATEN. grad_norm steady ~0.25 across all 114k steps (fp32 fix held perfectly). ~5.5k steps left (~3h) -> final PPL ~13.1-13.3. NEXT once job hits step 120000 / state CD: (1) read final PPL; (2) sbatch verify_stage0_handoff.sh (extracts stage0_model_only.pt from last.ckpt = final post-decay best; want RESULT PASS 0/0); (3) sbatch train_biomedclip_kd_150m_h100.sh = Phase 6 contrastive. Phase 5 effectively COMPLETE.",
    "2026-07-19: PHASE 5 COMPLETE. Full Stage-0 (job 2341991) finished all 120000 steps, FINAL val PPL 13.18 (train PPL 10.6). STRETCH tier (13.10) essentially matched; beats Floor 15.62 and Target 13.76 decisively vs A100 70M's 15.62. fp32 numerical fix held perfectly end-to-end (no collapse, grad_norm ~0.25 throughout). stage0_model_only.pt extracted (204 keys) from last.ckpt. FIXED verify_stage0_handoff.py: added sys.path repo-root insert (mirrors train_stage0_distill.py:40) — the ModuleNotFoundError 'hybrid_xmamba' was because python scripts/x.py puts scripts/ on path not repo root (not installed via pip -e). NEXT: re-run verify (RESULT PASS) then Phase 6 sbatch train_biomedclip_kd_150m_h100.sh (watch for 80GB OOM at bs=128 + Missing keys:0 load).",
    "2026-07-19: PHASE 6 STARTED, blocked on DATA ACCESS (not code). Handoff verify PASSED (0 missing/0 unexpected, 61 norm keys). Phase-6 job 2351233 loaded backbone (Missing keys:0), BiomedCLIP text teacher (196M) + image ViT (unfreeze 2 blocks), built JointMultiTaskLightningModule — then FAILED at load_mimic_cxr: HF 401 Unauthorized / DatasetNotFound for 'itsanmolgupta/mimic-cxr-dataset'. Confirmed via anon web fetch = 401 => dataset is GATED/PRIVATE (exists, not removed). FIX: export HF_TOKEN (account with dataset access; accept gating terms) then resubmit; sbatch inherits env. load_mimic_cxr (train_contrastive.py:437) reads HF_TOKEN from env automatically. Fallbacks if no access: override dataset.hf_repo_id to an accessible mirror, OR add local-path branch (load_from_disk/imagefolder) to load_mimic_cxr. NOT YET TESTED: 80GB OOM at bs=128 (surfaces after data loads); config findings_field/impression_field are best-guess pending column verify.",
    "2026-07-19: MIMIC DATA WIRED OK via local cache + offline (HF_DATASETS_OFFLINE=1 HF_HUB_OFFLINE=1, MIMIC_CACHE_DIR=/sc/home/krishankumar.bhushan/dataset/mimic_cxr_cache). Loaded 27570 train / 3063 val, columns ['image','findings','impression'] (config field names CORRECT). Backbone loaded Missing:0 Unexpected:0. Model assembled (HybridTextEncoder 184M + ViT 86M + teacher 195M, 15.2M trainable). NEW CRASH in sanity-val: 'CUDA error: Invalid access of peer GPU memory over nvlink or a hardware error' reported at token_embedding — but log states errors are ASYNC so the real fault is earlier. PRIME SUSPECT: trainer.compile_model=true (Phase-6 hardcoded it; Stage-0 ran compile_model=FALSE on the SAME node gx13v1 for 3 days flawlessly, and the h100 trainer yaml warns to disable compile for the custom Mamba/mLSTM Triton kernel path). FIX: COMPILE env (default false) + GRAD_CKPT env added to train_biomedclip_kd_h100.sh. If it recurs with compile off -> suspect gx13v1 hardware; add to --exclude and retry. STILL UNTESTED: bs=128 fit on 80GB.",
    "2026-07-19: CUDA FAULT = FAULTY NODE gx13v1, CONFIRMED. Same job on gx07 cleared sanity-val and the model summary with NO CUDA error (compile_model=false AND the earlier compile=true theory both irrelevant — compile was exonerated first). gx13v1 now added to --exclude in all h100 scripts (cudaErrorContained = contained hardware fault). NEXT ERROR (benign config bug, fixed): ValueError val_check_interval(250) > training batches(215). MIMIC 27570/bs128 = 215 batches/epoch; Lightning reads an int val_check_interval as batches-WITHIN-epoch unless check_val_every_n_epoch=None. The configs set it null but train_contrastive.py never passed it to pl.Trainer (defaulted to 1). FIX: thread check_val_every_n_epoch=cfg.trainer.get('check_val_every_n_epoch',1) into pl.Trainer (:838) -> val_check_interval now counts GLOBAL STEPS, batch-size agnostic (bs=256 -> only 107 batches would also have broken). Sanity-val at bs=128 passed on gx07 => 80GB memory fit looks OK so far.",
    "2026-07-19: PHASE 6 IS TRAINING (job 2351410, gx07). Ran ~4 epochs / ~1000 steps before OOM. KILL GATES so far: cos_text_teacher 0.874-0.892 >= 0.85 PASS; val/clip_loss 4.54 (gate <3.0, still early); retrieval climbing i2t R@10 0.013->0.020->0.023, t2i 0.018->0.025->0.031 (gate >=0.1045 by step 3000 — long way to go, but backbone only just unfroze). OOM CAUSE: at freeze_text_encoder_steps=1000 the 184M backbone unfreezes (was 15.2M trainable) -> activations+grads+optimizer for 12 layers appear at once -> OOM in selective_scan (scan_interface.py:86). Confirms the 80GB ceiling flagged 2026-07-08: scan intermediate 128*256*1536*16 ~= 3.2GB EACH and the fp32 stability fix DOUBLES that vs bf16; bs=128 is also 4x Stage-0's tokens/microbatch. FIX: GRAD_CKPT default true in train_biomedclip_kd_150m_h100.sh (same lever as Stage-0), keeping bs=128 so in-batch negatives (the MIMIC lever) are preserved. Fallback BATCH_SIZE=64. NOTE: train/clip_loss logged ~0.000 vs val/clip_loss 4.54 — verify that gap once training is stable past step 1000.",
    "2026-07-19: GRAD-CKPT FIX WORKED — Phase 6 job 2351429 (gx10) passed the step-1000 backbone unfreeze that OOM'd the previous run and kept training (27min+ and counting). use_gradient_checkpointing=true confirmed, bs=128 PRESERVED so the in-batch-negative lever is intact. Frozen-backbone epochs 1-4: i2t R@10 0.013->0.020->0.023, t2i 0.018->0.025->0.031. MONITORING: the [CLIPRetrieval epoch=N] lines are plain prints and DO flush to the SLURM log (per-epoch, ~215 steps each) — that's the usable progress signal; only the Rich per-step bar is buffered. Generalized monitor_stage0.sh with LOGGER_SUBDIR (contrastive uses TensorBoardLogger name='tensorboard' -> logs/tensorboard, vs Stage-0 logs/stage0_kd) and a broad tag filter so it dumps clip_loss/cos_text_teacher/retrieval too. GATE WATCH: MIMIC i2t R@10 >= 0.1045 by step 3000 (currently 0.023 pre-unfreeze).",
    "2026-07-19: PHASE 6 STEP-3000 GATE PASSED (job 2351429, bs=128 + grad-ckpt). val i2t R@10: 0.0232@999 (frozen) -> 0.0728@1249 (unfreeze jump) -> 0.0895@1999 -> 0.1090@2749 -> 0.1068@2999, vs gate >=0.1045 by step 3000. t2i R@10 0.0996. val/clip_loss 4.64->3.50; val/total 4.85->3.62; simcse 0.28->0.06. RESOLVED the train/clip_loss=0.000 mystery: NOT a bug — effective_alpha_kd=1.0 during the 1000-step freeze (pure KD), CLIP starts at step 1024 (4.13->2.92). CAUTION FLAGS: (a) cos_text_teacher 0.89@999 -> 0.60 post-unfreeze (student trades teacher alignment for CLIP); (b) grad_norm rising 0.25 -> 12.2 pre-clip with gradient_clip_val=1.0 => ~12x clipping every step, same rising shape that preceded Stage-0 collapses — WATCH; (c) 5000 steps @ bs128 on 27570 pairs = ~23 epochs, and at 2999 both R@10 and clip_loss ticked the wrong way (0.1090->0.1068, 3.4935->3.5023) => possible overfit onset; save_top_k=3 on val/loss preserves the best ckpt. CRITICAL CAVEAT: 10.68% is the IN-TRAINING metric on an N=3063 gallery — NOT directly comparable to the 10.45% A100 baseline measured via evaluate_cxr_retrieval.py. Must reconcile with the authoritative protocol (plan lesson #5) before citing any beat.",
    "2026-07-19: PHASE 6 OVERFITTING CONFIRMED @ step ~2749 (job 2351429). val/total_loss MIN 3.6083@2749 then 3.6229 -> 3.6928 -> 3.7385@3499 (3 consecutive rises); val/clip_loss min 3.4935@2749 then rising; val/kd_loss 0.3542->0.4394; cos_text_teacher 0.6458->0.5606. i2t R@1 peaked 0.0186@2249, R@5 peaked 0.0676@2749, both now DECLINING. i2t R@10 PLATEAUED ~0.1025-0.1110 (0.1110@3499 = best but within noise). grad_norm ~12.3 pre-clip vs clip 1.0 (heavy 12x clipping). KEY FINDING (inverts a plan assumption): bs=128 at fixed max_steps=5000 = 640k samples = 23.2 EPOCHS on 27570 pairs, vs A100 bs=32 5000 steps = 5.8 epochs. Bigger batch at the same step count means 4x MORE passes, so it overfits HARDER not less; val optimum ~12.8 epochs (step 2749). For any rerun: scale max_steps DOWN with batch (bs128 -> ~2750-3000 steps). ACTION: killed run; best ckpt is step ~2749 (save_top_k=3 on val/loss). NEXT: authoritative evaluate_cxr_retrieval.py on best ckpt — the in-training 11.1% is on an N=3063 gallery and is NOT comparable to the 10.45% A100 baseline (lesson #5). NOTE: checkpoints are DIRECTORIES (contrastive-step=XXXXXX-val/), format must be checked before eval.",
    "2026-07-19: FIXED 3 bugs in eval_h100.sh retrieval path before first use: (1) it passed --model-config, which evaluate_cxr_retrieval.py does NOT accept (arch is auto-detected from the ckpt) -> would have errored instantly; (2) DATASET default was 'mimic_cxr' but the script's choices are exactly {mimic, indiana}; (3) --cache-dir was never passed so it used the stale willi default /scratch/bhushkri/... -> would have hit the gated repo and 401'd. Now passes --cache-dir ${EVAL_CACHE_DIR} and defaults DATASET=mimic. IMPORTANT (corrects my earlier caveat): evaluate_cxr_retrieval.py loads MIMIC via split='train[90%:]' = the SAME 3063 pairs as the in-training val metric, so the in-training ~11.1% IS protocol-comparable. It also explicitly loads fine-tuned image_encoder.* (guards the fresh-ViT 1.89% trap). BEST CKPT identified: outputs/h100_kd_150m_v2_bs128/checkpoints/contrastive-step=002750-val/total_loss=3.6083.ckpt (filename confirms the val-loss minimum).",
    "2026-07-19: LATENT EVAL BUG exposed by the 150M scale-up. evaluate_cxr_retrieval.py:load_text_encoder built HybridTextEncoder(cfg, embed_dim=dim) — i.e. it assumed the CLIP joint-space embed_dim equals the model HIDDEN dim. That coincided for the 70M (dim=512 == embed_dim=512) so it never surfaced; the 150M has dim=768, so it built a 768-d projection head and died with 'size mismatch for projection_head.3.weight: ckpt [512,768] vs model [768,768]'. FIX: infer embed_dim from the checkpoint (projection_head.3 is Linear(dim->embed_dim), so shape[0]==embed_dim), fall back to dim. VERIFIED locally: 150M-like (dim=768) infers embed_dim=512 -> 0 missing/0 unexpected; 70M-like (dim=512) -> 0/0 (backward compatible). This would have blocked EVERY 150M retrieval eval — same class as the plan's 'eval must auto-detect from checkpoint' lesson.",
    "2026-07-19: PHASE 6 RUN-1 AUTHORITATIVE RESULT = i2t R@10 0.1084 (10.84%), t2i R@10 0.0983, mean R@10 0.1033, N=3063, ckpt contrastive-step=002750-val/total_loss=3.6083.ckpt. Eval clean: fine-tuned image_encoder loaded (151 keys, 0 missing/0 unexpected), embed_dim=512 auto-detected, norms 1.0, paired cosine 0.386. VS PLAN BAR: floor 0.1045 CLEARED, target 0.12 NOT reached, stretch 0.14 no. VS A100 baseline 0.1045: +0.39pp, but SE at p=0.108,n=3063 is ~0.56pp => the gain is WITHIN 1 SE, i.e. this MATCHES the A100 rather than beating it. => The plan's central thesis (32->128 in-batch negatives pushes MIMIC to 12%+) is NOT yet supported — BUT the test was CONFOUNDED by the epoch budget (23 epochs vs A100's 5.8; overfitting from step ~2750). The negatives lever has not had a fair test. FIXES for the controlled rerun: (a) MAX_STEPS now env-overridable in train_biomedclip_kd_h100.sh (was hardcoded 5000); (b) evaluate_cxr_retrieval.py tiers updated to the H100 bar (mimic floor 0.1045 / target 0.12 / stretch 0.14) — it still carried the OLD refactor bars (0.0823/0.0999/0.12) and so printed '✓ TARGET' for a 10.84% run, which would have been misleading in the writeup.",
    "2026-07-20: EPOCH-MATCHED BATCH SWEEP — THE PLAN'S CENTRAL THESIS IS NOT SUPPORTED. Both arms saw exactly 384,000 samples = 13.93 epochs. bs=128 x 3000 steps (~127 negatives): in-training val i2t R@10 = 0.114, t2i 0.108, cos_text_teacher 0.566. bs=64 x 6000 steps (~63 negatives): i2t R@10 = 0.120 (peak 0.122), t2i 0.112, cos 0.582. => HALF the negatives did BETTER (+0.6pp, ~1 SE at n=3063, so state it as 'not worse / nominally ahead'). The epoch fix alone lifted bs=128 from 0.1084 (23 epochs, run-1 authoritative) to 0.114 (14 epochs). CONCLUSION: on a 27570-pair set the binding lever is OPTIMIZER-STEP COUNT, not in-batch negative count — bs=64 got 2x the updates on identical data and won. H100 memory headroom is better spent on more steps than bigger batches. CAVEAT: do NOT compare val/clip_loss across batch sizes — InfoNCE grows with negative count (ln128-ln64=0.69 vs observed gap 0.56), so that difference is an artifact, not alignment; retrieval is the only fair comparator. IMPLICATION: Phase 3 (distributed all_gather negatives) drops in priority — it scales the lever that this sweep shows is NOT binding on this dataset. NEXT: authoritative evaluate_cxr_retrieval.py on BOTH best ckpts vs floor 0.1045 / target 0.12; then a bs=32 x 12000 arm (same 13.93 epochs) to complete the sweep as {32,64,128} — 256 will not fit on 80GB.",
    "2026-07-20: PHASE 6 AUTHORITATIVE RESULTS (all N=3063, best-by-val/loss ckpt, fine-tuned image_encoder loaded 151 keys 0/0). bs=128 23ep -> i2t R@10 0.1084; bs=128 14ep -> 0.1090; bs=64 14ep -> 0.1113 (BEST). Floor 0.1045 CLEARED by all three; TARGET 0.12 MISSED by all three; stretch no. STATISTICS: spread across the 3 arms is 0.29pp vs SE ~0.57pp (p~0.11,n=3063) => the arms are STATISTICALLY INDISTINGUISHABLE, and all sit within ~1.2 SE of the A100 baseline 0.1045. => PHASE 6 IS A WELL-CONTROLLED NULL RESULT: neither the negatives lever nor the epoch fix produced a significant MIMIC gain. TWO WRITEUP FINDINGS: (1) backbone LM quality did NOT transfer — Stage-0 went 15.62->13.18 PPL (stretch tier) yet MIMIC retrieval stayed flat, so LM quality is not the retrieval bottleneck; (2) checkpoint-selection metric matters — in-training retrieval peaked 0.120-0.122 (bs64) vs 0.1113 authoritative, because ModelCheckpoint selects on val/total_loss (min @4500) while retrieval peaks later (~6000). Do NOT reselect on peak retrieval: val split == eval split (train[90%:], same 3063 pairs), so that is selection-on-test. Defensible number = 0.1113. NEXT: Indiana eval (cache already present at ~/dataset/indiana_cxr_cache) on the bs=64 best ckpt to get the other gate for free, then Phase 7 data diversification.",
    "2026-07-21: REVIEWED a proposed recipe change (data expansion + vit_unfreeze=0 + freq_kd=true + head_lr 6e-4->3e-4 + checkpoint-on-retrieval). VERDICT: 2 of 4 training changes are FALSIFIED by ablations already in this repo (vit_unfreeze=0 cost 2.5pp MIMIC and recovered zero Indiana; freq_kd=true cost 0.94pp Indiana), 1 is invalid (checkpoint-on-retrieval = selection-on-test, val split IS eval split train[90%:]), 1 is CORRECT (head_lr). Data expansion is already Phase 7, but its stated diagnosis (overfitting 27.5k pairs is the binding constraint) is weakly supported: cutting epochs 23->14 moved authoritative R@10 only 0.1084->0.1090. NEW FINDING the proposal missed: backbone_lr/head_lr were HARDCODED at bs=128 values in train_biomedclip_kd_h100.sh:90-91, so the ENTIRE batch sweep ran at bs=128 LRs — the bs=64 winner trained ~1.4x too hot. The sweep was never LR-matched, which is an unreported confound on the Phase-6 null result.",
    "2026-07-21: PHASE 6B SCAFFOLDING DONE (local; run pending H100). (a) BACKBONE_LR/HEAD_LR env-overridable in train_biomedclip_kd_h100.sh; (b) 150M wrapper now DERIVES backbone_lr/head_lr/MAX_STEPS from BATCH_SIZE with a fixed 384000-sample (13.93-epoch) budget for {32,64,128} — both the LR confound and the epoch-drift confound are now structurally impossible; default batch flipped 128->64 (the best arm); EXPERIMENT name carries head LR so same-batch arms don't overwrite. (c) biomedclip_kd_joint_v2.yaml freq_kd default true->FALSE (was only false via launch-script override — a live trap); train_biomedclip_kd_phase15.sh now passes distill.freq_kd=true explicitly to preserve its ablation semantics. (d) 2 new parity tests (LR overridability; bs x steps == 384000 + sqrt-scaling off the bs=32 anchor). validate_for_willi.sh GREEN 74 passed 5 skipped 9/9 gates. NEXT on H100: two bs=64 arms (head 4.24e-4 default vs 3.0e-4), watch grad_norm fall from ~12.3, then authoritative eval vs 0.1113 with SE ~0.57pp in mind. ALSO RECOMMENDED FIRST: Phase 6C teacher-ceiling reference (stock BiomedCLIP on the same protocol, no training) — decides whether 12% is reachable at all.",
    "2026-07-22: PHASE 6B RUN-1 (job 2357924) FAILED AT DATA LOAD — gated-dataset regression, NOT the LR change. LR wiring worked perfectly (log: backbone=1.41e-5 head=4.24e-4 max_steps=6000, distill.* overrides all applied, backbone loaded Missing:0). Crash = DatasetNotFoundError 401 on itsanmolgupta/mimic-cxr-dataset: the run went ONLINE and hit the gated repo. ROOT CAUSE: the generic template train_biomedclip_kd_h100.sh never carried the offline settings that made the 2026-07-19 Phase-6 runs work (HF_DATASETS_OFFLINE=1 HF_HUB_OFFLINE=1 + local cache) — they were passed manually then, and my Phase-6B scaffolding made this script the entry point without persisting them; MIMIC_CACHE_DIR also defaulted to an empty scratch path. FIX: template now exports HF_DATASETS_OFFLINE/HF_HUB_OFFLINE=1 (overridable) and defaults MIMIC_CACHE_DIR=/sc/home/$USER/dataset/mimic_cxr_cache (the known-good 2026-07-19 cache). validate_for_willi green 74/9 gates. RESUBMIT: git pull then sbatch (offline+cache now baked), or without pulling: HF_DATASETS_OFFLINE=1 HF_HUB_OFFLINE=1 MIMIC_CACHE_DIR=/sc/home/$USER/dataset/mimic_cxr_cache sbatch scripts/train_biomedclip_kd_150m_h100.sh",
    "2026-07-22: PHASE 6B BOTH LR ARMS LAUNCHED + TRAINING HEALTHY (offline data fix confirmed working — 'offline mode enabled', cache /sc/home/$USER/dataset/mimic_cxr_cache, 27570/3063 loaded, backbone Missing:0). Arm A job 2359951 (gx14, head=4.24e-4 √-matched): frozen R@10 0.010->0.023, unfreeze@1000 -> 0.066->0.085 by ~epoch4. Arm B job 2359952 (gx11, head=3.0e-4 conservative): frozen 0.011->0.024, unfreeze -> 0.058->0.070 by ~epoch3. Both ~epoch 3-4 of ~14, normal shape, no anomaly. NOTE: prior 0.1113 best actually ran head=6e-4 (hardcoded bs128 value) — so BOTH current arms are LOWER LR than the run being beaten. TOO EARLY to compare arms (Phase-6 peak/overfit was steps ~2000-4500; now ~1300-1700). KEY METRIC grad_norm NOT in the [CLIPRetrieval] prints — pull via monitor tensorboard tags to test whether lower LR cut the ~12.3 vs clip=1.0 (12x clipping); that's the mechanistic answer. NEXT: let both finish 6000 steps; authoritative evaluate_cxr_retrieval.py on both best-by-val/loss ckpts vs 0.1113 (SE ~0.57pp, need >~1.1pp for a real win); then Phase 6C teacher-ceiling reference regardless.",
    "2026-07-25: PHASE 6B COMPLETE = 7th CONSECUTIVE NULL. Both arms finished 6000 steps clean. Arm A head_lr=4.24e-4 (job 2359951): in-training i2t R@10 peak 0.120@ep11, final 0.116, cos_text_teacher 0.570, val/clip_loss 2.897, val/total_loss 3.024. Arm B head_lr=3.0e-4 (job 2359952): peak/final 0.119, cos 0.574, val/clip_loss 2.829, val/total_loss 2.955. Prior best head_lr=6e-4: peak 0.122, final 0.116. Spread 0.1-0.3pp vs SE ~0.57pp => LR IS NOT THE BINDING CONSTRAINT. SECONDARY FINDING: lower LR removed the late-epoch retrieval rollover (arm B ends at its max, no overfit decline) while leaving the plateau height unchanged => OVERFITTING IS NOT BINDING EITHER. Lever tally now 7 nulls (PPL 15.62->13.18, 70M->150M, negatives 32->128, epochs 23->14, batch 128 vs 64, head_lr 6e-4->4.24e-4, head_lr 6e-4->3.0e-4) vs 1 positive (ViT unfreeze 0->2, +2.5pp).",
    "2026-07-25: FALSIFIED THE cos_text_teacher CEILING CLAIM (reviewed two external plateau analyses; both built their headline architecture argument on it). CLAIM: cos~0.57 is a representational ceiling from a causal SSM student vs bidirectional PubMedBERT teacher, therefore bidirectionalize the text tower. REFUTED BY OUR OWN LOGS: during the KD-only warmup (steps 0-1000, CLIP gated off at lightning_module.py:1012-1016, alpha_kd_warmup=1.0, LM backbone FROZEN so only projection_head+attn_pool+logit_scale train = 15.2M params) cos_text_teacher reaches 0.874-0.892. It FALLS to 0.60->0.57 only AFTER CLIP switches on at step 1000, and then sits at 0.566/0.570/0.574/0.582 across a 2x LR range, both batch sizes and both model scales. A frozen causal backbone + small head hitting 0.89 against the bidirectional teacher rules out an architecture ceiling; 0.57 is a LOSS-WEIGHT EQUILIBRIUM between KD (alpha_post=0.3) and CLIP (beta=1.0) whose targets are themselves cos~0.5-0.7 apart in joint space -- exactly what the code comment at :1086-1090 predicted. Invariance to LR/batch/scale is the signature of an equilibrium, not of an optimization failure. CONSEQUENCES: (1) cos_text_teacher must NEVER gate whether the text tower is architecturally adequate -- any experiment with a 'did cos exceed 0.62' kill criterion draws a wrong conclusion; (2) KD-anchor decay (6D-2) is PROMOTED, it attacks the equilibrium directly and is derived from our data not literature; (3) bidirectional encode (6E) retained but RE-MOTIVATED on report structure (Impression recontextualizes Findings), and that framing must survive into the writeup.",
    "2026-07-25: PLAN EXTENDED with Phases 6C (measurement block, zero training), 6D (factorial lever block: vit_unfreeze depth / KD decay / SigLIP+multipos / stack), 6E (bidirectional encode, conditional on 6C-2), 6F (disjoint selection split). Also verified and REJECTED from the two external proposals: 'the 0.1113 is a last-checkpoint artifact, best-val recovers 0.9pp free' (FALSE -- 0.1113 came from contrastive-step=002750-val/total_loss=3.6083.ckpt which IS best-by-val/total_loss per train_contrastive.py:801-806; the 0.120-0.122 in-training peak is on the same 3063 pairs the eval uses = banned selection-on-test); XBM cross-batch queue (= the MoCo queue already ablated as harmful, moco_queue_size=0 is canonical); RAD-DINO/MedSigLIP image-backbone swap (breaks assert img_out == model.embed_dim at :419-422 and removes the BiomedCLIP joint space the KD teacher targets -- different project); resolution 224->336/448 (BiomedCLIP pos-embeds fixed at 224); two-stage text-only distillation to raise cos (optimizes a number that already hits 0.89); 'switch last-token to mean pooling' (moot -- v2 uses pooling_strategy=attention, never last-token). CONFIRMED-CORRECT and adopted: KD never decays (step function at :1091-1095), hard one-hot arange(B) CLIP targets (:540), strict-index gt=arange(N) in eval (evaluate_cxr_retrieval.py:412), gpt2-vs-WordPiece tokenizer mismatch, val_split==test_split, vit_unfreeze sweep is config-only (_get_vit_blocks generalized at :441-457). USER DECISIONS: 6D runs regardless of the 6C-1 teacher number; no PhysioNet credentialing yet so full-MIMIC scale-up stays out of scope and Phase 7 remains multi-source only.",
    "2026-07-25: PHASE 6C-6F SCAFFOLDING COMPLETE (local; runs pending H100). validate_for_willi.sh GREEN 9/9 gates, 86 passed 5 skipped (was 74). NEW SCRIPTS: reference_biomedclip_zeroshot.py (6C-1 stock BiomedCLIP both towers on the identical train[90%:] N=3063 gallery + 6C-2 tower-swap 2x2 grid, each cell also reported dedup-aware, plus the strict-index oracle ceiling); audit_mimic_duplicates.py (6C-3, CPU/text-only -- drops the image column so nothing decodes a PNG; reports oracle R@K ceiling on the eval gallery and analytic+simulated false-negative rate per batch size); run_phase6c_measurements.sh (SLURM, offline+cache baked); submit_phase6d_arms.sh (dry-run-by-default submitter for arms D0/D1a/D1b/D1c/D2/D3/D4/D5). CODE: lightning_module.py gains kd_decay_steps/alpha_kd_floor (linear alpha_kd ramp post-unfreeze; 0 preserves the step function exactly), _siglip_loss, _multipos_clip_loss, _pos_mask_from_hash, and train|val/false_neg_rate logging that fires whenever the dataset emits text_hash REGARDLESS of use_multipos (that is how the lever earns its justification); hybrid_lm.py gains logit_bias (init -10, appended to the head param group) and encode(bidirectional=) with a length-aware _reverse_index that reverses only real tokens and is its own inverse; configuration_hybrid.py gains bidirectional_encode; train_contrastive.py emits text_hash via blake2b (NOT the salted builtin hash(), which differs across dataloader workers) and threads all four new distill keys + echoes them to the SLURM log; evaluate_cxr_retrieval.py gains group_ids_from_texts/normalize_report_text + optional dedup-aware groups= in compute_retrieval_metrics, and AUTO-DETECTS bidirectional_encode from checkpoint hparams (the flag adds no weights so it cannot be sniffed from the state dict -- same failure class as the fresh-ViT load that read 1.89% instead of 10.94%). 12 NEW PARITY TESTS pin: config defaults ARE the control; the alpha_kd ramp math incl. kd_decay_steps=0 back-compat; multipos == nt_xent on an identity mask (strict generalisation, so enabling it changes nothing on duplicate-free batches); SigLIP finiteness/sign/bias-in-optimizer; hash grouping + None fallback; text_hash stability and case/whitespace collision; bidirectional adds zero state-dict keys yet changes the embedding; _reverse_index involution with right padding; hparams round-trip for eval auto-detect; dedup-aware metric reduces to strict when all reports are unique; and that every launch-script lever is env-overridable at its Phase-6B default. 6F needs NO code -- SELECTION_SPLIT=true just repoints train/val to train[:85%]/train[85%:90%] while the authoritative eval stays hardcoded at train[90%:].",
    "2026-07-25: PHASE 6C COMPLETE (job 2372055, 86s wall, gx08) on the head=3.0e-4 best-by-val/total_loss ckpt (step 4750, 2.9465). TOWER GRID N=3063 strict-index i2t R@10: stock_vit x biomedclip_text 0.0340 | stock_vit x student_text 0.0232 | finetuned_vit x biomedclip_text 0.0431 | finetuned_vit x student_text 0.1172. FOUR HYPOTHESES KILLED: (1) TEACHER PARITY DEAD -- stock BiomedCLIP is 3.40%, the student is 3.4x that; the 12% target is NOT above the anchor and real headroom exists (oracle 99.0%). (2) THE KD ANCHOR IS A DRAG, NOT A CEILING -- BiomedCLIP's text tower is worth only 4.31% even on the fine-tuned ViT, so alpha_kd_post=0.3 spends the whole post-warmup run pulling z_text toward a representation ~3x WORSE than the one CLIP is building. This is a stronger and more actionable version of the external review's guess => 6D-2 PROMOTED to co-priority with 6D-1, plus a D2b alpha_kd_floor=0.05 fallback arm. (3) FALSE NEGATIVES QUANTITATIVELY DEAD -- at bs=64 only 19% of batches contain a single false negative, mean 0.58 pairs out of a 4096-entry matrix (bs=128: 2.32 of 16384). BOTH external reviews ranked this a top-3 cause and called it 'the direct explanation' for the flat negatives lever; it is not => MULTIPOS DROPPED from the 6D-3 arm, which is now SigLIP-only with downgraded expectations (SigLIP's headline justification in both reviews was this non-existent problem). Mask + false_neg_rate logging stay implemented for Phase 7, where multi-source data may change the duplication profile. (4) METRIC ARTIFACT DEAD -- gallery duplicates are 2.0% (largest group 40 of 3063), oracle R@10 = 99.0%, dedup-aware R@10 differs from strict by 0.03pp; keep the strict metric as headline. (5) TEXT TOWER IS NOT THE WEAK HALF -- swapping BiomedCLIP's text tower IN costs 7.41pp => 6E DEPRIORITISED per the pre-registered rule (code shipped, inert behind BIDIRECTIONAL=false, opportunistic only). CAVEAT to state in the writeup: the fine-tuned ViT was co-trained with the student text tower so that cell is favoured by construction; mitigating evidence is that fine-tuning still HELPED BiomedCLIP's own text tower (3.40 -> 4.31), so the ViT did not drift into a space hostile to the teacher. Defensible claim = 'substituting BiomedCLIP's text tower does not help', NOT 'the student text tower is strictly better'. LOGICAL GAP: 6C-2 answers 'is BCT better than ours' (no); it does NOT answer 'would a bidirectional student beat a causal one', which is what 6E proposes -- 6E is UNSUPPORTED, not REFUTED. INCIDENTALS: stock_vit x student_text is strongly asymmetric (i2t 0.0232 vs t2i 0.0813) = hubness in the student text space relative to stock-ViT geometry, while the co-trained pair is balanced (0.1172/0.1087) -- evidence of genuine co-adaptation, not a collapsed text space; the '1 missing key: logit_bias' at load is expected and benign (ckpt predates the SigLIP param, loads at its -10 init under strict=False, encode() never reads it); fine-tuning 2 ViT blocks lifts the FROZEN-TEXT system 3.40 -> 4.31 (+0.91pp). NET CONCLUSION: the binding constraint is the IMAGE REPRESENTATION. Every text-side and optimization lever is null (7 of them); the only two positives on record are both image-side (ViT unfreeze 0->2 = +2.5pp; ViT unfreeze with frozen text = +0.91pp). 6D priority order set accordingly: 6D-1 (ViT depth) and 6D-2 (KD decay) first, 6D-3 SigLIP-only third. ALSO NOTE: 6C's finetuned_vit x student_text cell IS the authoritative 6B-4 number for the head=3.0e-4 arm = 0.1172, vs the 0.1113 baseline -- +0.59pp, ~1 SE, so still not a win, but it is the best authoritative number recorded so far.",
    "2026-07-26: PHASE 6D COMPLETE -- VIT ADAPTATION DEPTH IS THE LEVER. Seven arms, bs=64, 6000 steps, LR-matched, 13.93 epochs each. In-training i2t R@10 @ep13: D0 vit=2 control 0.116 | D1a vit=4 0.132 | D1b vit=6 0.150 | D1c vit=12 0.168 (peak 0.171) | D2 kd_decay 0.118 | D3 siglip 0.119 | D5 gamma_simcse=0 0.122. D1c = +5.2pp over control = ~9x the 0.57pp SE, MONOTONE in depth, with val/clip_loss falling monotonically alongside (2.897/2.774/2.687/2.585) and i2t R@1 nearly doubling (0.017->0.030). Eval images are never trained on, so this is generalization, not a tie-breaking or selection artifact. No OOM at bs=64 with all 85.1M ViT params trainable (the D1c OOM risk did not materialize). IN-TRAINING 0.168 EXCEEDS THE PLAN'S STRETCH TIER (0.14), not merely target (0.12) -- pending authoritative confirmation. 6D-2 FALSIFIES MY OWN 6C-DERIVED PREDICTION: I argued from 6C that the alpha_kd=0.3 anchor drags z_text toward a 4.31% representation and that releasing it should help. The mechanism fired exactly as designed (cos_text_teacher collapsed 0.570 -> 0.194) and retrieval did NOT move (+0.2pp); val/clip_loss was marginally WORSE (2.934 vs 2.897), so at alpha=0.3 KD is a mild REGULARIZER not a drag. KD-anchor hypothesis now dead in BOTH directions. 6D-3 SigLIP (+0.3pp) and 6D-5 gamma_simcse=0 (+0.6pp) both NULL -- combined with 6C-3 killing the false-negative premise, the entire objective-repair line of both external reviews is empirically closed. RUNNING TALLY: 10 nulls (Stage-0 PPL, model scale, negatives, epochs, batch, head_lr x2, KD decay, SigLIP, SimCSE), 1 dominant lever (ViT adaptation depth) now with a dose-response curve. 6D-4 stack is redundant -- D2/D3/D5 are null so the stack IS D1c. NEXT = Phase 6G: vit_lr has sat at 1e-6 for the whole project (3 orders below head_lr), so the winning arm is one where the whole tower is unfrozen but barely moving; sweep vit_lr {3e-6,1e-5,3e-5} at depth 12, add full-tower scope (patch_embed/pos_embed/final norm/visual proj are STILL frozen at depth 12 since _get_vit_blocks returns transformer blocks only), run authoritative MIMIC + Indiana evals on D1c, and confirm under SELECTION_SPLIT=true since the arm comparison itself used the eval gallery.",
    "2026-07-26: AUTHORITATIVE EVALS IN -- STRETCH TIER CLEARED. D1c (vit_unfreeze=12, ckpt step 4750 total_loss=2.6680): MIMIC i2t R@10 = 0.1714 (R@1 0.0300, R@5 0.1032, t2i R@10 0.1538, paired cosine 0.4230, N=3063). D1b (vit=6): 0.1430 (R@1 0.0206, t2i 0.1394, cos 0.4084). Authoritative dose-response is MONOTONE too: 0.1113 (vit=2) -> 0.1430 (vit=6) -> 0.1714 (vit=12). +6.01pp over the standing 0.1113 baseline = ~10.5x SE. Floor 0.1045 CLEARED, target 0.12 CLEARED, STRETCH 0.14 CLEARED. INDIANA on the same ckpt: i2t R@10 = 0.0485 (t2i 0.0700, R@1 0.0094, paired cos 0.2730, N=743) vs floor 0.0404 CLEARED and vs the A100 baseline 0.0390 = +0.95pp; target 0.055 still open. => THE CROSS-DOMAIN RISK DID NOT MATERIALISE: unfreezing all 12 ViT blocks on 27.5K in-domain MIMIC pairs improved Indiana as well, so deep image adaptation is a genuine representation improvement, NOT an in-domain/cross-domain trade. Phase 7 gate cleared. RECONCILIATION NOTE (inverts the Phase-6 finding): authoritative 0.1714 slightly EXCEEDS the in-training final 0.168, whereas in Phase 6 authoritative 0.1113 fell ~1pp below the in-training peak 0.122. Cause: in Phase 6 the val/total_loss minimum (step 4500) sat well before the retrieval peak (~6000), so val-loss selection cost real points; with the stronger image tower both curves peak together at ~4750 and selection-on-val-loss is now free. Also visible in the checkpoint listings -- both D1b and D1c bottom at 4750 and tick up at 5000, i.e. mild late overfitting that the selection correctly avoids. HEADLINE FOR THE WRITEUP: MIMIC i2t R@10 8.23% (pre-refactor) -> 10.45% (A100 refactor) -> 17.14% (H100 + deep ViT adaptation), with the single decisive intervention being image-tower adaptation depth while ten text-side and objective-side levers were null.",
    "2026-07-26: PHASE 6G-1/6G-2 RESULTS (in-training, N=3063; authoritative evals still pending). vit_lr sweep at depth 12: 1e-6 (D1c ref) final 0.168 / peak 0.171 / val_clip 2.585 / train_clip 1.17 / cos 0.544 | 3e-6 final 0.174 / PEAK 0.183@ep9 / val_clip 2.721 / train_clip 0.702 / cos 0.540 | 1e-5 final 0.149 / peak 0.168 / val_clip 3.412 / train_clip 0.113 / cos 0.685 | 3e-5 final 0.145 / peak 0.163 / val_clip 3.487 / train_clip 0.037 / cos 0.803. => vit_lr IS AN INVERTED-U AND 1e-6 WAS ALREADY NEAR-OPTIMAL. Mechanism is unambiguous overfitting: train/clip_loss collapses monotonically with vit_lr (1.17->0.70->0.11->0.04) while val/clip_loss rises (2.585->3.49); at 3e-5 the 85M-param image tower has effectively memorised the 27570 training pairs. DEPTH WAS THE FREE LUNCH, LR IS NOT. Combined with 6D: image-tower adaptation has an OPTIMUM -- too little (depth 2 -> 0.1107) and too much (lr 3e-5) both underperform. MECHANISTIC COROBORATION FOR THE 6D-2 NULL: cos_text_teacher RISES with vit_lr (0.544->0.685->0.803), i.e. at high image-tower LR the ViT moves toward the text encoder rather than the text encoder toward the images, leaving z_text nearer its KD anchor. The two towers trade off, which is why releasing the KD anchor alone changed nothing. 6G-2 SCOPE IS A NULL: scope=all (87.2M trainable) vs blocks (86.0M) gives 0.170 vs 0.168 and val_clip 2.580 vs 2.585, inside noise -- patch_embed/cls_token/pos_embed/final norm/visual projection contribute nothing; the transformer blocks carry the entire adaptation. Keep vit_unfreeze_scope=blocks canonical. CRITICAL SUBTLETY THAT PROMOTES 6G-5 FROM FORMALITY TO NECESSITY: the vit_lr=3e-6 arm has HIGHER RETRIEVAL BUT WORSE VAL LOSS than D1c (peak 0.183 vs 0.171; val/total_loss 2.858 vs 2.721). Retrieval and val-loss have DIVERGED AGAIN -- the exact Phase-6 failure mode. Selecting on val/total_loss will not find that 0.183 peak, and selecting on retrieval against train[90%:] is selection-on-test. The disjoint selection split is the ONLY legitimate way to exploit a retrieval peak the loss does not track => 6F should become the CANONICAL protocol for further tuning. 6G-5 (clean split) still running: at ep12 i2t R@10 = 0.256 on the N=1532 SELECTION gallery -- NOT comparable to N=3063 without a gallery-size correction, but consistent with the depth effect holding. NEXT: authoritative evals on all four finished 6G arms; the in-training ordering is checkpoint-selection dependent and is NOT a final ranking.",
    "2026-07-27: PHASE 6G AUTHORITATIVE + CLEAN-SPLIT COMPLETE. MIMIC N=3063 best-by-val/total_loss: D1c (vit_lr=1e-6, blocks) 0.1714 | scopeall (lr=1e-6, all) 0.1704 | lr3e6 0.1632 | lr1e5 0.1606 | lr3e5 0.1456 | CLEANSPLIT 0.1459. (1) vit_lr=1e-6 CONFIRMED OPTIMAL -- the 0.183 in-training peak at 3e-6 was UNREACHABLE under honest selection (its best-by-val-loss ckpt evals to 0.1632, below D1c), so the 6G-1 divergence resolved against the higher LR. (2) OVERFIT ONSET MOVES EARLIER MONOTONICALLY WITH LR: best step 4750->3250->2500->1750 across 1e-6->3e-5. (3) val/total_loss IS A POOR CROSS-ARM PROXY: all five arms span only 2.6285-2.6680 (0.04 nats) while retrieval spans 2.6pp, and the LOWEST-loss arm (lr3e6) is not the best retriever. Within-arm ckpt selection on val loss is sound; across-arm selection on it picks the WRONG arm. The depth sweep's loss/retrieval agreement was fortunate, not guaranteed. (4) paired cosine RISES with vit_lr (0.4230->0.4692) while retrieval FALLS -- pulling matched pairs together is NOT the same as separating them from negatives; paired cosine must not be read as a quality proxy. (5) scope null (0.1704 vs 0.1714). CLEAN-SPLIT (6G-5): trained train[:85%] (26038), selected on train[85%:90%] (N=1532), evaluated train[90%:] (N=3063) -- MIMIC 0.1459 (still clears STRETCH 0.14), Indiana 0.0390. The 2.55pp gap to 0.1714 is 3.76 SE, i.e. REAL: it is the honest cost of test-informed checkpoint selection, confounded with 5.6% less training data (decomposable only via 6G-7). REPORT BOTH: 0.1714 is protocol-matched to every prior number in this project (all used val==test); 0.1459 is the defensible thesis headline. *** CORRECTION TO THE 2026-07-26 NOTE ***: I reported D1c's Indiana 0.0485 as '+0.95pp over the A100 baseline 0.0390, cross-domain risk did not materialise'. That OVER-READ the number. At N=743, p~0.045, SE=0.76pp, so 0.0485 vs 0.0390 is 1.25 SE -- NOT a difference -- and the clean-split run lands at exactly 0.0390. DEFENSIBLE STATEMENT: Indiana is UNCHANGED WITHIN NOISE across every variant tested (0.0390-0.0485). Deep ViT adaptation neither helped nor hurt cross-domain: no in-domain/cross-domain trade, but no gain either. Indiana remains as data-bound as the original ablations concluded, which RAISES the priority of Phase 7. NEXT: Phase 7 (multi-source CXR), optionally 6G-7 (VIT_UNFREEZE=2 SELECTION_SPLIT=true) for a clean-protocol depth-2 endpoint that would make the depth claim protocol-independent.",
    "2026-07-27: 6G-7 COMPLETE -- CLEAN-PROTOCOL DEPTH-2 ENDPOINT = 0.1081 (ckpt step 3750, MIMIC N=3063). This completes the 2x2 and changes the headline effect size. FULL GRID: vit=2 val==test 0.1107 | vit=12 val==test 0.1714 (+6.07pp, 9.7 SE) || vit=2 clean 0.1081 | vit=12 clean 0.1459 (+3.78pp, 6.2 SE). (1) THE DEPTH EFFECT IS REAL BUT ~40% SMALLER THAN THE ORIGINAL PROTOCOL IMPLIED: 3.78pp not 6.07pp. Still 6.2 SE so it holds comfortably, but 6.07pp MUST NOT be quoted as the effect size. (2) TEST-INFORMED SELECTION IS NOT A CONSTANT OFFSET -- ITS VALUE SCALES WITH OVERFITTING: 0.26pp at vit=2 (0.5 SE, i.e. nil) vs 2.55pp at vit=12 (3.8 SE). Mechanistically clear: at 14.2M trainable image params the val-loss curve is flat and checkpoint choice is near-arbitrary; at 85.1M the model overfits, the curve acquires real structure, and choosing the checkpoint with the test set is worth something. (3) DECOMPOSITION OF THE 2.55pp: both clean-split runs trained on 5.6% less data, and at vit=2 the COMBINED cost of less data + honest selection was only 0.26pp, so the data component is <=~0.3pp at both depths (assuming it does not scale with depth -- reasonable but unproven). Therefore ~2.2 of the 2.55pp at vit=12 is TEST-INFORMED SELECTION, not lost data. METHODOLOGICAL FINDING FOR THE WRITEUP: the advantage a model gains from selecting checkpoints on its evaluation split GROWS WITH HOW MUCH THAT MODEL OVERFITS -- here from nil at 14M trainable image params to 2.2pp at 85M. Rarely measured. FINAL NUMBERS: quote clean protocol as primary, MIMIC i2t R@10 10.81% -> 14.59% (+3.78pp, 6.2 SE, stretch tier cleared); protocol-matched to prior project numbers (val==test) 11.07% -> 17.14%; Indiana flat within noise throughout (0.0390-0.0485, SE 0.76pp). PHASE 6 IS NOW CLOSED. NEXT: Phase 7 multi-source data.",
    "2026-07-28: (1) PHASE 6G AUTHORITATIVE EVALS WERE ALREADY COMPLETE — no re-run needed. Checked against the record before acting: the 0.1714 is NOT an in-training number. It came from evaluate_cxr_retrieval.py on MIMIC train[90%:] N=3063 (checkbox 6G-6 'AUTHORITATIVE 6G RESULTS', all six arms, plus the 2026-07-26 note 'AUTHORITATIVE EVALS IN'). The in-training numbers for the same arm were 0.168 final / 0.171 peak, and the plan already reconciles the two at line ~252 (authoritative slightly EXCEEDS in-training because with the stronger image tower the val-loss minimum and the retrieval peak coincide at step 4750, unlike Phase 6 where val-loss selection cost ~1pp). Fields present in the 6G-6 table (paired cosine, t2i R@10, N=3063, best-step + val/total_loss per arm) are emitted by the eval script, not by the [CLIPRetrieval] in-training print. All six 6G arms have authoritative MIMIC numbers; Indiana authoritative exists for D1c (0.0485) and cleansplit (0.0390). The four non-winning vit_lr arms have no Indiana eval and do not need one — they are all worse on MIMIC. PLAN HYGIENE: 6G-5 appeared TWICE, once as a stale unchecked '[ ] ... (running)' entry contradicted by the later '[x] CLEAN-SPLIT RESULT' entry. Since CLAUDE.md makes checkboxes ground truth, the stale one is now '[x] 6G-5 (motivation; result below)'. (2) EFFICIENCY-CURVE HARNESS BUILT + BUG FIXED. scripts/performance_profile.py resolved --model through ModelRegistry, which only registers hybrid_350m/1_3b/7b/mamba_baseline/xlstm_baseline — so SIX of the NINE names in its own --model choices list (hybrid_70m, hybrid_70m_v2 via yaml, hybrid_150m*, mamba_70m_baseline, xlstm_70m_baseline, mamba/xlstm_150m_baseline), i.e. EVERY config this project actually trains including the active hybrid_150m_v2 backbone, raised ValueError before taking a single measurement. Configs now load from configs/model/*.yaml (source of truth, filtered to HybridConfig dataclass fields since the yamls carry learning_rate/warmup_steps/distill) with registry fallback. NEW --sweep mode: latency/throughput/peak-memory vs seq_length across multiple models, fitted log-log SCALING EXPONENTS (~1.0 linear = the Mamba/mLSTM claim, ~2.0 = softmax attention), CSV+JSON output, per-point peak-memory reset (else memory is attributed to the largest earlier point), OOM recorded as a row instead of killing the sweep, --backward for the training step, --dtype bf16. Second bug found in local smoke: the backward path did logits=out['logits'] but forward() returns a CausalLMOutput dataclass -> AttributeError; fixed with hasattr(out,'logits'). Sweeping past max_position_embeddings=1024 is legitimate because use_pos_embedding=False (hybrid_lm.py:43) — pinned by a test so re-enabling it fails loudly. NEW scripts/profile_efficiency_h100.sh: hybrid vs BOTH single-family baselines at identical dim/depth (only layer_pattern differs), inference + training, L=256..16384, bf16. Needs NO dataset and NO checkpoint (random ids, fresh weights) so it cannot hit the gated-MIMIC 401 class of failure. 3 new parity tests (config loading, slope-fit correctness incl. degenerate inputs, long-sequence validity). validate_for_willi.sh GREEN 9/9, 92 passed 5 skipped (was 89). CURVES NOT YET MEASURED — the numbers require the H100: sbatch scripts/profile_efficiency_h100.sh.",
    "2026-07-28: EFFICIENCY CURVES MEASURED (H100 80GB HBM3, gx07, bf16, bs=4; jobs 2382432 [150m] / 2382434 [70m]). Outputs on the SERVER at analysis/efficiency_{150m,70m}/{inference,training}/efficiency_curves.{csv,json} -- NOT yet pulled into the repo. (1) LINEAR SCALING CONFIRMED to L=16384. CAVEAT: the PRINTED full-range exponents UNDERSTATE scaling (xlstm 0.652) because the H100 is underutilised at short sequences (150M xlstm L=2048->92.0ms, L=4096->99.0ms, almost free), which drags the fit down. Asymptotic (L>=4096) fits are the honest numbers: latency hybrid 1.012 / mamba 1.011 / xlstm 0.920; memory 0.965/0.965/0.880. NOT yet implemented in performance_profile.py -- report both until it is. (2) xLSTM IS DRAMATICALLY CHEAPER THAN MAMBA: 150M @ L=16384 inference mamba 1105ms/41.99GB/59,280 tok/s | hybrid 925ms/42.00GB/70,882 | xlstm 355ms/7.12GB/184,860 -- 5.9x less memory and 3.1x faster DESPITE MORE PARAMETERS (159.0M vs 140.5M non-emb). Per-token memory 2.5282 / 2.5285 / 0.4082 GB per 1k tok. (3) THE HYBRID'S WIN IS IN TRAINING, NOT INFERENCE: inference peak = MAX over layers so hybrid == pure mamba (42.00 vs 41.99 GB); training peak = SUM of saved activations so composition matters -- 150M @ L=2048 training mamba 1348ms/67.5GB vs HYBRID 1078ms/54.0GB (25% faster, 25% less memory), xlstm 309ms/11.2GB. Same pattern at 70M (26%/23%). THIS IS THE CONCRETE DEFENSIBLE JUSTIFICATION FOR THE HYBRID OVER PURE MAMBA. (4) TRAINING MEMORY IS THE CEILING: 150M hybrid and mamba both OOM at L=4096 on 80GB, xlstm reaches 8192; exponent clean linear 0.97-0.98 => constant-factor problem, not complexity. (5) NOT A BOTTLENECK AT THIS PROJECT'S LENGTHS: CXR reports <=256 tok, PubMed <=512; at L=256 (150M) the HYBRID IS THE FASTEST of the three (18.08 vs 19.78/21.12 ms). The curves are a SEPARATE architectural contribution and do not explain any retrieval or generation number. (6) CAVEAT FOR THE WRITEUP: the '~2.0 = quadratic attention' line in the tool's own output is a REFERENCE CLAIM, NOT A MEASUREMENT -- there is no attention/transformer baseline in this repo (verified by grep over configs/model/ and hybrid_xmamba/layers/). Either add one at identical dim/depth or cite the 2.0, never claim it as measured. (7) Incidental: xlstm throughput regime change at L=4096 (150M 89,042->165,436 tok/s; 70M 132,806->246,309) = TFLA kernel efficiency jump.",
    "2026-08-16: *** OBJECTIVE PIVOT: RETRIEVAL -> MEDICAL REPORT GENERATION. *** User decision. From here a GENERATED radiology report scored against ground truth by ROUGE-L / CheXbert F1 is the optimization target. Retrieval becomes a SUPPORTING CHAPTER -- not discarded: it is a complete closed body of evidence about what does and does not move a CXR joint space, AND it remains the pretraining objective that produces the aligned image tower the generator is conditioned on. Its numbers are FINAL (clean protocol 10.81% -> 14.59%, +3.78pp 6.2 SE; protocol-matched 11.07% -> 17.14%; Indiana flat within noise). WHY IT IS DEFENSIBLE ON THE EVIDENCE ALREADY IN THIS FILE: retrieval is finished as a research question -- 10 clean nulls, 1 dominant lever (ViT adaptation depth) and that lever is EXHAUSTED (depth saturated at 12/12 blocks, vit_lr an inverted-U with 1e-6 already optimal, scope null); there is no remaining untested axis with evidence behind it. The two live constraints are both DATA not method (Indiana data-bound per 6G-4 + the 2026-07-27 correction; the image tower memorises the 27570-pair set as soon as vit_lr rises, 6G-1). And report generation exercises the causal Mamba/mLSTM decoder AS A GENERATOR, which retrieval never did -- it only used it as an encoder. PLAN RENUMBERED: NEW 7 = PhysioNet credentialing (BLOCKING) | NEW 8 = local MIMIC-CXR-JPG build | NEW 9 = retrieval on full data | NEW 10 = image-conditioned generator | NEW 11 = report-gen eval | OLD 7 (multi-source) -> 9G optional | OLD 8 (writeup) -> 12.",
    "2026-08-16 (yet even later): THIRD SMOKE-TEST ATTEMPT -- job SUBMITTED this time (job 2457309, --account=aisc) but sat PD (QOSNotAllowed) indefinitely per squeue --me. User ran sacctmgr show user $USER withassoc format=User,Account,Partition,QOS, which showed exactly two associations: account=aisc/QOS=aisc (no partition column value shown, presumably scoped to AISC partitions only) and account=default/QOS=normal. Diagnosed: cpu-batch is a GENERAL cluster partition, not an AISC one, so it needs the general QOS -- account=aisc's QOS=aisc is the wrong one for it. FIX: build_mimic_cxr_local.sh default changed --account=aisc -> --account=default. Updated the script's header comment, the parity test's assertion (now checks for --account=default, removed the now-stale --account=aisc assertion), and H100_SCALING_PLAN.md Phase 7E/7D-verify. validate_for_willi.sh GREEN 9/9, 100 passed 5 skipped. THREE separate submission-mechanics failures in a row (login-node block -> missing --account -> wrong-QOS account) before a single byte has been transferred -- none were about the actual PhysioNet credentials or .netrc, which remain unverified but likely fine. NEXT ACTION: same command, should now actually RUN (squeue --me should show ST=R, not PD): STAGE=meta OUT=/tmp/mimic_smoke_test sbatch scripts/build_mimic_cxr_local.sh -- then tail the log for a clean 200, not a 401.",
    "2026-08-16 (yet later): SECOND SMOKE-TEST ATTEMPT FAILED, different cause. STAGE=meta OUT=/tmp/mimic_smoke_test sbatch scripts/build_mimic_cxr_local.sh (after the login-node fix) was refused by sbatch itself: 'No Slurm account specified (--account). This job would use your default account, which commonly triggers submit-limit/accounting policy errors.' This cluster requires an EXPLICIT --account on every sbatch submission -- confirmed live, not covered by the docs pages fetched earlier. FIX: asked the user whether to try --account=aisc (matches every existing GPU script in this repo, same user/cluster) or check saccount first; user chose --account=aisc. Added #SBATCH --account=aisc to build_mimic_cxr_local.sh's header (was deliberately omitted before, on the theory that the general cpu-batch/cpu-interactive partitions might not need the AISC-specific allocation -- WRONG, the account requirement is orthogonal to which partition/hardware the job targets). Fixed test_build_mimic_cxr_local_slurm_wrapper_is_cpu_only_on_cpu_batch's assertion accordingly (was asserting --account=aisc is ABSENT, now asserts it IS present). validate_for_willi.sh GREEN 9/9, 100 passed 5 skipped. NEXT ACTION: same command as before, should now queue successfully -- watch logs/mimic_build_<jobid>.log for a clean run (no 401 = netrc is also fine).",
    "2026-08-16 (even later): USER RAN THE SMOKE TEST, HIT THE LOGIN NODE BLOCK. python build_mimic_cxr_local.py meta on lx01 was refused: 'This command is not allowed on the login node!' -- BEFORE any network request, so unrelated to the .netrc credentials (which the user had just correctly set up: nano ~/.netrc, chmod 600). RESOLVED via docs.sc.hpi.de (fetched live: Login-Nodes, Data-Transfer, Partitions, Run-Nodes pages) -- the login node rejects ALL script execution, stronger than the previously-known bash <script> restriction. Official path: cpu-interactive (8h) for meta/manifest/pack, cpu-batch (7-day) for fetch -- NOT a Run Node (rx01/rx02, explicitly not for data acquisition per the docs) and NOT an AISC GPU partition (zero GPU need). Built scripts/build_mimic_cxr_local.sh: STAGE env var, submits to cpu-batch, no GPU request, no --account=aisc (general cluster partition, not the AISC allocation -- user should pass --account explicitly if their site requires one). Added test_build_mimic_cxr_local_slurm_wrapper_is_cpu_only_on_cpu_batch (had to fix the assertion to check the literal '#SBATCH --gpus' directive, not the substring '--gpus' anywhere in the file -- the script's own header comment says 'Do not add --gpus' which false-positived the first version). validate_for_willi.sh GREEN 9/9, 100 passed (was 99) 5 skipped. IMPORTANT UNFINISHED COURTESY STEP surfaced by the docs: 'Always contact helpdesk before transferring large datasets' (verbatim) -- email sc-helpdesk@hpi.de before the full ~310-400GB fetch, not just the meta/manifest smoke tests. Also corrected the standing 'compute nodes are offline' characterization: that was specifically about a 401 gated-HF-dataset auth failure baked into HF_DATASETS_OFFLINE=1, not evidence of no general egress -- the docs recommending downloads FROM cpu-interactive/cpu-batch would be incoherent otherwise. NEXT ACTION: STAGE=meta OUT=/tmp/mimic_smoke_test sbatch scripts/build_mimic_cxr_local.sh, check the log, delete the test dir.",
    "2026-08-16 (later): USER CONFIRMED PhysioNet credentialing COMPLETE -- CITI training, credentialed account, and DUA signed on BOTH mimic-cxr/2.1.0 and mimic-cxr-jpg/2.1.0. Username bhushkri. Phase 7A/7B/7C marked done; 7D (netrc setup)/7E (network path)/7F (quota) remain, all local-only checks, no external dependency. IMPLEMENTED Phase 8 in full: scripts/build_mimic_cxr_local.py (4 stages, python `requests` not wget/curl -- .netrc auto-read via Session.trust_env, no subprocess/credential-in-argv); scripts/mimic_cxr_vendor/section_parser.py VENDORED VERBATIM from MIT-LCP/mimic-cxr@e8d26fff (fetched live via curl, confirmed authentic against known structure); scripts/mimic_cxr_vendor/extract.py ports the per-study findings/impression extraction logic from MIT-LCP/mimic-cxr@18cdc41c create_section_files.py (custom overrides + section_text() + last-matching-index, findings and impression kept SEPARATE per Phase 8F -- the companion CLI collapses them into one 'conclusion' column, which we deliberately do NOT do); scripts/dump_legacy_gallery_hashes.py (NEW) for the leakage-guard input, hash construction verified byte-identical to the existing normalize_report_text/text_hash conventions; configs/dataset/cxr_mimic_full.yaml (NEW); load_mimic_cxr local_parquet_dir branch in train_contrastive.py; evaluate_cxr_retrieval.py FIXED for str image paths (the crash my own 2026-08-16 review flagged the original proposal for missing) in BOTH IndianaEvalDataset and MIMICValDataset, plus local_parquet_dir/mimic_split threaded through build_dataloader/CLI/eval_h100.sh; DATASET_CONFIG env lever in train_biomedclip_kd_h100.sh (default unchanged -- Arm-0 control safe); .gitignore guards; pandas/requests/pyarrow added to requirements.txt (were transitive, now direct). VERIFICATION: validate_for_willi.sh GREEN 9/9, 99 passed (was 92) 5 skipped -- 7 new parity tests, confirmed none silently skipped. BEYOND the parity suite: manually ran `manifest` and `pack` end-to-end against synthetic PhysioNet-shaped fixtures (fake split/metadata CSVs, a real report .txt at the correct files/pXX/pSUBJECT/sSTUDY.txt path) -- manifest correctly frontal-filtered, deduped one-per-study, extracted findings/impression, computed report_hash; pack correctly joined and dropped a synthetic leaked subject, and correctly HARD-FAILED (non-zero exit, clear error) when the leakage-guard match rate (50%, by construction) was below the 95% default gate, then correctly proceeded when --min-match-frac was lowered to 0.4. NOT testable from here: stage_meta/stage_fetch need a live PhysioNet connection -- that is the actual next action, gated on 7D (netrc) + 7E (find a host with both egress and multi-hour process survival).",
    "2026-08-16: REVIEWED the proposed full-MIMIC data solution + build_mimic_cxr_local.py. VERDICT: THE STRATEGY IS CORRECT AND ADOPTED; four factual/code errors and several unstated risks must be fixed first. CORRECT AND VERIFIED AGAINST THE CODE: (a) MIMIC-CXR-JPG (~570 GB, 377,110 JPGs) not DICOM (4.7 TB) -- right project, no pydicom, no windowing to defend in a viva; (b) downscale-in-flight with originals deleted => ~310-400 GB TRANSFERRED, ~6 GB KEPT, peak disk ~4 GB regardless of corpus size -- fits the 200 GB quota easily; (c) frontal-only one-per-study -- right, the report is study-level and pairing laterals duplicates the text side (and would manufacture the in-batch false negatives 6C-3 measured this dataset does NOT currently have, 0.58 pairs/bs=64 batch); (d) LEAKAGE IS THE #1 RISK and evaluate_cxr_retrieval.py:362 does hardcode split='train[90%:]' on MIMIC_REPO -- CONFIRMED; (e) subject-level not study-level exclusion -- right, patients recur with near-identical reports; (f) move the primary metric to the official subject-disjoint split -- right, and MANDATORY for report generation since that is what published RRG numbers use; (g) the itsanmolgupta mirror's provenance genuinely cannot be stated and it is a third-party redistribution of credentialed data -- a real viva vulnerability and a real DUA concern; (h) ZERO IMAGE AUGMENTATION ANYWHERE -- VERIFIED, all four transform sites (train_contrastive.py:186,300,368 + evaluate_cxr_retrieval.py:250) are Resize -> [Grayscale(3)] -> ToTensor -> Normalize; (i) MIMICJointDataset.__getitem__ needs ZERO changes -- VERIFIED at :406-408; (j) store at 320 not 224 for crop headroom -- right, +3 GB; (k) Arm 0 reproduction control first -- right and matches this project's discipline; (l) normalize_report_text (eval:414) and blake2b (train_contrastive:424) already exist and are directly joinable -- VERIFIED. WRONG OR MISSING: (1) 'same DUA and CITI training you already have; it's a click' is FALSE -- this project has NEVER been credentialed (note 2026-07-25) and realistic lead time is 1-4 weeks dominated by human review of the supervisor reference. This is now Phase 7 and it BLOCKS EVERYTHING. (2) --cut-dirs=4 is OFF BY ONE, should be 3: after -nH the path is files/mimic-cxr-jpg/2.1.0/files/pXX/... so cutting 4 strips the SECOND files/ too, images land at pXX/... while the resize step looks for files/pXX/... => every chunk converts 0 images AND THE RUN REPORTS SUCCESS. (3) wget --base= with a plain-text -i list is version-dependent (documented for HTML input via --force-html) -- write ABSOLUTE URLs. (4) -N with -c makes wget warn and disable timestamping -- drop -N. (5) check=False swallows a total 401: indistinguishable from tolerated 404s, so the run would spin through ~100 chunks downloading nothing -- ASSERT that a chunk converting 0 of N aborts. (6) .netrc over HTTPS in wget is version-dependent -- prefer curl --netrc or wget --user/--password from ENV (never argv; it leaks into ps and SLURM logs). (7) The homegrown ^[A-Z ]+: SECTION_RE UNDERMINES THE VERY PROVENANCE ARGUMENT the proposal makes -- vendor MIT-LCP mimic-code/mimic-cxr/txt/section_parser.py from the start and record its git SHA. (8) THE HASH-JOIN LEAKAGE GUARD WILL SILENTLY UNDER-MATCH: the legacy gallery came from a mirror with an unknown section parser, so hashes built by OUR parser will not reproduce theirs, and every miss is a LEAKED SUBJECT. MANDATORY: report matched/3063, treat <95% as a FAIL, and in that case drop the legacy gallery entirely. (9) THE PROPOSAL MISSED that evaluate_cxr_retrieval.py needs an image-PATH branch, not just a repo branch: both eval datasets (:284-291, :331-337) do `if not isinstance(img, Image.Image): Image.fromarray(img)` which CRASHES on a str path. (Storing 'L' grayscale is safe in both paths -- verified.) (10) ~210k pairs SHRINKS after the text filter: ~155-160k studies have FINDINGS, ~190k+ have FINDINGS or IMPRESSION. The build must emit BOTH counts and the findings-only-vs-both decision must be explicit. (11) THE OVERFITTING FRAMING NEEDS NARROWING against this plan's own record: optimization-side overfitting is NOT binding here (epochs 23->14 = +0.06pp; lower LR removed the rollover without changing plateau height). What 6G-1 measured is that the IMAGE TOWER SPECIFICALLY memorises. So the testable claim is 'more data relaxes the image-adaptation dose constraint', not 'more data fixes overfitting'. Arm 2's pre-registered prediction is exactly the right test of that narrow claim. (12) UNSTATED OPERATIONAL RISKS: inodes (~430k files may trip an inode cap before the byte cap); and WHERE the fetch runs -- compute nodes are offline (HF_*_OFFLINE=1 everywhere) and the login node refuses `bash <script>`, so a multi-hour download needs a verified egress host that will not reap the process. Both are now Phase 7E/7F, to be answered BEFORE the build. (13) BIGGEST OMISSION: THE PROPOSAL DOES NOT MENTION THE REPORT-GENERATION OBJECTIVE AT ALL. VERIFIED ARCHITECTURE GAP: HybridLanguageModel.forward() (hybrid_lm.py:147) takes ONLY input_ids -- no inputs_embeds, no encoder_hidden_states, no cross-attention; generate() (:230) likewise; and there is NO rouge/chexbert code anywhere in the repo. There is currently no way to condition the decoder on an image. That is Phase 10 and it is the largest piece of new work in the pivot.",
    "2026-08-19: Phase 9A-prep COMPLETE end-to-end. Arm-0 subset fetch (job 2462107) FINISHED: 16940 images at 320px downloaded; arm0-relevant subset fully present in the shared mimic_full/files tree. Arm-0 pack (job 2468317) SUCCEEDED on gx17v1: 68696 rows with both text+image so far, 20268/26968 arm0 hashes matched (~75%, partial-fetch fine -- pack only counts rows whose local_jpg exists); wrote arm0_train.parquet (19881 pairs / 14471 subjects), arm0_validate.parquet (161), arm0_test.parquet (226) into mimic_full/ via --out-prefix. First pack attempt (job 2468301) FAILED -- NOT a code bug: scheduler landed it on node ga03, a DIFFERENT CPU ARCH (ARM/Grace) than the gx-series x86 nodes the .venv was built on -> '.venv/bin/python: cannot execute binary file: Exec format error', instant fault before importing anything, nothing corrupted. ga03 was already blacklisted in all three GPU scripts (eval_h100.sh, train_biomedclip_kd_h100.sh, train_biomedclip_kd_150m_h100.sh); build_mimic_cxr_local.sh just never carried the line. FIX: added '#SBATCH --exclude=ga03' to build_mimic_cxr_local.sh (validate_for_willi.sh green, 9/9 gates, 108 passed). Main fetch (job 2461245) still Running on gx17v1, ~12.8% (23424 images) and now uncontended (arm0 done) so the ConnectTimeout walls should subside. NEXT: launch Arm-0 D1c reproduction run against the arm0_ parquets. Loader hardcodes train/validate/test.parquet filenames, so point it via a symlink dir (arm0_train.parquet->train.parquet etc.) + dataset.local_parquet_dir override, DATASET_CONFIG=cxr_mimic_full, VIT_UNFREEZE=12, SELECTION_SPLIT=true (clean 85/5/10 split of arm0_train, the honest 0.1459 protocol). Gate: reproduce 0.1459 +/-1.1pp. Confirm with user before launching (billed GPU run).",
    "2026-08-20: Arm-0 launch (job 2470516, 150M/gx09) failed at dataloader-prep: 'ValueError: Unknown dataset for contrastive training: cxr_mimic_arm0'. ROOT CAUSE (not Arm-0-specific -- pre-existing bug in cxr_mimic_full.yaml, inherited when arm0 was copied from it): scripts/train_contrastive.py prepare_dataloader() has an OUTER dispatch (`if name == 'mimic_cxr': load_mimic_cxr(...)  else: raise ValueError`) that runs BEFORE load_mimic_cxr ever inspects local_parquet_dir -- dataset_name is a DISPATCH KEY, not a descriptive label, and must be the literal 'mimic_cxr' regardless of which yaml file supplies it. cxr_mimic_full.yaml had dataset_name:'cxr_mimic_full' (its own filename) since creation; never caught because Phase 8 only built data, this Arm-0 launch was the FIRST time either config was ever run through actual training. The existing parity test (test_load_mimic_cxr_dispatches_to_local_parquet_when_configured) only exercised load_mimic_cxr directly, bypassing this exact outer gate. FIX: dataset_name -> 'mimic_cxr' in both cxr_mimic_full.yaml and cxr_mimic_arm0.yaml (now identical fields, differ ONLY in local_parquet_dir). Updated both dataset-config parity tests + added a new regression test (test_prepare_dataloader_dispatches_local_parquet_configs_to_load_mimic_cxr) that calls prepare_dataloader itself with a fake load_mimic_cxr, proving dataset_name='mimic_cxr' reaches it -- the exact path job 2470516 hit. validate_for_willi.sh green, 9/9 gates, 110 passed. NEXT: git pull on cluster (config fix not yet on cluster), then relaunch: DATASET_CONFIG=cxr_mimic_arm0 VIT_UNFREEZE=12 SELECTION_SPLIT=true sbatch scripts/train_biomedclip_kd_150m_h100.sh. Main fetch (job 2461245) still running on gx17v1, unaffected throughout.",
    "2026-08-20: Arm-0 150M smoke test COMPLETED CLEAN (job 2471261, gx09, ~3.5h wall). DATASET_CONFIG=cxr_mimic_arm0 VIT_UNFREEZE=12 SELECTION_SPLIT=true on hybrid_150m_v2 + arm0 subset (16899 train / 994 select / 226 test). 6000 steps / 22 epochs, no crashes. In-training i2t R@10 on the 994-pair selection split: 0.030 -> peak 0.227 (epoch 17), with the expected freeze->unfreeze jump exactly at step 1000 (0.055->0.121 same epoch). train/cos_text_teacher settled 0.516-0.517 (matches the ~0.57 equilibrium band from the original Phase-6 runs). No flatlining/divergence -- qualitatively a healthy, non-corrupted dataset (images decode, reports parse, image-report pairs correctly aligned). NOT a 0.1459 reproduction -- two axes differ from that number simultaneously: backbone (150M at vit_unfreeze=12 was never run historically; 150M only tested at canonical vit_unfreeze=2, plateaued ~0.116) and data volume (arm0's 16899 vs legacy's 26038 train pairs). Also this is the in-training metric on the SELECTION split, not an authoritative held-out eval with best-by-val-loss checkpoint selection. Logged as new checkbox '9A-smoke (150M)' in the plan, distinct from and NOT satisfying the literal 9A gate (still open, blocked on the missing 70M Stage-0 checkpoint). Net: strong qualitative pipeline health signal on the backbone actually being carried forward (per the generation pivot); the strict numeric 70M reproduction remains undone and is a user decision (recover 70M ckpt vs accept this smoke test as sufficient). Main fetch (job 2461245) still running on gx17v1 throughout, unaffected.",
    "2026-08-20: USER DECISION -- proceed on the 150M backbone toward Phase 10 (image-conditioned report generator), accepting the 9A-smoke pipeline-health result as sufficient rather than pursuing the strict 70M Arm-0 numeric reproduction (0.1459 gate). Phase 9's remaining full-data retrieval attribution arms (9B data-only, 9C vit_lr sweep) are therefore DEFERRED, not run -- not needed to unblock Phase 10, and CLAUDE.md already treats retrieval as a closed supporting chapter; this just formalizes not re-opening it further right now. current_phase moved phase8_mimic_full_local_build -> phase10_image_conditioned_generator. Phase 8's main fetch (job 2461245) continues running unaffected in the background and should still be left to finish (needed for the eventual production retrieval/eval tables in Phase 12, and Phase 10D/10G want the full report corpus). Phase 10A (additive inputs_embeds/prefix_embeds on HybridLanguageModel.forward()/generate(), bit-identical-when-None parity test) is the first concrete unblocked step -- needs no data, no GPU. Given it changes a signature every existing Stage-0/contrastive/eval path depends on, drafting a proper implementation plan before touching hybrid_lm.py rather than improvising the diff.",
    "2026-08-20: Phase 10A + 10B-architecture IMPLEMENTED per the approved plan (/Users/krish/.claude/plans/pure-hatching-stallman.md). hybrid_lm.py forward()/generate() gained additive inputs_embeds/prefix_embeds kwargs (default paths untouched, verified bit-identical by parity tests); new hybrid_xmamba/models/prefix_mapper.py::ImagePrefixMapper (attention-free, adaptive_avg_pool1d 197->k). 5 new tests in tests/test_willi_parity.py. validate_for_willi.sh green: 115 passed (was 110), 9/9 gates. H100_SCALING_PLAN.md checkboxes 10A and 10B-architecture ticked; 10B(training)/10C/10D/10E/10F remain open, blocked on the Phase 8 fetch finishing (job 2461245, still running) and GPU access -- correctly out of scope for this CPU-only architecture step. HybridTextEncoder deliberately left untouched (retrieval-era, closed chapter; Phase 10's generator will get its own training script per 10E, never routed through it). Committed and pushed (e792517, e102bfd).",
    "2026-08-20: Phase 11A IMPLEMENTED (2nd unblocked-work item, per user go-ahead 'lets work on the process unblocked work'). New scripts/evaluate_report_generation.py: rouge_l_score + corpus_bleu are pure Python (LCS-based ROUGE-L F, brevity-penalty BLEU-N, no new runtime dep); meteor_score_corpus is nltk-backed and returns None (not 0.0, so callers can distinguish 'not computed' from 'scored zero') when the wordnet corpus isn't staged locally -- compute nodes have no internet, same constraint already noted for 11B's CheXbert weights. Decoding harness: greedy_decode() is a thin wrapper around Phase 10A's generate(prefix_embeds=..., top_k=1) (reuses already-tested code); beam_search_decode(beam_size=3) is genuinely new logic built directly on forward(inputs_embeds=...) since generate() has no beam mode -- pinned by a beam_size=1-equals-greedy_decode correctness invariant test. --smoke-test CLI mode proves both decoders run end-to-end on a tiny random-init model + ImagePrefixMapper, no checkpoint or MIMIC data needed; --hyp-file/--ref-file CLI mode computes the metrics over any precomputed pairs, ready for 11C's retrieval-NN baseline once that script exists. Added nltk>=3.8 to requirements.txt (METEOR-only; guarded, no hard runtime dependency). 6 new parity tests. validate_for_willi.sh green: 121 passed (was 115), 9/9 gates. Manually verified both CLI modes end-to-end via `conda run -n willi_parity` (this machine has no local .venv) -- smoke-test ran clean, and a synthetic hyp/ref pair produced ROUGE-L=0.811, BLEU-1=0.772, BLEU-4=0.638, METEOR correctly reported as skipped. Still open: 11B (CheXbert F1), 11C (retrieval-NN baseline), 11D (real checkpoint + official test-split generation -- needs Phase 8 data + a trained Phase 10 generator), 11E (qualitative appendix). Main fetch (job 2461245) continues unaffected in the background; last checked ~23.4%/43423 images converted (2026-08-20 16:39, before this Phase 11A work).",
    "2026-08-20: Phase 10E INFRASTRUCTURE built (3rd unblocked-work item, same session, user asked 'proceed further on the tasks... unblocked or require no data or no checkpoint'). Also answered user's question on what checkpoint is needed: 10C needs the Phase-9 best CONTRASTIVE checkpoint (image tower) -- Phase 9 was deferred, so the best candidate on hand is the Arm-0 150M smoke-test checkpoint at outputs/h100_kd_150m_v2_bs64_head4.24e-4/checkpoints/ (job 2471261), not a formal Phase-9 winner; 10D needs the Phase-5 STAGE-0 LM checkpoint (val PPL 13.18) for decoder init, on the 150M side per the pivot -- exact current path still needs confirming when 10D is actually executed. Research subagent (ac0207554c2fbd838) mapped train_contrastive.py's ImageTextDataset/load_mimic_cxr, JointMultiTaskLightningModule's CLIP-loading + configure_optimizers param-group patterns, confirmed hybrid_lm.py's nn.CrossEntropyLoss() has no explicit ignore_index arg (relies on PyTorch's -100 default -- not previously exploited anywhere in the codebase), and confirmed NO existing code path calls image_encoder.trunk.forward_features() for the pre-pooling patch grid (only the pooled 512-d CLIP output is used anywhere) -- this had to be written fresh. Built: configs/model/hybrid_150m_v2_rrg.yaml; new ReportGenerationLightningModule (hybrid_xmamba/training/lightning_module.py) with lazy load_image_encoder() so it's CPU-unit-testable via a precomputed batch['patch_grid'] tensor with zero open_clip/BiomedCLIP dependency; scripts/train_report_generation.py (imports load_mimic_cxr from train_contrastive.py directly rather than duplicating it, deliberately bypassing prepare_dataloader's dataset_name=='mimic_cxr' dispatch-string requirement); scripts/train_report_generation_h100.sh (mirrors train_biomedclip_kd_h100.sh conventions, fails fast on missing DECODER_CKPT). Caught and fixed two bugs before landing: (1) an initial version of _step()'s train/lr logging called self.trainer.optimizers[0] unconditionally, which raises RuntimeError (not a None return) when no Trainer is attached -- broke calling training_step() directly in a unit test; wrapped in try/except RuntimeError. (2) the original _step() only masked the k image-prefix positions with -100 and forgot that report tokens are ALSO right-padded to a fixed max_length (ImageTextDataset) -- pad positions needed masking too, via batch['attention_mask'], or a real training run would train on pad-token targets. 4 new parity tests incl. a manual-CE regression pin proving the -100-default-ignore_index assumption holds today. validate_for_willi.sh: 125 passed (was 123), 9/9 gates green. Manually verified: Hydra compose of model=hybrid_150m_v2_rrg dataset=cxr_mimic_full resolves prefix_k=32/image_patch_dim=768 correctly; train_report_generation.py's AST parses and its top-level imports succeed. Explicitly still NOT runnable (by design, matches 10E's plan-of-record scope): needs Phase 8 data (fetch job 2461245 still running) + a Phase 10D decoder checkpoint, neither of which exist yet. 10C/10D/10F remain fully open.",
    "2026-08-23: USER PASTED main-fetch log (job 2461245) -- progress at time of paste: ~56% (converted so far ~105-122k of 218131 target), _stage scratch dir only 3.1G (expected -- images are resized/kept small, raw streamed bytes are discarded per design). User worried this is 'only half done and will take 7+ more days'. ASSESSED: this pace is actually IN LINE with the script's own original 9-day extrapolation (comment in build_mimic_cxr_local.sh, measured live from job 2457894's first chunk) -- not badly behind. The dominant visual noise in the log (huge walls of ConnectTimeoutError retries) is EXPECTED PhysioNet-side throttling under sustained load, already investigated and NOT a new problem -- concurrency was already tested higher (WORKERS effectively 16 during the brief arm0+main overlap on 2026-08-17) and produced MORE timeouts, not more throughput, so raising WORKERS further is NOT recommended; this was already tried and rejected, see the 2026-08-17 notes above. TIME-LIMIT RISK FLAGGED (the actually actionable finding): job 2461245 was submitted 2026-08-17 under --time=6-12:00:00 with NO --requeue in the sbatch header -- it will hit that wall-clock cap around 2026-08-24, i.e. imminently, and will NOT auto-resubmit when killed. USER MUST watch `squeue -u krishankumar.bhushan` and, the moment the job ends (TIME_LIMIT or otherwise), immediately resubmit the IDENTICAL command: `STAGE=fetch OUT=/sc/home/krishankumar.bhushan/dataset/mimic_full sbatch scripts/build_mimic_cxr_local.sh` -- fully resumable (todo recomputed from which local_jpg files already exist on disk), so a gap here costs nothing but idle wall-clock, not correctness. SEPARATE, HIGHER-LEVERAGE RECOMMENDATION: no need to wait on the main fetch AT ALL to start validating Phase 10E -- the Arm-0 subset fetch (job 2462107, COMPLETED 2026-08-19) is already fully packed at mimic_full/arm0_train.parquet (19881 pairs) / arm0_validate.parquet (161) / arm0_test.parquet (226), real complete image+report pairs sitting on disk right now. Recommended NEXT ACTION: point DATASET_CONFIG at those arm0_ parquets (same symlink-dir trick already used for the Arm-0 retrieval run, i.e. arm0_train.parquet->train.parquet etc. + dataset.local_parquet_dir override) and launch scripts/train_report_generation.py / train_report_generation_h100.sh against them as the FIRST-EVER real execution of the Phase 10E pipeline (built 2026-08-20, never yet run against real data) -- this catches dataloader/tokenizer/ImagePrefixMapper/checkpoint-loading integration bugs now instead of after the full ~10-12 day fetch finishes. Needs DECODER_CKPT resolved first (10D, the Phase-5 Stage-0 150M checkpoint, val PPL 13.18 -- exact path on the cluster still unconfirmed per the 2026-08-20 note above; check ./outputs/h100_stage0_150m_v2/checkpoints/stage0_model_only.pt or equivalent). No code changes made this turn -- pure status assessment + next-action guidance.",
    "2026-08-23: FIRST-EVER LIVE EXECUTION of Phase 10E, two bugs found and fixed back to back, both the SAME root cause: Hydra's strict-struct mode rejects a CLI override for any key not declared SOMEWHERE in the composed config -- caught only at Hydra-compose time inside the SLURM job, after the DECODER_CKPT existence check already passed. BUG 1 (job 2478622): `model.vit_unfreeze_blocks=${VIT_UNFREEZE}` / `model.vit_lr=${VIT_LR}` in train_report_generation_h100.sh -- hybrid_150m_v2_rrg.yaml never declared either key (only image_patch_dim/prefix_k were added for Phase 10B; vit_unfreeze_blocks/vit_lr were assumed covered by train_report_generation.py's cfg.model.get(...) Python-side defaults, which only apply to READS, not to Hydra's CLI-override WRITE path, which resolves before any Python runs). FIX: added both keys to hybrid_150m_v2_rrg.yaml (vit_unfreeze_blocks: 0, vit_lr: 1.0e-6, matching 10C's frozen-tower default). BUG 2 (job 2478635, next resubmit): `decoder_checkpoint=\"${DECODER_CKPT}\"` -- configs/config.yaml (the base config train_report_generation.py's @hydra.main(config_name=\"config\") actually uses) never declared decoder_checkpoint at all; train_contrastive.py's equivalent lm_checkpoint works fine in production ONLY because it uses --config-name config_70m, a DIFFERENT base file that happens to declare lm_checkpoint -- config.yaml itself doesn't. FIX: added `decoder_checkpoint: null` to configs/config.yaml. REGRESSION TEST (added after bug 2, catches BOTH bugs and any future recurrence in one shot): test_train_report_generation_h100_slurm_wrapper_hydra_overrides_compose extracts the SLURM wrapper's actual python invocation block verbatim via regex, substitutes each ${VAR} with a literal placeholder (deliberately NOT parsed from the script's own defaults -- EXPERIMENT nests ${PREFIX_K}, several lines carry trailing inline comments, both make robust default-extraction more fragile than just supplying flat test literals), and replays the exact override list through hydra.compose() -- reproducing the live failure mode offline. Extended test_hybrid_150m_v2_rrg_config_matches_150m_v2_architecture too, with a lighter-weight per-key existence check (every model.<key>= token in the wrapper must have a matching declared key in the yaml) as a second, independent line of defense. validate_for_willi.sh green: 126 passed (was 125; bug-1 fix + bug-1 test added 125, bug-2 fix + bug-2 test added 126 -- test count math: +1 net since bug-1's fix only extended an existing test, bug-2's fix added one new test), 9/9 gates, both times. Committed and pushed as two commits (fad5559 for bug 1; bug 2 not yet committed as of writing this note -- see next commit). NEXT ACTION FOR THE USER: git pull, then relaunch the exact same smoke command from the previous turn -- `DATASET_CONFIG=cxr_mimic_arm0 MAX_STEPS=50 BATCH_SIZE=16 EXPERIMENT=h100_report_gen_arm0_smoke sbatch scripts/train_report_generation_h100.sh` -- both known Hydra-override gaps are now closed; if a third one surfaces (the pattern suggests it's plausible -- this is genuinely the first live run of a script built entirely from static analysis), it will be the same class of bug (an undeclared key somewhere in the override chain) and the fix is the same shape (declare it, extend the compose-regression test).",
    "2026-08-23: SMOKE RUN SUCCEEDED CLEAN (job 2478641, gx10, 17s wall for 50 steps): both Hydra-override fixes (fad5559, 37ca523) confirmed correct in production, not just via the offline compose test. Decoder checkpoint loaded with 0 missing / 0 unexpected keys (perfect match against stage0_model_only.pt). BiomedCLIP image encoder loaded fine (frozen, 86.2M params, eval mode). Dataloaders built against the arm0 parquets: 19881 train / 161 validation samples, all images present (no None-image filtering losses). Model summary: decoder 183M + prefix_mapper 1.2M trainable, image_encoder 86.2M frozen, 271M total -- matches the architecture exactly. Trained 50/50 steps in 17s (~3/s), train/lm_loss_step 5.776, epoch-mean 6.022 -- finite, no NaN/inf, no crash. This is the FIRST-EVER successful end-to-end execution of the Phase 10E pipeline against real data, closing out the 'genuinely untested' risk flagged when it was built 2026-08-20. Checkpoint saved to outputs/h100_report_gen_arm0_smoke/checkpoints/. CAVEAT: 50 steps is far too few to read anything into the loss trajectory itself (warmup_steps default is 500, not yet completed) -- this run validates PLUMBING, not model quality. Local tooling note: this same session hit the documented EPERM read/edit lockout (see the historical plan-blocker entry) immediately after this success was reported -- repo-wide, writes/touches unaffected, resolved itself (user confirmed 'permission is restored') without a session restart this time; this note was written once access returned. NEXT ACTION: launch the real arm0 run at full defaults (10k steps) to get an actual (if data-limited, non-final) report-generation checkpoint: `DATASET_CONFIG=cxr_mimic_arm0 EXPERIMENT=h100_report_gen_arm0 sbatch scripts/train_report_generation_h100.sh`. NOTE FOR LATER: arm0's own test.parquet (226 pairs) must NOT be used for reportable 11D numbers -- it is the legacy-training-hash subset's own carved split, not the official subject-disjoint mimic-cxr-2.0.0-split.csv.gz held-out set (same distinction as the retrieval-era cross_protocol_number_comparison warning) -- fine for qualitative spot-checks (11E) and pipeline confidence, not for a final table.",
    "2026-08-23: FULL ARM0 RUN COMPLETE (job 2478647, gx08, 10000/10000 steps, wall 17:31:25->19:22:55 ~= 1h52m, ~1.6 it/s, bs=32 default). Same clean-load signature as the smoke run (0 missing/0 unexpected decoder keys). train/lm_loss: ~5.7-6.0 at step 50 (smoke) -> 0.465 step-level / 0.489 epoch-mean at step 10000 (16 epochs over 19881 train pairs, since drop_last on train -> 621 steps/epoch, 16*621+64=10000 exactly, matching the log's 'Epoch 16 ... 64/64'). No NaN/inf, no crash, no OOM at default bs=32/no grad-ckpt on 80GB. ASSESSED, NOT YET CONFIRMED: a CE loss this low after 16 full epochs on only ~20k examples with a 184.9M-param trainable decoder+prefix_mapper, ZERO image augmentation (same gap 9D flagged for retrieval's ViT -- 6G-1 measured outright memorization at just 85M trainable image params on the SAME arm0-sized pool), and a fairly aggressive head_lr=3e-4 for the newly-initialized prefix_mapper is CONSISTENT WITH overfitting/memorization -- but is also plausible on its own given MIMIC reports are heavily templated (6C-3: 2% exact-duplicate reports; loss on predictable boilerplate tokens goes near-zero regardless of whether the model generalizes on the CONTENT tokens). The train loss number alone cannot distinguish these two explanations. val/lm_loss was never visible in the static log tail (trainer.val_check_interval=250 should have triggered ~40 validation passes over 10k steps) -- almost certainly a rich-progress-bar-in-non-tty artifact (only the final frame survives a plain `cat` of a non-interactive SLURM log), NOT evidence validation didn't run; the job would have crashed loudly on a real validation-step error rather than finishing cleanly with 'Trainer.fit stopped: max_steps=10000 reached'. GROUND TRUTH IS SITTING IN THE CHECKPOINT FILENAMES (not yet read this turn): ModelCheckpoint(monitor='val/lm_loss', save_top_k=3, filename='report_gen-{step:06d}-{val/lm_loss:.4f}') bakes the actual val loss into each of the 3 saved filenames under outputs/h100_report_gen_arm0/checkpoints/ (plus last.ckpt, unlabeled). NEW TOOLING BUILT THIS TURN (commit 267b0bd) specifically to resolve this ambiguity beyond just the val-loss number: scripts/evaluate_report_generation.py gained --checkpoint mode (load_report_generation_module + generate_from_patch_grid, CPU-unit-tested via test_generate_from_patch_grid_runs_and_returns_expected_shape for both greedy/beam) -- generates real text from real held-out arm0 images and prints it next to the ground-truth report, the decisive qualitative check (content-specific reports on unseen images = real learning; identical boilerplate regardless of image = memorization/mode collapse). validate_for_willi.sh: 128 passed (was 126), 9/9 gates green. NEXT ACTION FOR THE USER: (1) `ls -la outputs/h100_report_gen_arm0/checkpoints/` and paste back the filenames (val loss is literally in the name); (2) `git pull` then run the new inspection tool against a few held-out VALIDATION images (not train -- train images would look artificially good even under genuine overfitting): `python scripts/evaluate_report_generation.py --checkpoint outputs/h100_report_gen_arm0/checkpoints/last.ckpt --parquet /sc/home/krishankumar.bhushan/dataset/mimic_full/arm0/validate.parquet --num-samples 10 --decode greedy` (run this ON A COMPUTE NODE with GPU access, e.g. via srun/an interactive job, or add a small one-off sbatch wrapper -- it needs the same BiomedCLIP HF-hub weights + CUDA the training job used). Main Phase 8 fetch (job 2461245) still running in background throughout, unaffected -- was approaching its 6-day-12-hour --time cap around now/imminently per the 2026-08-23 earlier note; worth a `squeue -u krishankumar.bhushan` check alongside this.",
    "2026-08-23: USER'S ls CONFIRMED THE FILENAME BUG PREDICTED ABOVE, LIVE: `report_gen-step=000250-val` etc. are DIRECTORIES (drwxrwx---), not files -- Lightning's filename='report_gen-{step:06d}-{val/lm_loss:.4f}' does NOT sanitize the '/' inside 'val/lm_loss', so it created a nested dir per checkpoint instead of a flat file (the real .ckpt is one level deeper, named lm_loss=X.XXXX.ckpt, still recoverable by ls-ing into any of those dirs -- no data lost, just misleadingly presented). FIXED (commit 5f6f2f0): ReportGenerationLightningModule._step() now also logs a flat-named 'val_lm_loss_ckpt' alias (val-only; monitor='val/lm_loss' for best-k SELECTION is untouched, that's a plain dict-key lookup with no filesystem meaning); train_report_generation.py's filename= template interpolates that flat alias instead. SAME LATENT PATTERN CONFIRMED to also exist in train_stage0_distill.py/_resume.py ({val/loss:.4f}) via grep -- documented, deliberately NOT fixed (historical, already-executed production scripts; the Stage-0 150M checkpoint this whole Phase 10E chain's DECODER_CKPT came from lives there, not touching it without a reason). 2 new regression tests (behavioral: mocks module.log and asserts val_lm_loss_ckpt is logged on val steps only; static: greps train_report_generation.py's filename= template for any remaining '/' inside {} braces). SEPARATE ISSUE, same message: user's --checkpoint mode invocation was run directly on the LOGIN NODE (lx01) and correctly refused ('This command is not allowed on the login node!') -- same constraint documented in build_mimic_cxr_local.sh's header, just not yet hit by this new script. FIX: new scripts/inspect_report_generation_h100.sh (SLURM wrapper, mirrors train_report_generation_h100.sh's conventions -- aisc-batch/account/qos/exclude=ga03,gx17v1,gx13v1/offline-HF env -- but lightweight: --time=00:30:00, --mem=32G, no --requeue, since this is a handful of forward passes not a training run). Defaults CHECKPOINT to last.ckpt and PARQUET to arm0/validate.parquet (not train -- generations on train images look artificially good even under genuine overfitting). validate_for_willi.sh: 131 passed (was 128), 9/9 gates green. Bug during test-writing (self-caught, not user-facing): a copy-paste edit left a stray `assert torch.isfinite(out.float()).all()` line orphaned inside the WRONG test function (NameError: 'out' not defined) -- caught immediately by running the targeted tests before the full harness, fixed before commit. NEXT ACTION FOR THE USER: git pull, then `sbatch scripts/inspect_report_generation_h100.sh` (all defaults already point at job 2478647's last.ckpt + arm0 validation images) -- this is THE decisive check for whether the 0.489 epoch-mean train loss reflects real learning or memorization. Main Phase 8 fetch (job 2461245) status not re-checked this turn -- still worth a squeue look, was approaching its 6d12h cap around 2026-08-24.",
    "2026-08-24: --checkpoint mode inspection (job 2482264) CONFIRMED MEMORIZATION, not learning: samples 4/7/9 (studies 50523025/50835299/50937713, three different patients) generated BYTE-IDENTICAL text; fine clinical content (PICC positions, effusions, 'left costophrenic angle not fully included', specific impressions) never reproduced -- only generic boilerplate ('lungs clear', 'no acute process') plus, occasionally, a very visually obvious device (tube/line) survives. Repetition loops present in 7/10 samples ('No displaced fractures identified' x6 etc) -- a separate greedy-decoding degeneration symptom on top of the memorization. Aggregate rouge_l=0.285/bleu_1=0.325/bleu_4=0.121 look respectable in isolation but are almost certainly inflated by 'copying the dominant template scores okay against a templated corpus' -- exactly why 11C's retrieval-NN baseline must run before any generator number is trusted (per H100_SCALING_PLAN.md's own 11C entry). SEPARATE GOOD NEWS same message: user resubmitted the main fetch (job 2482266, since 2461245 hit its wall-clock cap as predicted) -- '218131 target / 60534 still to do' = 72.3% done, up sharply from ~56% two days prior. USER DECISION (via AskUserQuestion, 3 options offered: augment+full-data together / full-data-first-isolate-variable / augment-now-rerun-arm0-first) -- chose 'add augmentation now, rerun on arm0 first': cheapest/fastest signal, confirms the lever helps before committing the full-data GPU run to it. IMPLEMENTED (commit e3682d0): new build_image_transform(cfg, is_train) in train_contrastive.py, shared by ImageTextDataset AND MIMICJointDataset (both previously had byte-identical hardcoded Resize->Grayscale->ToTensor->Normalize pipelines, now both route through this one helper) -- RandomResizedCrop(scale=0.8-1.0) + RandomRotation(7deg) applied ONLY when cfg.dataset.use_augmentation (new key, declared false in both cxr_mimic_arm0.yaml/cxr_mimic_full.yaml so retrieval's closed chapter is UNCHANGED unless a future arm explicitly opts in) AND is_train (never val/test -- augmenting eval would make it nondeterministic regardless of the training question). load_mimic_cxr() now threads is_train=(split=='train'). train_report_generation_h100.sh gained an AUGMENT env lever (default false) -> dataset.use_augmentation=${AUGMENT}; the existing hydra-compose regression test (test_train_report_generation_h100_slurm_wrapper_hydra_overrides_compose) picked up the new override token automatically and required env_defaults['AUGMENT'] be added -- exactly the safety net it was built for. 5 new tests (augmentation on/off x is_train gating matrix; byte-identical-to-old-pipeline regression pin when use_augmentation is unset/False; is_train threading through load_mimic_cxr, train augmented / validation never). validate_for_willi.sh: 136 passed (was 131), 9/9 gates green. H100_SCALING_PLAN.md: 9D marked '9D-infra' [x] (lever built + applied to Phase 10E first; retrieval's own arm under this item's ORIGINAL scope remains open/deprioritized; the EXPERIMENT of confirming it fixes memorization is explicitly NOT yet done). NEXT ACTION FOR THE USER: git pull, then `AUGMENT=true EXPERIMENT=h100_report_gen_arm0_aug DATASET_CONFIG=cxr_mimic_arm0 sbatch scripts/train_report_generation_h100.sh` (same arm0 data, same 10k steps, ONLY augmentation toggled on, so this is a clean single-variable comparison against job 2478647); once done, rerun the inspection tool against the NEW checkpoint (`CHECKPOINT=outputs/h100_report_gen_arm0_aug/checkpoints/last.ckpt sbatch scripts/inspect_report_generation_h100.sh`) and compare side by side against this turn's samples for whether different images now produce genuinely different text. Also worth a squeue check on job 2482266 (72.3% done) periodically -- same no-auto-resubmit caveat as before if it hits its own wall-clock cap.",
    "2026-08-24 (later): AUGMENTATION EXPERIMENT CAME BACK NEGATIVE (job 2482543 trained job, job 2483050 inspection): with AUGMENT=true, same arm0 data, same 10k steps -- train/lm_loss 0.481 step / 0.507 epoch-mean, essentially unchanged from (marginally WORSE than) the no-augmentation run's 0.465/0.489. Samples 4/7/9 (same three studies as before, 50523025/50835299/50937713) STILL byte-identical, character for character, to each other AND to the pre-augmentation run's output for the same studies -- augmentation had ZERO measurable effect on the memorization pattern. Samples 1/3 (50173042/50484024) newly converged to a near-duplicate pair too (were distinct pre-augmentation) -- if anything, template convergence looks slightly WORSE, not better. Aggregate metrics within noise (rouge_l 0.284 vs 0.285, bleu_4 0.103 vs 0.121 -- down). CONCLUSION: this is a DATA-VOLUME problem, not a regularization problem -- 19881 examples / 184.9M trainable params / 16 epochs is fundamentally too little signal for crop+rotation alone to fix, consistent with 9E's a priori hypothesis that overfitting-driven effects should shrink with ~8-10x more data, not with augmentation at this scale. DECISION (assessed, not asked -- this was the user's own pre-stated conditional: 'confirm augmentation helps before committing the full-data GPU run to it', and the answer is 'it doesn't, don't gate the full-data run on it'): STOP tuning on arm0, wait for the Phase 8 fetch (job 2482266, was 72.3% done as of the prior note, not re-checked this turn). BUILT IN PARALLEL (commit 1934825, same turn): Phase 11C's retrieval-NN baseline, since arm0's rouge_l~0.28 numbers are now confirmed uninterpretable without an objective floor to compare against (per H100_SCALING_PLAN.md's own 11C entry, which explicitly says a generator that doesn't beat this baseline 'has contributed nothing'). evaluate_report_generation.py --retrieval-baseline: embeds query+gallery images with STOCK BiomedCLIP (no Phase-9 best contrastive checkpoint exists -- matches 10C's frozen-tower default), cosine-similarity nearest-neighbor, emits that training report verbatim, scored with the same metrics as --checkpoint mode for direct comparison. nearest_neighbor_indices() is the CPU-tested pure-tensor core; the BiomedCLIP-embedding path is untested-by-design (same evaluate_lm.py-style split as load_report_generation_module()). New scripts/retrieval_baseline_h100.sh (SLURM wrapper, same login-node-refuses-this pattern as the inspection tool; queries validation against the train gallery, never against itself). 2 new tests. validate_for_willi.sh: 138 passed (was 136), 9/9 gates green. H100_SCALING_PLAN.md: 11C marked '11C-infra' [x] (built, NOT yet run -- explicit in the note). NEXT ACTION FOR THE USER: git pull, then `sbatch scripts/retrieval_baseline_h100.sh` (defaults already point at arm0/train.parquet gallery + arm0/validate.parquet query, 10 samples, full ~19881-image gallery -- a few minutes on H100). Compare its rouge_l/bleu_1/bleu_4 directly against the 0.284/0.337/0.103 (augmented) or 0.285/0.325/0.121 (non-augmented) generator numbers already measured -- if the retrieval baseline scores comparably or higher, that's the final confirmation the arm0 generator hasn't learned anything beyond copying templates, and the ONLY path forward is the full-data run. Once Phase 8 finishes, relaunch training on DATASET_CONFIG=cxr_mimic_full (AUGMENT can stay on or off, given today's null result it's low-stakes either way) and re-run BOTH --checkpoint inspection and --retrieval-baseline against the new checkpoint before drawing any conclusion.",
    "2026-08-24 (later still): 11C RUN, RESULT DECISIVE (job 2483076, gx09, ~3min for the full 19881-image gallery + 10 queries): retrieval-NN baseline BEATS the arm0 generator on every metric -- rouge_l 0.369 vs 0.285(no-aug)/0.284(aug); bleu_1 0.396 vs 0.325/0.337; bleu_4 0.178 vs 0.121/0.103. Per 11C's own stated bar ('a generator that does not beat this baseline has contributed nothing'), the arm0-trained generator CONTRIBUTED NOTHING -- numerically confirms the qualitative --checkpoint-mode finding from earlier today. Notable: sample 7 (query 50835299) retrieved an EXACT-duplicate report (rouge_l=1.000) -- direct live confirmation of 6C-3's ~2% exact-duplicate-report finding, not a bug. Stock (non-fine-tuned) BiomedCLIP similarity is clearly imperfect (some retrievals, e.g. sample 6, pull a thematically-related-but-clinically-wrong ICU-device report) -- expected, since no Phase-9 best contrastive checkpoint exists yet; 10C/Phase-9 would sharpen this baseline further, making it an even higher bar. H100_SCALING_PLAN.md 11C updated with the concrete numbers (checkbox now '11C (arm0)', not '11C-infra'); flagged to rerun once a full-data generator exists, since more data could improve EITHER side of the comparison. USER SEPARATELY PASTED sc-helpdesk@hpi.de's answers to the Phase 7 courtesy-contact email's open questions (storage/quota/CPU-only-queue, first raised 2026-08-16, never definitively answered until now): (1) recommends /sc/scratch (global, NOT node-local -- different from the per-job local scratch used for _stage) or /sc/projects/sci-aisc for anything large/temporary, stay under 2TB soft guidance; (2) storage is CAPACITY-limited not INODE-limited, so the ~450k-file build is fine regardless of the ~200GiB-quota/inode-count uncertainty flagged back on 2026-08-17; (3) compute nodes have a REAL 1 Gbit/s internet uplink (shared -- avoid saturating it), which also explains where a good chunk of the ConnectTimeoutError noise throughout the fetch logs likely comes from (shared-uplink contention on THIS cluster, not purely PhysioNet-side throttling as earlier assumed -- doesn't change the 'don't raise WORKERS' conclusion, if anything reinforces it from a different angle since more local concurrency would worsen shared-uplink contention too); (4) CPU-only jobs on aisc-* without --gpus are fine, exactly what build_mimic_cxr_local.sh already does. USER FLOATED bulk-downloading the full ~500GB+ dataset to global scratch as a fallback IF the current streamed-fetch-and-discard approach fails -- ASSESSED AND AGREED WITH THE USER'S OWN INSTINCT: do NOT switch now. Job 2482266 is resumable, already 72.3%+ through as of the prior check, and none of the helpdesk answers reveal an actual problem with the current design (they just retroactively confirm assumptions already baked into it) -- switching would discard most-of-the-way-done progress for a fundamentally different, more complex approach (bulk download + local processing + manual 2TB quota tracking) with no evidence the current one is actually broken. Keep documented as the fallback, not the default. IMPORTANT CORRECTION PROPAGATED (commit not yet made as of writing -- see next): 11A's and 11B's own plan entries still said 'compute nodes have no internet' as of this morning, even though line 457's EARLIER correction (from the original Phase 7 investigation) already established this was a misreading of a 401 gated-dataset auth error, not a real connectivity gap -- the helpdesk's explicit '1 Gbit/s internet uplink' answer is now definitive, second, independent confirmation. Fixed 11A's METEOR-skip note and 11B's CheXbert-weights note to point at this correction -- 11B in particular may now be unblocked (CheXbert weights downloadable directly from a compute job, no separate staging step needed) though NOT attempted this turn (Phase 8 fetch is the priority; 11B is independent unblocked work worth returning to). No code changes this turn -- doc-only (H100_SCALING_PLAN.md + this file); validate_for_willi.sh not re-run (nothing under hybrid_xmamba/scripts/configs touched).",
    "2026-08-27: Phase 8I -- fetch job 2482266 essentially complete: 217999/218131 (99.94%). Started this invocation with 157597 already done / 60534 remaining, converted 60402 of those (converted-so-far counter is per-invocation, not cumulative -- confirmed by arithmetic: 157597+60402=217999). ~132 images dropped to retryable NameResolutionError/502 blips after per-chunk retries exhausted. Also clarified/corrected a stale 271M-param concern raised by user: 271M = 184.9M trainable decoder + 86.2M frozen BiomedCLIP tower (0 trainable); the decoder itself is already the intended ~183M 150M-family size (matches the existing [181,186]M parity band), not a bug -- recommended against downsizing to 70M since it does not address the actual root cause (data volume) and would abandon the Stage-0 150M checkpoint investment. Next: one more STAGE=fetch resubmit to mop up stragglers, then STAGE=pack EXCLUDE_HASHES=legacy_gallery_hashes.txt (never run for the full/non-arm0 build) to produce train/validate/test.parquet at cxr_mimic_full.yamls local_parquet_dir, then launch full-data Phase 10E training (EXPERIMENT=h100_report_gen_full sbatch scripts/train_report_generation_h100.sh -- DATASET_CONFIG and DECODER_CKPT already default correctly), then re-run BOTH --checkpoint inspection and --retrieval-baseline against the new checkpoint/full parquets before trusting any numbers (same discipline that caught arm0 memorization).",
    "2026-08-28: Phase 8 pack complete (job 2491325): 218121 rows -> leakage guard (hash-join 4288/3002=142.8%, expected/safe given known report-template duplication -- report_hash hashes text content not patient identity, so one legacy hash can match multiple full-corpus subjects sharing boilerplate) -> drops 22563 rows/3574 subjects -> train 191462 / validate 1433 / test 2663. Full-data Phase 10E training (job 2491338, EXPERIMENT=h100_report_gen_full) completed cleanly: 10k steps, decoder 0 missing/0 unexpected keys, train/lm_loss_epoch=1.123. Phase 11 eval (job 2491499 checkpoint inspection + job 2491505 retrieval-NN baseline, both n=10 default): generator rouge_l=0.197/bleu_1=0.241/bleu_4=0.031 vs retrieval floor rouge_l=0.224/bleu_1=0.252/bleu_4=0.055 -- generator still below floor on all 3, has not cleared the bar. BUT meaningfully different from arm0: the floor itself dropped hard (arm0 0.369 rouge_l -> full 0.224, expected on a properly-deduplicated 191k-subject gallery), and qualitative output is no longer byte-identical across patients like arm0 (same ~3 template families but real per-sample variation, still hallucinating ungrounded content e.g. NG-tube template on patients whose reference never mentions one). Leading hypothesis: UNDERTRAINING not memorization this time -- same 10k-step/batch-32 budget = ~1.7 effective epochs on the 9.6x-larger full train set (arm0 got ~16 epochs, which is why IT memorized). n=10 too small to trust standalone. Next: rerun both --checkpoint and --retrieval-baseline at NUM_SAMPLES=200 for a trustworthy read; check train/val loss trajectory near step 10000 for an undertraining signal (falling steeply near the end -> rerun with higher MAX_STEPS).",
    "2026-08-28 (later): 11C FULL-DATA n=1433 RUN (jobs 2491600 checkpoint-inspection + 2491687 retrieval-NN baseline, user ran both at NUM_SAMPLES=1433 = the entire validate.parquet, not the planned 200) REVERSES the n=10 preliminary read: generator BEATS the retrieval floor on all 3 metrics -- rouge_l 0.2075 vs 0.1881; bleu_1 0.2706 vs 0.2605; bleu_4 0.0707 vs 0.0465. Per 11C's own bar, the full-data generator has contributed something. Checked for the same memorization pattern that caught arm0: of 1433 generated reports, 1055 (73.6%) are exact-duplicate text across 184 template clusters (largest: 56/50/34/34/27 identical generations spanning different patients) -- far less collapsed than arm0 (near-total duplication) but still substantial boilerplate-templating, so some of the win over the retrieval floor is plausibly easy-boilerplate-vs-templated-reference rather than full per-patient grounding. Loss-trajectory undertraining check inconclusive: only last 3 checkpoints' val_lm_loss available in the log (step 9500=1.1095, 9750=1.1046, 10000=1.1007) -- shallow decline at the tail, not steep, but too short a window (no curve from step 0) to rule out undertraining either way. Source: user-pasted cluster session transcript logs/log1.log (not re-run by this session -- both jobs already completed on the cluster before the user shared the log; mid-session a macOS filesystem permission issue transiently blocked all read access to the repo root, resolved by the user, no data lost). H100_SCALING_PLAN.md updated: banner (line 8) + new '11C (full, n=1433)' checkbox entry with full numbers. Next: 11D (official subject-disjoint test split) and/or 11B (CheXbert F1, unblocked per the 1Gbit uplink correction) and/or 11E (qualitative appendix) -- no single next step forced by this result; user to choose.",
    "2026-08-28 (11B): CHEXBERT F1 HARNESS BUILT (infra only -- this session has no cluster/GPU access to run it live). scripts/evaluate_report_generation.py gained --chexbert (usable with --checkpoint, --retrieval-baseline, or --hyp-file/--ref-file) + --chexpert-csv: binary_f1_from_labels() computes per-label/micro/macro F1 over CHEXPERT_14_LABELS and the standard 5-label RRG subset CHEXBERT_5_LABELS (Atelectasis/Cardiomegaly/Consolidation/Edema/Pleural Effusion) from binary label matrices -- pure counting, no I/O, CPU-unit-tested with synthetic arrays (same testable/untested split as 11C's nearest_neighbor_indices vs embed_images). load_chexpert_ground_truth_labels() parses mimic-cxr-2.0.0-chexpert.csv.gz (U-Zeros convention: 1.0->positive, {0.0, -1.0 uncertain, NaN}->not-positive), unit-tested against a synthetic gzip CSV fixture. chexbert_ref_vs_ground_truth_agreement() cross-checks our own CheXbert labeling of the REFERENCE text against these official ground-truth labels for the same study_id -- a labeler-wiring sanity check per this item's original plan wording, not a generator-quality metric. THE ACTUAL TEXT->LABEL STEP (label_reports_with_chexbert) is backed by the f1chexbert pip package (auto-downloads chexbert.pth on first use) -- UNVERIFIED AGAINST REAL WEIGHTS, since building this required no cluster access and the package's exact API could not be confirmed live; ANY failure (missing package, download failure, API mismatch) is caught and degrades to a printed 'CheXbert labeling skipped: <error>' + None, the same contract meteor_score_corpus already uses for the nltk/wordnet guard -- so a wrong assumption here surfaces as a clear skip message, never a silently wrong number or a crash. inspect_report_generation_h100.sh and retrieval_baseline_h100.sh both gained CHEXBERT=true / CHEXPERT_CSV=... env levers (default false; empty-array expansion uses the repo's established set -u-safe ${ARR[@]+\"${ARR[@]}\"} pattern from eval_h100.sh, pinned by a new wrapper-convention test after almost using the bash<4.4-unsafe bare form). f1chexbert added to requirements.txt as a guarded optional dep (same pattern as nltk for METEOR). 12 new tests (F1 math incl. known-value and perfect/disjoint edge cases, ground-truth CSV parsing+binarization+filtering, graceful-skip-on-missing-package via monkeypatching sys.modules[name]=None, --chexbert flag key-presence gating, both SLURM wrapper conventions). validate_for_willi.sh: 147 passed (was 138 pre-Phase-8-pack; some of the delta between sessions is from unrelated Phase 8/10E work, not just this turn's 12), 5 skipped, 9/9 gates green. H100_SCALING_PLAN.md 11B marked '11B-infra' [x] (built, not run -- matches the 11C-infra precedent from 2026-08-24). NEXT ACTION FOR THE USER (this run IS the integration test for the f1chexbert assumption): git pull, then CHECKPOINT=./outputs/h100_report_gen_full/checkpoints/last.ckpt PARQUET=/sc/home/$USER/dataset/mimic_full/validate.parquet NUM_SAMPLES=1433 CHEXBERT=true sbatch scripts/inspect_report_generation_h100.sh (reuses the same checkpoint/data as the n=1433 ROUGE-L/BLEU run already done) -- if the log shows 'CheXbert labeling skipped: <error>', report that exact message back so the integration point (not the tested F1 math) can be fixed. 11D (official test split) and 11E (qualitative appendix) remain open and independent.",
    "2026-08-29: 11B LIVE-TESTED, API WAS WRONG, NOW FIXED AND VERIFIED. User ran CHEXBERT=true sbatch scripts/inspect_report_generation_h100.sh (job 2492037, same n=1433 checkpoint-inspection run as the earlier ROUGE-L/BLEU-only pass) -- the log shows the exact same aggregate rouge_l/bleu_1/bleu_4 as before (reproducible) plus a clean 'CheXbert labeling skipped: ModuleNotFoundError: No module named f1chexbert' -- confirms the previous session's graceful-degradation contract works correctly (no crash, no silently wrong number), but also that the package was never actually installed on the cluster venv. Used this as the trigger to verify the f1chexbert API for real (WebSearch + WebFetch of the package's own PyPI README and vilmedic's usage of it, since this session has no cluster access to test the installed package directly): the PREVIOUS integration guessed a WRONG API entirely -- F1CheXbert(device=...) constructor + an imagined per-report .get_label() method, backing a hand-rolled binary_f1_from_labels()/load_chexpert_ground_truth_labels() pipeline for the planned ground-truth-CSV cross-check. The REAL, confirmed API is F1CheXbert()(hyps=[...], refs=[...]) -> (accuracy, accuracy_per_sample, chexbert_all, chexbert_5), where chexbert_all/chexbert_5 are sklearn classification_report(output_dict=True)-style dicts (per-label + 'micro avg'/'macro avg' entries) -- f1chexbert computes labeling AND scoring end-to-end internally in one call, so the hand-rolled F1 math was both wrong-API AND unnecessary. REWROTE compute_chexbert_metrics to the real API; REMOVED binary_f1_from_labels, load_chexpert_ground_truth_labels, label_reports_with_chexbert, chexbert_ref_vs_ground_truth_agreement, and the --chexpert-csv CLI flag / CHEXPERT_CSV env var in both SLURM wrappers -- the public f1chexbert API exposes no per-report label vector, so the originally-planned ground-truth-CSV cross-check is genuinely not implementable against it (dropped, not deferred), not merely an integration detail to fix later. 5 tests removed with the dead functions (147->142); the 2 remaining CheXbert tests (graceful-skip-on-missing-package, --chexbert flag key-presence gating) still pass against the new API surface. validate_for_willi.sh: 142 passed, 9/9 gates green. NEXT ACTION FOR THE USER: git pull, pip install f1chexbert inside the cluster venv (confirmed real PyPI package v0.0.2, deps torch>=1.8.1/transformers>=4.23.1/scikit-learn/numpy/appdirs/pandas -- all but scikit-learn already present, pip resolves it), then rerun CHEXBERT=true sbatch scripts/inspect_report_generation_h100.sh against the full-data checkpoint. If it still skips, the printed message is the next thing to debug -- e.g. this session could not confirm whether f1chexbert's own chexbert.pth download mechanism respects/ignores the wrapper's default HF_HUB_OFFLINE=1 (f1chexbert is not confirmed to use the HF Hub for the download, but this is unverified either way).",
    "2026-08-29 (later): f1chexbert INSTALLED, HF_HUB_OFFLINE=1 CONFIRMED AS THE BLOCKER (open unknown from the previous note now resolved). `pip install f1chexbert` on the login node hit a cluster-wide custom error ('This command is disabled for users on our cluster'); `python -m pip install` next hit the separate generic 'not allowed on the login node!' guard; `srun --partition=aisc-batch ... --pty bash -c \"pip install...\"` was rejected outright ('Partition aisc-batch is batch-only; submit with sbatch only'); submitting the pip install itself as an sbatch job (`sbatch --wrap=\"...pip install f1chexbert\"`) hit the SAME cluster-wide pip block INSIDE the batch job too, confirming it's not login-node-specific; `python -m pip install` inside that same sbatch job got past the pip block (it's a different guard) but failed with `No module named pip` -- the project's .venv genuinely has no pip module, consistent with the cluster's docs (docs.sc.hpi.de/cluster/software_installation/Python-Conda/) which mandate conda and only tolerate pip 'if not possible' (f1chexbert confirmed NOT on conda-forge via WebSearch, so this is exactly that exception case -- but the bare pip binary is blocked regardless of justification). FIX: `sbatch --wrap=\"source .venv/bin/activate && uv pip install f1chexbert\"` (job 2493754) -- uv is the SAME tool setup_env_h100.sh already uses to build this .venv, specifically because bare pip is blocked cluster-wide; succeeded cleanly (Installed 6 packages: appdirs, f1chexbert==0.0.2, joblib, narwhals, scikit-learn, threadpoolctl). Reran CHEXBERT=true sbatch scripts/inspect_report_generation_h100.sh at the full NUM_SAMPLES=1433 (job 2493764): ROUGE-L/BLEU reproduced bit-for-bit against jobs 2491600/2491687/2492037 (rouge_l 0.2075, bleu_1 0.2706, bleu_4 0.0707), confirming no regression from any of this session's changes. CheXbert this time printed a NEW, more specific skip: `OSError: We couldn't connect to 'https://huggingface.co' ... and couldn't find them in the cached files` -- this IS the open unknown from the previous note, now answered: f1chexbert downloads its underlying checkpoint from HF Hub on first use, and DOES respect HF_HUB_OFFLINE (transformers reads this env var globally), which both SLURM wrappers export as `${HF_HUB_OFFLINE:-1}` by default (correct for the report-gen model itself, wrong for f1chexbert's first-ever download on this node, since nothing is cached yet). Compute nodes have real internet (proven by the uv install succeeding via sbatch minutes earlier), so this is purely an env-var default fighting itself, not a genuine network-access wall. NEXT ACTION FOR THE USER: rerun with the offline flag overridden so the download can happen once and populate the cache -- cheap test first: `CHECKPOINT=./outputs/h100_report_gen_full/checkpoints/last.ckpt PARQUET=/sc/home/$USER/dataset/mimic_full/validate.parquet NUM_SAMPLES=10 CHEXBERT=true HF_HUB_OFFLINE=0 sbatch scripts/inspect_report_generation_h100.sh`; if that produces real CheXbert F1 numbers (not another skip), rerun at the full NUM_SAMPLES=1433 the same way (second run should hit the now-warm cache regardless of the flag). No code change needed for this fix -- it's a run-time env override, not a bug in evaluate_report_generation.py.",
    "2026-08-29 (later still): TWO MORE f1chexbert BUGS FOUND, ISOLATED VENV BUILT. After HF_HUB_OFFLINE=0 fixed the huggingface.co connection error, hit `FileNotFoundError: ~/.cache/chexbert/chexbert.pth` -- traced to a real bug in f1chexbert's own source (pulled the sdist tarball from PyPI directly, no GitHub repo exists): its `download_model()` helper calls `hf_hub_download(..., force_filename=f)` inside a bare `except Exception: print(e)` that swallows the failure; `force_filename` was removed from modern `huggingface_hub`, so the download silently fails and the checkpoint is never written. Fixed with a one-off manual download+copy to the exact expected path (job 2493899, after job 2493894 failed by landing on ARM node ga03 with no --exclude). Next run got past that but hit a THIRD bug: `AttributeError: BertTokenizer has no attribute encode_plus` -- that method was removed in transformers>=5.0, and f1chexbert==0.0.2 (2023-era) is fundamentally incompatible with the modern transformers this cluster's uv install pulled in. Rather than downgrade the shared main .venv's transformers (load-bearing for GPT-2 tokenizer/BiomedCLIP/BioMedLM elsewhere), user chose to build a fully ISOLATED venv just for CheXbert scoring, decoupled via plain hyps.txt/refs.txt files. See the 11B task entry for the full build (write_hyps_refs + --dump-dir in evaluate_report_generation.py; new score_chexbert_standalone.py + setup_chexbert_venv_h100.sh + score_chexbert_h100.sh; DUMP_DIR lever on both existing wrappers; 8 new tests, 150 passed 9/9 gates). NEXT: sbatch scripts/setup_chexbert_venv_h100.sh, then rerun with DUMP_DIR=... to dump hyps/refs, then score_chexbert_h100.sh against them -- this is the real integration test.",
    "2026-08-30: ISOLATED-VENV ARCHITECTURE EXERCISED LIVE, A FOURTH f1chexbert BUG FOUND AND FIXED. User ran all 3 staged next-actions. (1) setup_chexbert_venv_h100.sh (job 2493922) succeeded cleanly: torch==2.13.0+cpu, transformers==4.57.6, f1chexbert import OK. (2) n=1433 --checkpoint inspection with DUMP_DIR=results/report_gen_full_n1433 and no --chexbert (job 2493923) succeeded: aggregate ROUGE-L/BLEU reproduced bit-for-bit against every prior run of this same n=1433 eval (rouge_l 0.2075, bleu_1 0.2706, bleu_4 0.0707, meteor null since nltk is absent -- graceful degradation working as designed), and dumped 1433 hyp/ref pairs to hyps.txt/refs.txt -- confirms --dump-dir works correctly end to end without disturbing the existing metrics path. (3) score_chexbert_h100.sh (job 2494015) FAILED with `ValueError: too many values to unpack (expected 3)` at f1chexbert.py:229 (`y_type, y_true, y_pred = _check_targets(...)`) -- a 3-value unpack of the PRIVATE sklearn.metrics._classification._check_targets API. ROOT-CAUSED (not guessed) by diffing scikit-learn's own source directly across git tags via raw.githubusercontent.com: _check_targets(y_true, y_pred) returned exactly (y_type, y_true, y_pred) through tag 1.7.2; scikit-learn 1.8.0 added a sample_weight param and return value, making it a 4-tuple. f1chexbert has no scikit-learn upper pin (same missing-ceiling pattern as its missing transformers<5 pin), so step (1)'s unpinned `uv pip install f1chexbert` silently pulled scikit-learn>=1.8.0 into .venv_chexbert. FIXED: scripts/setup_chexbert_venv_h100.sh now pins `\"scikit-learn<1.8\"` explicitly (both uv and pip-fallback branches), and its smoke test now prints the installed scikit-learn version too. New parity test test_setup_chexbert_venv_h100_slurm_wrapper_pins_scikit_learn_below_1_8 (mirrors the existing transformers<5 pin test). validate_for_willi.sh: 151 passed (was 150), 9/9 gates green. NEXT ACTION FOR THE USER: git pull; `sbatch scripts/setup_chexbert_venv_h100.sh` again to REBUILD .venv_chexbert with the fixed pin (the existing venv from job 2493922 has the broken scikit-learn baked in, must be rebuilt not reused); then `DUMP_DIR=results/report_gen_full_n1433 sbatch scripts/score_chexbert_h100.sh` again -- hyps.txt/refs.txt from job 2493923 are already on disk, no need to regenerate. This is the fourth attempt at the actual integration test and should finally produce real CheXbert F1 numbers.",
    "2026-08-30 (later): setup_chexbert_venv_h100.sh IS NOT RERUNNABLE IN PLACE -- FOUND AND FIXED. User ran `sbatch scripts/setup_chexbert_venv_h100.sh` to pick up the scikit-learn<1.8 pin (job 2494759): FAILED immediately with `error: Failed to create virtual environment / Caused by: A virtual environment already exists at: .venv_chexbert`. Root cause: `uv venv` refuses to overwrite an existing venv directory unless passed `--clear` (or UV_VENV_CLEAR=1) -- job 2493922 had already created .venv_chexbert once, and the script's `uv venv --python ... VENV_DIR` line had no --clear flag, so ANY rerun of this script (not just this specific pin fix) would hit this. Directly self-defeating for a workflow whose whole point is 'find a bad pin, fix it, rebuild the venv'. FIXED: added --clear to both the uv venv invocation and the python -m venv fallback branch, so the script is idempotent/resubmittable in place going forward. New parity test test_setup_chexbert_venv_h100_slurm_wrapper_is_rerunnable_in_place (asserts --clear appears at least twice, once per branch). validate_for_willi.sh: 152 passed (was 151), 9/9 gates green. NEXT ACTION FOR THE USER: git pull; `sbatch scripts/setup_chexbert_venv_h100.sh` again (will now actually clear+rebuild); then `DUMP_DIR=results/report_gen_full_n1433 sbatch scripts/score_chexbert_h100.sh`.",
    "2026-08-30 (later still): --clear ITSELF UNRELIABLE ON THIS CLUSTER'S NFS HOME FILESYSTEM -- FOUND AND FIXED. `sbatch scripts/setup_chexbert_venv_h100.sh` with the --clear fix applied (job 2494771) FAILED again, differently: `error: Failed to create virtual environment / Caused by: failed to remove directory .../.venv_chexbert/lib: Directory not empty (os error 39)`. `uv venv --clear`'s own internal directory-removal logic cannot reliably wipe the large existing site-packages tree (torch/transformers/scikit-learn/f1chexbert, many nested files) on this cluster's NFS-backed /sc/home filesystem. FIXED (more robust than depending on either tool's internal --clear behavior): the script now does an explicit `rm -rf \"${VENV_DIR}\"` itself before calling `uv venv`/`python -m venv` with no --clear flag at all -- predictable, matches this repo's existing explicit-cleanup convention (e.g. the /tmp/mimic_smoke_test rm -rf in the Phase 8 build notes) rather than trusting a tool's internal clear implementation on an unusual filesystem. Updated parity test test_setup_chexbert_venv_h100_slurm_wrapper_is_rerunnable_in_place to assert the rm -rf pattern instead of --clear. validate_for_willi.sh: 152 passed (unchanged count, same test updated not added), 9/9 gates green. NEXT ACTION FOR THE USER: git pull; `sbatch scripts/setup_chexbert_venv_h100.sh` again (third attempt at just getting the venv rebuilt); then `DUMP_DIR=results/report_gen_full_n1433 sbatch scripts/score_chexbert_h100.sh`. hyps.txt/refs.txt from job 2493923 remain valid and on disk throughout all of this venv-tooling back-and-forth -- no regeneration needed.",
    "2026-08-30 (finally): REAL CHEXBERT F1 NUMBERS PRODUCED. FIVE PACKAGE/TOOLING BUGS FIXED TOTAL. Job 2494776 (setup_chexbert_venv_h100.sh with the rm -rf fix) SUCCEEDED cleanly: scikit-learn==1.7.2 resolved as expected (below the 1.8 ceiling), f1chexbert import OK. Job 2494784 (DUMP_DIR=results/report_gen_full_n1433 score_chexbert_h100.sh) SUCCEEDED -- the CheXbert integration test finally passed after five separate f1chexbert-package/tooling bugs across this whole saga (wrong assumed API; HF_HUB_OFFLINE blocking download; force_filename removed from hf_hub_download; encode_plus removed in transformers>=5.0; _check_targets 4-tuple in scikit-learn>=1.8.0; plus the two venv-rebuild tooling failures just above, which are a sixth/seventh issue depending how you count but not f1chexbert's fault). RESULT (n=1433, validate.parquet): CheXbert F1 (14-label) micro/macro = 0.3097/0.1548; CheXbert F1 (5-label) micro/macro = 0.3059/0.1983; exact-match label-set accuracy = 0.3531. Saved to results/report_gen_full_n1433/chexbert_metrics.json with the full per-label precision/recall/F1/support breakdown. CLEARS the plan's pre-registered Floor tier (ROUGE-L>=0.15 -- 0.2075 clears it; CheXbert-14-micro>=0.25 -- 0.3097 clears it) but sits well below Target (ROUGE-L>=0.22, CheXbert-14-micro>=0.40). PER-LABEL PATTERN (the actual finding, not just the aggregate number): precision is consistently much higher than recall across almost every label -- e.g. Atelectasis 0.41P/0.04R (support 446), Edema 0.40P/0.08R (support 263), Pleural Effusion 0.73P/0.15R (support 423) -- with Cardiomegaly the one real exception (0.45P/0.63R, support 443). FOUR of 14 labels are never predicted at all: Lung Lesion, Pneumonia, Pneumothorax, Pleural Other (all F1=0.0). This is the CheXbert-level confirmation of 11C's 73.6%-exact-duplicate-template finding: the generator has learned a small set of safe, generic 'normal chest' templates that score reasonably on ROUGE-L/BLEU against a corpus where many reports genuinely are near-normal, but it essentially never commits to asserting a rare or serious finding -- exactly the failure mode a clinical deployment would care most about, and exactly what raw n-gram overlap metrics alone would NOT have surfaced (this is precisely why CheXbert F1 was worth the five-bug fight to get working). TWO CAVEATS BEFORE TREATING 0.3097 AS THE FINAL HEADLINE NUMBER: (1) this run is on validate.parquet, NOT the official subject-disjoint test split -- that comparison is Phase 11D, still open, and the plan explicitly calls for reporting the OFFICIAL split number, not this one, in the final writeup; (2) the retrieval-NN baseline (11C, jobs 2491600/2491687) has ROUGE-L/BLEU numbers but NO CheXbert F1 yet -- the plan's own PRE-REGISTERED rule (written before any CheXbert number existed) states 'the retrieval-NN baseline is run FIRST and its ROUGE-L/CheXbert F1 become the real floor,' so strictly by that rule the generator's CheXbert F1 win over retrieval has not yet been demonstrated, only assumed by analogy to the ROUGE-L/BLEU win. NEXT ACTION FOR THE USER (in order of what actually unblocks the strongest claim): (1) score CheXbert on the retrieval-NN baseline for the true floor comparison -- `DUMP_DIR=results/retrieval_floor_n1433 sbatch scripts/retrieval_baseline_h100.sh` (reuses the existing 191,462-pair train gallery + validate.parquet from job 2491687) then `DUMP_DIR=results/retrieval_floor_n1433 sbatch scripts/score_chexbert_h100.sh` against that dump, using the now-working isolated venv (no more setup needed, .venv_chexbert already built correctly); (2) independently, 11D (official subject-disjoint test split, the number that actually belongs in the final writeup) and 11E (qualitative appendix, N=20 side-by-side generated/ground-truth pairs including failure cases) remain open and can proceed in either order.",
    "2026-08-30 (retrieval-floor attempt, invalid -- MISLED BY MY OWN NEXT-ACTION INSTRUCTIONS, FOUND AND FIXED). User followed the note above's exact command (DUMP_DIR=results/retrieval_floor_n1433 sbatch scripts/retrieval_baseline_h100.sh, NO other env vars set, job 2494817) then score_chexbert_h100.sh (job 2495070). RESULT LOOKED PLAUSIBLE BUT IS NOT THE INTENDED COMPARISON: aggregate rouge_l=0.3688/bleu_1=0.396/bleu_4=0.178 over only n=10 samples matches the CLOSED arm0 retrieval-NN numbers from 11C(arm0) (rouge_l 0.369) almost exactly, not the intended full-data floor (rouge_l 0.188, n=1433, jobs 2491600/2491687). ROOT CAUSE (my own instruction was wrong, not a cluster/package issue this time): retrieval_baseline_h100.sh's TRAIN_PARQUET/PARQUET env-var defaults still pointed at /sc/home/$USER/dataset/mimic_full/arm0/{train,validate}.parquet -- stale from before Phase 8's full-data pack landed, never updated when h100_report_gen_full training (job 2491338) became the active target. The 'reuses the existing ... gallery ... from job 2491687' claim in the prior note's NEXT ACTION was FALSE -- job 2491687 must have had explicit TRAIN_PARQUET/PARQUET overrides that this session never captured or told the user to reproduce, and NUM_SAMPLES was also left at its default of 10 rather than 1433. WHILE INVESTIGATING found the SAME staleness bug in inspect_report_generation_h100.sh's CHECKPOINT (defaulted to outputs/h100_report_gen_arm0/... instead of h100_report_gen_full/...) and PARQUET defaults too -- every real full-data run of THAT script so far has also depended on the user manually overriding CHECKPOINT+PARQUET each time (which they always did, correctly, in every actual command run so far -- this bug just never got exercised by accident until this retrieval-baseline run). FIXED (real script defaults, not just an instruction correction, since this exact mistake could recur on 11D/11E work too): inspect_report_generation_h100.sh's CHECKPOINT now defaults to ./outputs/h100_report_gen_full/checkpoints/last.ckpt and its PARQUET to mimic_full/validate.parquet (no arm0/); retrieval_baseline_h100.sh's TRAIN_PARQUET/PARQUET now default to mimic_full/train.parquet and mimic_full/validate.parquet (no arm0/) -- arm0 (CLOSED per CLAUDE.md's Phase 9 note) is still fully usable, just requires an explicit override now instead of being the silent default. 2 new parity tests (test_inspect_report_generation_h100_slurm_wrapper_defaults_to_full_data_not_arm0, test_retrieval_baseline_h100_slurm_wrapper_defaults_to_full_data_not_arm0) assert the exact corrected default strings and that 'arm0' does not appear inside either variable's actual assignment line (comments referencing arm0 as historical context are fine and expected). validate_for_willi.sh: 154 passed (was 152), 9/9 gates green. NEXT ACTION FOR THE USER: `git pull`; rerun `DUMP_DIR=results/retrieval_floor_n1433 NUM_SAMPLES=1433 sbatch scripts/retrieval_baseline_h100.sh` (defaults now correctly point at full data, but NUM_SAMPLES must still be set explicitly -- its default stays 10 for cheap sanity checks) -- this overwrites the invalid n=10 arm0 dump already in that directory with the correct n=1433 full-data one; then `DUMP_DIR=results/retrieval_floor_n1433 sbatch scripts/score_chexbert_h100.sh` again against the corrected dump. This is the actual, valid full-data retrieval-floor CheXbert comparison the previous note intended."
  ],
  "rejected_by_prior_evidence": {
    "vit_unfreeze_blocks_0": "Already run (jobs 1942/1948/1949): MIMIC 10.45%->7.97%, Indiana IDENTICAL 3.90%. Freezing the ViT loses in-domain and recovers nothing cross-domain. ViT-unfreeze=2 is a proven pure +2.5pp win.",
    "freq_kd_true": "Already run (jobs 1922/1923 vs 1930/1931): freq_kd ON -> Indiana 2.96%, OFF -> 3.90%. Cross-domain regression; directly attacks the Phase-7 Indiana >=4.04% gate. yaml default flipped to false 2026-07-21 (was only false in practice via launch-script override).",
    "checkpoint_on_retrieval_metric": "SELECTION-ON-TEST. mimic_cxr.yaml sets validation_split == test_split == 'train[90%:]', the same 3063 pairs evaluate_cxr_retrieval.py:346 evaluates. Legitimate version = carve a third disjoint selection split out of train[:90%] -- that is Phase 6F-1.",
    "best_vs_last_checkpoint_claim": "FALSE. The 0.1113 was measured on contrastive-step=002750-val/total_loss=3.6083.ckpt, which IS the best-by-val/total_loss checkpoint (train_contrastive.py:801-806, monitor=val/total_loss mode=min save_top_k=3). It is not a last-checkpoint artifact and there is no free ~0.9pp to recover. Chasing the 0.120-0.122 in-training peak is the banned selection-on-test.",
    "xbm_cross_batch_memory_queue": "This IS the MoCo queue. Already ablated in-repo and found harmful post-KD-warmup; moco_queue_size=0 is canonical and appears in lessons-carried. Renaming it does not make it new evidence.",
    "image_backbone_swap_raddino_medsiglip": "Breaks assert img_out == model.embed_dim (lightning_module.py:419-422) and removes the BiomedCLIP 512-d joint space that the KD teacher targets. Not a lever -- a different project. Also dilutes an MSc contribution that is about the TEXT tower.",
    "image_resolution_increase": "BiomedCLIP ViT-B/16 position embeddings are fixed at 224; interpolating them perturbs the frozen joint space the whole design depends on. Poor cost/benefit here.",
    "text_only_distillation_to_raise_cos": "Optimizes cos_text_teacher, which already reaches 0.89 under pure KD (see 2026-07-25 falsification note). Not the bottleneck.",
    "last_token_to_mean_pooling": "Moot. v2 configs set pooling_strategy=attention (hybrid_lm.py:386-390); last-token pooling was never used.",
    "cos_text_teacher_as_architecture_gate": "FALSIFIED 2026-07-25. It is a KD-vs-CLIP loss-weight equilibrium, not an architecture ceiling. Any kill criterion of the form 'did cos exceed 0.62' draws a wrong conclusion.",
    "mimic_cxr_dicom_4_7TB": "WRONG PROJECT. Use MIMIC-CXR-JPG (~570 GB, the SAME 377,110 images, no pydicom, no windowing decisions to defend in a viva). Reports still come from MIMIC-CXR (the 135 MB mimic-cxr-reports.zip).",
    "storing_mimic_jpg_at_native_resolution": "The model sees 224x224 and source images average ~2500x3056, so ~99% of every pixel array is discarded on load. Downscale in flight; that 99% never touches the 200 GB quota. ~310-400 GB transferred, ~6 GB kept at 320px.",
    "keeping_the_hf_mirror_once_credentialed": "itsanmolgupta/mimic-cxr-dataset is a third-party redistribution of credentialed data whose provenance (which studies, which views, which section parser, whether it is subject-disjoint across the 90/10 cut) CANNOT BE STATED. Live viva vulnerability and a DUA concern. Migrate to the PhysioNet build.",
    "pairing_lateral_views_to_the_study_report": "Manufactures guaranteed in-batch false negatives -- the thing 6C-3 MEASURED this dataset does not currently have (0.58 pairs per bs=64 batch) -- and mixes two visual distributions. Frontal-only, one image per study.",
    "rerunning_stage0_from_scratch": "ASKED 2026-08-16 -- NO. (1) Backbone quality was MEASURED not to transfer: PPL 15.62 -> 13.18 moved retrieval FLAT, null #1 of 10. (2) Stage 0 trains on PUBMED ABSTRACTS; the full-MIMIC build changes nothing it sees -- the two are orthogonal. (3) The 13.18 checkpoint is hard-won (four failed runs, collapse at steps 3k/24k/28k, resolved only by the fp32 Mamba-scan + mLSTM-exp-gate fix) and costs ~3 DAYS of H100 wall; re-running re-exposes that fragility for zero expected gain. HONEST NUANCE THE PIVOT INTRODUCES: the null was measured on RETRIEVAL, which uses the backbone as an ENCODER (pooled embedding); report generation uses it as a GENERATOR (autoregressive decoding = exactly what LM pretraining optimises), so the null does NOT automatically carry over. That argues for KEEPING the 13.18 checkpoint, not rebuilding it. The legitimate cheap version is Phase 10G (domain-adaptive continuation on ~47M tokens of MIMIC report text, hours not days, run as an ablation against the untouched 13.18 control).",
    "cross_protocol_number_comparison": "NEVER compare a full-MIMIC number on the OFFICIAL split against 0.1459 on the LEGACY train[90%:] gallery. The official split is subject-disjoint BY CONSTRUCTION; the legacy gallery has UNKNOWN PROVENANCE and may leak subjects across its own 90/10 cut. The new protocol is HARDER, so the number can go DOWN while the model gets BETTER. Phase 9A (Arm 0) exists precisely to separate 'the pipeline changed' from 'the protocol got harder' from 'the model changed'.",
    "reopening_retrieval_arms": "Retrieval is CLOSED as of 2026-08-16. 10 nulls, 1 lever, and that lever is exhausted (depth saturated at 12/12, vit_lr inverted-U with 1e-6 optimal, scope null). The Phase-9 arms exist to extend the SUPPORTING chapter onto full data, not to chase a higher R@10. The optimization target is ROUGE-L / CheXbert F1."
  }
}
